| Task   | Status | Date       | Notes                                                              |
|--------|--------|------------|---------------------------------------------------------------------|
| T-0.1  | done   | 2026-09-03 | CLT/Homebrew/git pre-existing. CocoaPods, Android Studio via brew. JDK 17 via manual Temurin tar.gz (see DECISIONS.md). Xcode 26.6 installed from App Store, switched via xcode-select, license accepted, iOS 26.5 simulator runtime installed. |
| T-0.2  | done   | 2026-09-03 | FVM 4.3.0 installed. Flutter 3.47.2 (stable) installed and pinned via `fvm use stable`. Version recorded in DECISIONS.md. |
| T-0.3  | done   | 2026-09-03 | Flutter project created in place at repo root (not `~/Developer`, see DECISIONS.md) with org `com.panicker`, name `kharcha`, platforms android+ios. |
| T-0.4  | done   | 2026-09-03 | `.gitignore` extended per §4.7. `docs/SPEC.md`, `docs/DECISIONS.md`, `docs/PROGRESS.md` created. |
| T-0.5  | done   | 2026-09-03 | All packages from §9.3 added except `custom_lint`/`riverpod_lint` (deferred, see DECISIONS.md). `integration_test` added as an SDK dependency. |
| T-0.6  | done   | 2026-09-03 | `flutter_lints` (from template) + default `analysis_options.yaml`. `flutter analyze --fatal-infos` passes with 0 issues. |
| T-0.7  | done   | 2026-09-03 | Android `minSdk = 26`, `compileSdk = 37` (bumped from default 36 for plugin requirements), core library desugaring enabled for `flutter_local_notifications`. iOS deployment target 15.0 (Flutter template default). |
| Gate 0 | partial | 2026-09-03 | **Android: PASSED** — empty app built, installed, and ran on emulator `kharcha_test` (Pixel 6, API 34), confirmed via live `flutter run` session. **iOS: BLOCKED**, not passed — see DECISIONS.md for the `com.apple.provenance`/codesign issue. Proceeding to Phase 1 on the Android path per user instruction; iOS to be revisited later. |
| T-1.1  | done   | 2026-09-03 | User created a Supabase account and the `kharcha` project (org "Panicker Family") in `ap-south-1` (Mumbai) via the dashboard, guided over Claude-in-Chrome. Project ref `jqorwgiowfxxgjvayznj`, URL `https://jqorwgiowfxxgjvayznj.supabase.co`. Project uses Supabase's newer key format (`sb_publishable_...`) instead of a legacy JWT anon key — used as the `SUPABASE_ANON_KEY` value. `config/dev.json` created (gitignored) with URL + publishable key; `config/example.json` created (committed placeholder). |
| T-1.2  | done   | 2026-09-03 | Supabase CLI 2.116.0 installed via `brew tap supabase/tap && brew install supabase/tap/supabase`. `supabase init` run at repo root. `supabase login`'s automatic browser flow does not work in a non-TTY/agent shell (`LegacyLoginMissingTokenError`) — worked around by having the user generate a personal access token from the dashboard and run `export SUPABASE_ACCESS_TOKEN=...` + `supabase link --project-ref jqorwgiowfxxgjvayznj` themselves in their own terminal (kept out of the chat transcript deliberately). Link succeeded. |
| T-1.3  | done   | 2026-09-03 | Migrations `0001_extensions.sql`–`0004_budgets_recurring.sql` written verbatim from spec §6.2–§6.5 and pushed to the live project via `supabase db push` (run by the user, token stayed in their shell). |
| T-1.4  | done   | 2026-09-03 | `0005_functions_triggers.sql` written from spec §6.6 and pushed live. |
| T-1.5  | done   | 2026-09-03 | `0006_rls.sql` written from spec §7 and pushed live (after the app_releases ordering fix — see DECISIONS.md). All 10 target tables show RLS enabled in the dashboard. |
| T-1.6  | done   | 2026-09-03 | `0007_views.sql` (reporting views + `get_dashboard` RPC) and `0010_app_releases.sql` pushed live. All 4 views (`v_month_household`, `v_month_user`, `v_month_category`, `v_month_income`) visible in Table Editor. |
| T-1.7  | done   | 2026-09-04 | `0008_storage.sql` (receipts bucket + policies) and `0009_seed.sql` pushed live — verified: household row + 20 categories + 6 payment methods present in Table Editor. 4 auth users created via the dashboard: Rupesh, Tanish, Trupti (`role='member'`) and Vineet (`role='admin'`), all auto-linked to the household via the `handle_new_user` trigger, `display_name`/`role` set via SQL editor. Household deliberately shipped with 4 members instead of the spec's 5 (per user — no 5th member needed right now). Sign-ups disabled: Authentication → Providers → Email → "Allow new users to sign up" turned off and saved ("Successfully updated settings"). |
| T-1.8  | done   | 2026-09-04 | All 8 RLS checks from spec §7.1 executed in the SQL editor via JWT-claim impersonation (`begin; set_config(...); set local role authenticated; <test>; rollback;`), against 2 throwaway expense rows inserted and later removed. All 8 passed as specified — see DECISIONS.md for the full per-check results. No policy changes needed. |
| **Gate 1** | **passed** | 2026-09-04 | Covered by the T-1.8 RLS checklist: RLS-1 confirmed a member JWT (Rupesh) can read all household expenses; RLS-2/3/4 confirmed a member cannot update, delete, or insert-as another member. |
| **Bug found & fixed** | — | 2026-09-03 | `0006_rls.sql` (as copied verbatim from spec §7) enabled RLS on `app_releases` before that table exists (it's created in `0010_app_releases.sql`) — `supabase db push` failed with `relation "public.app_releases" does not exist`. Fixed by moving the `app_releases` RLS-enable + policy from `0006_rls.sql` into `0010_app_releases.sql`. Recorded in DECISIONS.md as a spec correction. |
| T-2.1  | done   | 2026-09-04 | `core/config/app_config.dart` implemented verbatim from spec §5.6. |
| T-2.2  | done   | 2026-09-04 | `core/money/money.dart`: `Money` extension type over paise, Indian-grouping `format()` via `intl`'s `en_IN` locale, compact L/Cr/k formatting, `tryParse`. 15 unit tests in `test/unit/core/money_test.dart`, incl. 1 lakh, 1 crore, and 99.995 rounding. |
| T-2.3  | done   | 2026-09-04 | `core/time/app_time.dart`: fixed UTC+5:30 offset helpers (no `timezone` package needed here — that lands in Phase 13 for notification scheduling). 9 unit tests incl. the 23:55-on-the-30th boundary case. |
| T-2.4  | done   | 2026-09-04 | `Result`/`Ok`/`Err` (`core/result/result.dart`), `Failure` taxonomy (`core/errors/failure.dart`), `ErrorMapper` (`core/errors/error_mapper.dart`), `AppLogger` with a 500-entry ring buffer (`core/logging/app_logger.dart`). 6 unit tests for the mapper's classification. |
| T-2.5  | done   | 2026-09-04 | All 11 Drift tables mirroring §6 (`Households`, `Profiles`, `Categories`, `PaymentMethods`, `Expenses`, `Incomes`, `Attachments`, `Budgets`, `RecurringRules`) plus `OutboxEntries` + `SyncMeta`, each syncable table carrying `syncStatus`/`localUpdatedAt`/`isDirty`. `AppDatabase` in `core/db/app_database.dart`, `schemaVersion = 1`. `dart run build_runner build` generates cleanly. |
| T-2.6  | done   | 2026-09-04 | 9 DAOs written (`ExpenseDao`, `IncomeDao`, `CategoryDao`, `PaymentMethodDao`, `BudgetDao`, `RecurringDao`, `AttachmentDao`, `OutboxDao`, `SyncMetaDao`) under `core/db/daos/`, each with watch/find/upsert/softDelete/markSynced/hardDelete. In-memory Drift tests in `test/unit/db/expense_dao_test.dart` (insert/read, soft-delete filtering + ordering, dirty/pending flagging). |
| T-2.7  | done   | 2026-09-04 | Freezed domain models for all 9 entities + 6 enums under `domain/models/`, each with `fromJson`/`toJson` and snake_case `@JsonKey`s matching the DB columns exactly. Mapper extensions (`toDomain()` / `toCompanion()`) between Drift rows and domain models under `data/local/mappers/` — see DECISIONS.md for the naming-collision handling this required. 9 JSON round-trip tests. |
| T-2.8  | done   | 2026-09-04 | Riverpod (`flutter_riverpod` + `riverpod_generator`, code-generated) wired via `ProviderScope` in `main.dart`; `appDatabaseProvider` (keepAlive) in `core/db/database_provider.dart`. |
| T-2.9  | done   | 2026-09-04 | `go_router` set up in `routing/app_router.dart` with the full route map from spec §10.1 (`StatefulShellRoute.indexedStack` for the 4-tab bottom nav + top-level modal routes for expense/income/budget/recurring/etc.), backed by placeholder screens under `features/*/screens/`. Auth redirect logic (T-3.4) not yet wired — router boots straight to the dashboard for now. |
| **Gate 2** | **passed** | 2026-09-04 | `fvm flutter analyze --fatal-infos` clean. `fvm flutter test` green (44 tests). Verified live on the Android emulator (`kharcha_test`, Pixel 6-class, API 34) via `fvm flutter run --dart-define-from-file=config/dev.json`: app builds, installs, and boots straight to the placeholder Dashboard with working bottom-nav tabs and FAB — no crashes, no Dart exceptions. Screenshot captured via `adb screencap`. |
| T-3.1  | done   | 2026-09-04 | `Supabase.initialize()` (with `publishableKey:`, not the deprecated `anonKey:`) plus `timezone` init added to `main()`'s bootstrap, per spec §11.1 steps 1–4. Steps 6 (notifications) and 8 (kick off sync) deferred to Phase 13/Phase 4 as noted inline. |
| T-3.2  | done   | 2026-09-04 | `AuthRepository` (`data/repositories/auth_repository.dart`): sign in, sign out, reset password, `currentSession`, `onAuthStateChange`, all returning `Result<void, Failure>`. Unit tests in `test/unit/data/auth_repository_test.dart` against a mocktail-mocked `GoTrueClient`. |
| T-3.3  | done   | 2026-09-04 | Full login screen + `LoginController`: inline email/password validation, password-visibility toggle, "Forgot password?" → reset email + "Check your email" snackbar. `ErrorMapper`'s invalid-credentials message corrected to the spec-exact "Email or password is incorrect." (was "Incorrect email or password." from T-2.4, before this string was load-bearing) — see DECISIONS.md. |
| T-3.4  | done   | 2026-09-04 | Router `redirect` + `GoRouterRefreshStream` (bridges Supabase's `onAuthStateChange` to go_router's `refreshListenable`): signed-out → `/login` always; signed-in bounced off `/login`/`/splash` → dashboard. `initialLocation` changed from the dashboard to `/splash`. |
| T-3.5  | done   | 2026-09-04 | New `ProfileDao` (registered on `AppDatabase`) + `ProfileRepository` + `currentProfileProvider`: resolves the signed-in member's profile from the Drift cache first (works offline immediately), with a best-effort background refresh from Supabase's `profiles` table. |
| T-3.6  | done   | 2026-09-04 | `AppDatabase.wipeAll()` (deletes every row from every table) wired into a new `SignOutController`, triggered from a minimal Sign-out control added to the Settings placeholder (full Settings UI is Phase 14). Offline session tolerance itself needed no new code — it's `supabase_flutter`'s default persisted-session behaviour; verified live (see Gate 3). |
| **Gate 3** | **passed** | 2026-09-04 | Verified live on the `kharcha_test` emulator. Signed-out boots to `/login` (screenshot). Real sign-in (user-entered credentials, not scripted) → dashboard. Force-stopped the app, fully severed connectivity (`svc wifi disable` + `svc data disable`, confirmed 0 connected networks via `dumpsys connectivity`), cold-relaunched → straight to dashboard, still signed in, no crash. Sign-out → back to `/login`; pulled the on-device `kharcha.sqlite` via `adb exec-out run-as ... cat` and confirmed all 11 tables have 0 rows. Two real bugs were found and fixed during this live pass — see DECISIONS.md. |
| T-4.1  | done   | 2026-09-04 | `ConnectivityService` (`core/network/connectivity_service.dart`) wraps `connectivity_plus`; `isOnline` future + `onStatusChange` stream. |
| T-4.2  | done   | 2026-09-04 | `TableRemoteDataSource` (shared select-since-cursor/upsert/soft-delete) + 8 named subclasses under `data/remote/`, plus `HouseholdRemoteDataSource` (single-row fetch). See DECISIONS.md for why these are thin subclasses of one shared implementation rather than 9 independent ones. |
| T-4.3  | done   | 2026-09-04 | `OutboxProcessor` (`data/sync/outbox_processor.dart`): dependency-ordered FIFO drain, `computeBackoff()` (`data/sync/backoff.dart`, exponential + ±20% jitter, 15 min cap), permanent-vs-transient classification reusing `ErrorMapper` (see DECISIONS.md), `OutboxEntries.status` column added (schema v1→v2). `upload` op wired structurally for Phase 10. 6 unit tests. |
| T-4.4  | done   | 2026-09-04 | `PullService` (`data/sync/pull_service.dart`): pages every entity's since-cursor changes (500-row pages), advances `sync_meta` cursors, tombstone handling. 3 unit tests incl. a 501-row paged pull. Found & fixed a real Drift `DateTime` UTC-flag bug in the cursor path while writing this test — see DECISIONS.md. |
| T-4.5  | done   | 2026-09-04 | `SyncEngine` (`data/sync/sync_engine.dart`): single-flight lock, push→pull→publish algorithm exactly per spec §9.6, owns the connectivity subscription (trigger 3) and 5-min periodic timer (trigger 6); `start()`/`stop()` are idempotent and resumable (sign-out calls `stop()`, next sign-in re-arms). 5 unit tests, incl. a rapid-double-trigger test that caught and fixed a real single-flight race — see DECISIONS.md. |
| T-4.6  | done   | 2026-09-04 | D12 conflict resolution lives in each `EntitySyncAdapter.pullApply()` (`data/sync/entity_sync_adapters.dart`): a dirty-and-newer local row is kept; otherwise remote wins, and if that discards an unpushed local edit, the loss is logged via `AppLogger`. 4 unit tests covering spec §13 Test 5 both directions, plus the clean-overwrite and tombstone cases. |
| T-4.7  | done   | 2026-09-04 | `SyncState` sealed class + `SyncStateController` (Riverpod `Notifier`) + `SyncBanner` widget, wired into `AppShell` above the nav shell. Renders nothing when idle-and-clean. |
| T-4.8  | done   | 2026-09-04 | `RealtimeListener` (`data/sync/realtime_listener.dart`): subscribes to `expenses`/`incomes`/`budgets` Postgres changes filtered by `household_id`, 2s per-table debounce → single-entity pull. Guarded by `AppConfig.realtimeEnabled` (default on). |
| **Gate 4** | **partial** | 2026-09-04 (re-verified 2026-09-05, fix implemented 2026-09-05) | Full push/pull/conflict/backoff machinery built and unit-tested (22 new tests in `test/unit/sync/`, `fvm flutter test` green at 78 total, `flutter analyze --fatal-infos` clean). **Live-verified the pull path** against the real Supabase project on the `kharcha_test` emulator: after a real sign-in, `kharcha.sqlite` (pulled via `adb exec-out run-as ... cat`) shows all 20 categories, 6 payment methods, 4 profiles, and the household row synced locally, with `sync_meta` populated for all 9 entities and an empty outbox — see DECISIONS.md for full detail. **Two-device "both edit offline, reconcile online" scenario live-verified 2026-09-05** on two real emulators (Vineet/admin + Rupesh/member, both offline, editing the same expense): reconciliation converges to one consistent state on both devices — no crash, no duplicate rows, no permanent fork. But it resolved by **last-device-to-push-wins, not newest-edit-wins** — Vineet's older offline edit silently overwrote Rupesh's newer one, with no `AppLogger` conflict entry on either device, contradicting spec §13 Test 5's "no data loss... the losing version is logged" guarantee for this literal case. **Fixed 2026-09-05**: pushes now carry an optimistic-concurrency compare-and-swap against the row's last known-good server value, so a genuine cross-device conflict is detected and resolved by timestamp (newest edit wins) regardless of push order — see DECISIONS.md for the full mechanism. Covered by 4 new tests in `test/unit/sync/push_conflict_resolution_test.dart` (matching base pushes cleanly; remote-newer conflict discards and logs the local edit; local-newer conflict retries without data loss; a brand-new row pushes unconditionally) plus a new `test/unit/db/migration_v2_to_v3_test.dart` proving the schema migration preserves existing family data on upgrade. `fvm flutter test` green at 182, `flutter analyze --fatal-infos` clean. Status held at **partial** pending a live two-device re-verification of this exact fix (not done this session — no emulator available) before flipping to passed. |
| T-5.1  | done   | 2026-09-04 | `CategoryRepository` + `PaymentMethodRepository` (`data/repositories/`): local-first Drift streams, every write also enqueues an outbox entry (create/update/archive/reorder → `op:'upsert'`; delete → `op:'delete'`). `delete()` is blocked with a `ValidationFailure` while a non-deleted expense still references the id (spec §11.5), offering "Archive instead" in the UI. 8 unit tests (`category_repository_test.dart`, `payment_method_repository_test.dart`) incl. the delete-guard both ways. Admin-only write is enforced by RLS (`cat_write`/`pm_write`) plus hiding the controls for members — the repositories themselves don't gate on role. |
| T-5.2  | done   | 2026-09-04 | Category management screen (`features/categories/`): drag-to-reorder (admin-only `ReorderableListView`), archive toggle, edit sheet (name/kind-locked-after-creation/icon/16-swatch colour), delete with an "Archive" snackbar action on the usage-guard failure. Members get the same list read-only, no controls. |
| T-5.3  | done   | 2026-09-04 | Payment method management screen — same shape as T-5.2, minus the icon/colour picker (`payment_methods` has no such columns; its icon is derived from `type` via `iconForPaymentMethodType`). |
| T-5.4  | done   | 2026-09-04 | `ExpenseRepository` (`data/repositories/expense_repository.dart`): create/update/delete → Drift + outbox; `watchFiltered`/`watchFilteredTotal` back the list (grows a `limit`, not an offset — see DECISIONS.md); `hasPossibleDuplicate`, `lastUsedPaymentMethodId`, `mostUsedCategoryIds`, `recentDistinctNotes/Merchants` back the Add Expense form. `undoCreate()` implements the 5s Undo snackbar (T-5.6). 7 unit tests in `expense_repository_test.dart`. **Found & fixed a real bug** while writing these tests: every mapper's `toDomain()` was missing the `.toUtc()` normalisation already applied on the pull side in Phase 4 — see DECISIONS.md, this would have silently pushed edited rows' timestamps to the server 5:30 off (IST). Fixed in all 9 `data/local/mappers/*.dart` files. |
| T-5.5  | done   | 2026-09-04 | Add/Edit Expense screen (`features/expenses/screens/expense_detail_screen.dart`) per spec §11.2: amount field with Money validation, category chip row (8 most-used + "More" sheet), payment-method chip row (defaults to last used), Today/Yesterday chips + date/time picker (blocks >1 day future), note/merchant fields with `RawAutocomplete` from the last 20 distinct values, admin-only "Paid by" member selector. |
| T-5.6  | done   | 2026-09-04 | Duplicate guard (`ExpenseRepository.hasPossibleDuplicate`, same amount+category+user within 2 minutes) shows a non-blocking "save anyway?" dialog. Undo snackbar (5s) calls `undoCreate()`, which removes the create's outbox entry outright if it hasn't been pushed yet (server never sees it) or falls back to a real soft-delete + outbox delete if it has. |
| T-5.7  | done   | 2026-09-04 | Expense List (`features/expenses/screens/expense_list_screen.dart`): reverse-chronological, grouped by date with a day-total header, infinite scroll (limit grows by 50 on near-bottom scroll, re-subscribing the same live Drift stream rather than true offset paging — see DECISIONS.md), swipe-right to duplicate (pre-fills Add form via `GoRouterState.extra`, date reset to now), swipe-left to delete (blocked for non-owners), cloud-off badge for dirty rows (new `Expense.isDirty`, excluded from JSON both ways). |
| T-5.8  | done   | 2026-09-04 | Filter sheet (`features/expenses/widgets/expense_filter_sheet.dart`): date range (This month / All time / custom), member(s), category(ies), payment method(s), amount range, only-mine, only-with-receipts — all combinable via `ExpenseFilter` (new freezed model, not persisted). Free-text search (300ms debounce) over note/merchant. Header shows the live filtered total via `watchFilteredTotal`. |
| T-5.9  | done   | 2026-09-04 | Same `ExpenseDetailScreen` handles this: a signed-in member who is neither the expense's owner nor an admin gets `_ReadOnlyExpenseView` instead of the form — no Save, no Delete, no editable fields. |
| **Gate 5** | **passed** | 2026-09-04 | All of T-5.1–T-5.9 implemented; `flutter analyze --fatal-infos` clean; `fvm flutter test` green at 93 tests. **Live-verified** on the `kharcha_test` emulator by the user: add/edit/delete an expense, category and payment method management (add/edit/archive/reorder), and the offline test (aeroplane mode save → cloud-off badge → reconnect → syncs) all passed. One bug found and fixed during this pass — see DECISIONS.md (the Undo snackbar never auto-dismissed). Settings screen got minimal "Categories"/"Payment methods" nav links added so those Phase 5 screens are reachable ahead of their real home in Phase 14. |
| T-6.1  | done   | 2026-09-04 | `ReportDao` (`core/db/daos/report_dao.dart`, new) + `ReportRepository` (`data/repositories/report_repository.dart`): household expense/income totals, per-member, per-category, and per-payment-method breakdowns, all as live Drift streams scoped to a `[start, end)` period — month-over-month is just calling the same total query again with the previous month's bounds, not a separate query. Per-payment-method isn't surfaced on the Dashboard yet (only cards 1/3/4/5 ship this phase) but is implemented and tested now for Analytics (Phase 11) to reuse. 8 DAO tests in `test/unit/db/report_dao_test.dart` against a hand-computed fixture (mixed members/categories/payment methods, a soft-deleted row, and an out-of-period row, all asserted excluded/included correctly). |
| T-6.2  | done   | 2026-09-04 | `SelectedMonthController` (`features/dashboard/controllers/`, Riverpod `Notifier`, `keepAlive`) holds the selected month so it survives tab switches and can be shared with Analytics later. App-bar month selector (‹ / month label / ›) plus a year+month-grid picker dialog; both the `›` arrow and picker cells are disabled for any month after the current one via `AppTime.isFutureMonth`. |
| T-6.3  | done   | 2026-09-04 | Dashboard cards 1/3/4/5 per spec §11.4: household summary (spent/income/net saved + month-over-month % with a coloured arrow), per-member breakdown (horizontal bars, sorted descending, **tap filters the Expense List to that member** — see DECISIONS.md for how that cross-tab handoff works), top 5 categories with a "See all" link to Analytics, and the last 5 expenses household-wide with tap-to-detail. Cards 2 (budget progress) and 6 (pending recurring) are out of scope per T-6.3's own task list — they land with Phases 8 and 9. |
| T-6.4  | done   | 2026-09-04 | Dashboard body wrapped in a `RefreshIndicator` whose `onRefresh` calls `SyncEngine.sync()` directly; the pull spinner is Flutter's own (tied to the `Future` it awaits), not driven off `SyncState`. |
| T-6.5  | done   | 2026-09-04 | Every percentage on the Dashboard (member share, category share, month-over-month change) guards its denominator being zero and renders a plain empty-state message instead of a bar/row when a card's underlying list is empty — no `NaN`, no `Infinity`, no divide-by-zero, verified against a household with a single ₹50 expense and no income (see Gate 6). |
| **Gate 6** | **passed** | 2026-09-04 | All of T-6.1–T-6.5 implemented; `flutter analyze` clean; `fvm flutter test` green at 101 tests. **Live-verified** on the `kharcha_test` emulator (`fvm flutter run --dart-define-from-file=config/dev.json`), screenshot captured via `adb exec-out screencap`: against the real seeded household data, the Dashboard rendered "This month: Spent ₹50.00, Income ₹0.00, Net saved -₹50.00", "Per member: Vineet ₹50.00 (100%)" with its progress bar, "Top categories: zomato 100% ₹50.00", and "Recent activity: ZXC · Vineet · ₹50.00" — all four live cards correct against the Expense List's own totals for the same month, with no crashes and no `NaN`/`₹0.00`-division artifacts despite the near-empty dataset. `widget_test.dart` needed two fixes to keep passing once the Dashboard started issuing live Drift queries — see DECISIONS.md. |
| T-7.1  | done   | 2026-09-04 | `IncomeRepository` (`data/repositories/income_repository.dart`) mirroring `ExpenseRepository`'s shape: local-first Drift read, every write → Drift + outbox. Deliberately simpler than expenses (no duplicate guard, no undo snackbar, no payment method — none are in scope for income per spec §11.6). Added `IncomeDao.watchById`/`countByCategory` (the latter mirroring `ExpenseDao.countByCategory`). **Bug found & fixed in passing**: `CategoryRepository.delete()`'s usage guard (T-5.1) only ever checked `expenseDao.countByCategory` — an income-kind category with existing income entries could be silently deleted, orphaning those rows' `category_id` client-side reads. Now sums both counts. 4 repository tests + 1 new category-guard test (`category_repository_test.dart`). |
| T-7.2  | done   | 2026-09-04 | Add/Edit Income screen (`features/income/screens/income_detail_screen.dart`, built out from its Phase-2 placeholder) per spec §11.6: amount field, income-kind category chips, Today chip + date picker (blocks >1 day future, no time-of-day — income is date-only), source/note fields, admin-only "Received by" member selector, same owner-or-admin edit rule as expenses (RLS `inc_update`/`inc_delete` mirror `exp_update`/`exp_delete`) with a `_ReadOnlyIncomeView` fallback. Income List (`features/income/screens/income_list_screen.dart`, new): reverse-chronological, grouped by date with a day-total header, swipe-to-delete — simpler than the Expense List (no filter sheet/search/infinite scroll; income volume is far lower). Reachable from Settings ("Income" nav link) and from the Dashboard's Income row (now tappable). New route `/income` (list) alongside the existing `/income/new`/`/income/:id`. |
| T-7.3  | done   | 2026-09-04 | Already substantially implemented in Phase 6 (T-6.1's `ReportDao.watchIncomeTotal`, the Dashboard's `_HouseholdSummaryCard` computing `net = income − expense`) — T-7.3 added the missing piece: the Dashboard's Income row is now tappable (`_SummaryRow.onTap`) and pushes `/income`, satisfying spec §11.6's "reachable ... from the Dashboard's income figure". |
| **Gate 7** | **passed** | 2026-09-04 | Income and expense totals are computed from separate tables/queries throughout (`ReportDao.watchExpenseTotal` vs `watchIncomeTotal`) — never contaminated by construction. `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 110 tests. **Live-verified** on the `kharcha_test` emulator: added a real ₹50,000 "Salary" income (source "Employer", received by Vineet) via the live UI (amount field, category chips, date picker, source field, member picker all worked correctly), saved, landed back on the Income List showing "Total ₹50,000.00" / "Today ₹50,000.00" / the new row with its category icon. Dashboard immediately reflected it live: "Income ₹50,000.00", "Net saved ₹49,950.00" (green), while "Spent ₹50.00" and every other card stayed unchanged — expense figures were not touched by the new income row. Pulled `kharcha.sqlite` off the device (`adb exec-out run-as ... cat app_flutter/kharcha.sqlite`, same technique as prior gates — note the correct path is `app_flutter/kharcha.sqlite`, not `databases/kharcha.sqlite`) and confirmed the row pushed successfully: `is_dirty=0`, `sync_status='synced'`, empty `outbox_entries`. |
| T-8.1  | done   | 2026-09-05 | `BudgetRepository` (`data/repositories/budget_repository.dart`): local-first Drift read, every write → Drift + outbox. `isValidBudgetScopeShape()` (`domain/models/budget.dart`) mirrors the DB's `budgets_scope_shape` check constraint exactly; `create()`/`update()` return `Result.err(ValidationFailure)` for an invalid shape instead of round-tripping to Postgres to be told no. New `BudgetDao.findByScope` (exact scope-shape lookup, mirroring the `budgets_unique_scope` index) and `ReportDao.watchExpenseForScope` (the 4 budget scopes are just the 4 combinations of an optional member/category filter on the existing expense-total query). 9 repository tests + 4 new `ReportDao` scope-filter tests. |
| T-8.2  | done   | 2026-09-05 | Budget list (`features/budgets/screens/budget_list_screen.dart`) and editor (`budget_detail_screen.dart`) screens per spec §11.7: scope chips (admin sees all 4; a member sees only User/User+Category, forced to themselves — mirrors `bud_write`'s RLS shape client-side), month stepper, rollover switch, alert-threshold slider, "Also create for the next 12 months" checkbox wired to `BudgetRepository.copyToNext12Months()` (creates 12, skipping any month that already has a same-shape budget). List screen groups by scope with a live per-row progress bar and an ok/warning/exceeded summary header. |
| T-8.3  | done   | 2026-09-05 | `domain/models/budget_status.dart`: `BudgetHealth` enum + `BudgetStatus` (spent/effective-budget/remaining/overspend/pct/health) + `computeRolloverPaise()`, all pure functions per spec's "compute locally; do not store the derived number." `BudgetRepository.watchStatus()` combines a live scoped-spend stream with a one-shot previous-month rollover lookup. 13 unit tests covering ok/warning/exceeded boundaries (incl. exactly-at-threshold and exactly-at-100%) and rollover math (under/over/no-previous-budget). |
| T-8.4  | done   | 2026-09-05 | Dashboard card 2 (`_BudgetProgressCard`/`_BudgetProgressRow` in `dashboard_screen.dart`): one row per this month's budget — spent/effective-budget, a coloured progress bar, and "₹X left · ₹Y/day for N days" (daily allowance = remaining ÷ `AppTime.daysRemainingInMonth`, new helper). Renders nothing when there are no budgets for the month, per the Phase 6 precedent for absent cards. |
| T-8.5  | done   | 2026-09-05 | `NotificationService` (`core/notifications/notification_service.dart`, brought forward from Phase 13 — see DECISIONS.md) + `BudgetAlertService` (`data/repositories/budget_alert_service.dart`): evaluates every current-month budget's status, firing a local notification on crossing into warning/exceeded, deduplicated via a `shared_preferences` key `budget_alert_<budgetId>_<yyyyMM>` storing the last-*notified* status (cleared on a return to `ok` so a later re-crossing counts as a new transition and notifies again). Wired to fire after every expense save (`expense_detail_screen.dart`) and on every app resume (`app.dart`). Copy matches spec §11.7 exactly ("Household budget 91% used — ₹5.00 left for 26 days" / "... exceeded by ₹X"). |
| **Bug found & fixed** | — | 2026-09-05 | **`DateTime.parse` on a bare Postgres `date` column silently corrupted the value by the device's UTC offset.** `spent_on`/`received_on`/`period_month`/`start_date`/`end_date`/`next_due_date`/`last_posted_on` are all `date`-typed (no time/zone) server-side; PostgREST serialises them as a bare `"2026-09-01"`, and Dart's `DateTime.parse` on a string with no offset returns a **local**-flagged DateTime — on an IST device (every Kharcha device, by design) that's the wrong instant by exactly 5:30 once stored via `millisecondsSinceEpoch`. Found live: a budget created for "September 2026" was pulled back with `period_month` = 2026-08-31T18:30Z, so an exact-equality month query for it silently returned nothing. Fixed with a shared `AppTime.parseDateOnly`/`parseDateOnlyOrNull` (forces `DateTime.utc(y,m,d)`, discarding the device timezone) applied via `@JsonKey(fromJson: ...)` on all 7 affected fields across `Expense`, `Income`, `Budget`, and `RecurringRule` — this was a **latent, pre-existing bug since Phase 4/5**, not new in Phase 8; range-filtered queries (`>=`/`<`, used by every Dashboard/Expense-List total) mostly masked it, only budgets' exact-equality month lookup surfaced it. See DECISIONS.md for the full diagnosis. |
| **Gate 8** | **passed** | 2026-09-05 | `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 140 tests. **Live-verified** on the `kharcha_test` emulator against the real Supabase project: created a household budget, edited it to ₹55 against the month's real ₹50 spend (91%, threshold 80%), backgrounded and resumed the app, and confirmed via `adb shell dumpsys notification` a real system notification posted on channel `budget_alerts` — title "Budget alert", body "Household budget 91% used — ₹5.00 left for 26 days" (matches spec copy exactly). Confirmed the dedup key `budget_alert_<id>_202609` = `"warning"` was persisted in `FlutterSharedPreferences.xml`, so a second resume without a status change does not re-fire (verified by design and by the persisted key, per T-8.5's dedup logic). All test budgets deleted afterward and confirmed hard-deleted locally after the tombstone round-trip through Supabase — no leftover test data. |
| T-9.1  | done   | 2026-09-05 | `domain/models/recurring_schedule.dart`: `advanceDueDate` mirrors the SQL `advance_due_date()` function's daily/weekly/monthly/yearly branches exactly, incl. the monthly clamp-to-last-valid-day; the SQL's own dead `p_weekday` parameter isn't carried into Dart (see DECISIONS.md). 15 unit tests incl. 31 Jan → 28 Feb (non-leap) and → 29 Feb (leap year), and yearly's deliberately-unclamped 29 Feb → 1 Mar overflow. |
| T-9.2  | done   | 2026-09-05 | `RecurringRepository` (`data/repositories/recurring_repository.dart`): local-first Drift read, every write → Drift + outbox; `create()` seeds `next_due_date = start_date`. `previewOccurrences()` wraps a new pure `dueOccurrencesFor`/`previewOccurrences` pair in `recurring_schedule.dart`. Rule editor (`features/recurring/screens/recurring_detail_screen.dart`, built out from its Phase-2 placeholder) per spec §11.8: title, kind (locked after creation, same precedent as category kind), amount, category (filtered by kind), payment method (expense only), note, frequency + interval + day-of-month (monthly only), start/end date pickers, Auto-post toggle, admin-only member picker, and the live "Next occurrences" preview. Recurring List (`recurring_list_screen.dart`, also built out): every household rule sorted active-first then by `next_due_date`, swipe-to-delete gated by owner-or-admin (`rec_write`'s RLS shape). New "Recurring" link on Settings. 6 repository tests. |
| T-9.3  | done   | 2026-09-05 | `RecurringPostingEngine` (`data/sync/recurring_posting_engine.dart`): runs inside `SyncEngine.sync()` right after the pull, pushes again immediately after. Auto-post rules catch up via `dueOccurrencesFor`, capped at 24 occurrences/run (T-9.3's own cap); non-auto-post rules are left untouched (next_due_date frozen at the earliest pending occurrence) except for silently skipping anything more than 30 days stale, per spec §11.8's last bullet. Idempotent per (rule, occurrence) via `ExpenseDao.findByOccurrence`/`IncomeDao.findByOccurrence` (new DAO methods) against a local crash-recovery re-run. A posted occurrence's `merchant`/`source` carries the rule's `title` (see DECISIONS.md — expenses/incomes have no title field of their own). 10 unit tests in `recurring_posting_engine_test.dart` (in-memory Drift) incl. the 24-occurrence cap across two runs and end-date deactivation. |
| T-9.4  | done   | 2026-09-05 | `OutboxProcessor._handleRecurrenceDuplicate`: a `23505` unique-violation on an expense/income upsert whose payload carries a non-null `recurring_rule_id` is caught, the local phantom row hard-deleted, and the outbox entry dropped as a success (no retry, no `'failed'` state) — the winning device's row arrives on the next pull. Distinct from T-9.3's same-device idempotency check; see DECISIONS.md for how the two races differ. 3 new tests in `outbox_processor_test.dart`, incl. a scripted "device A succeeds, device B's push is discarded" scenario against the same local outbox (see DECISIONS.md re: why this stands in for a literal two-device run, same precedent as Gate 4). |
| T-9.5  | done   | 2026-09-05 | Dashboard card 6 (`_PendingRecurringCard`/`_PendingRecurringRow` in `dashboard_screen.dart`): every active, non-auto-post, currently-due rule (mirrors `RecurringDao.dueOn`'s own filter, read off the same `householdRecurringRulesProvider` stream the list screen uses), with Post/Skip buttons wired to new `RecurringRepository.postPending`/`skipPending` → `RecurringPostingEngine.postOneOccurrence`/`skipOneOccurrence` (single-step: create-and-advance, or advance-only). Renders nothing when nothing is pending, same empty-state precedent as the budget card. |
| **Bug found & fixed** | — | 2026-09-05 | The Auto-post `SwitchListTile`'s subtitle was a `const Text` hardcoded to the "Off" copy — toggling the switch on during live verification left the helper text still reading "Off: ...". Fixed by keying the subtitle off `_autoPost`. |
| **Gate 9** | **passed** | 2026-09-05 | `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 176 tests (36 new, across `recurring_schedule_test.dart`, `recurring_posting_engine_test.dart`, `recurring_repository_test.dart`, and 3 new `outbox_processor_test.dart` cases). **Live-verified** on the `kharcha_test` emulator against the real Supabase project: created an auto-post "Netflix" ₹499/month rule (day 5, today) — the very next sync cycle posted it automatically with the correct category ("Subscriptions") and merchant ("Netflix"), and the rule's `next_due_date` advanced 5 Sep → 5 Oct. Created a second, manual "Rent" ₹15,000/month rule — the Dashboard's "Pending confirmations" card appeared with Skip/Post; tapping Post created the expense, advanced `next_due_date`, and the card disappeared once nothing was left pending. Both rules and their posted expenses were deleted afterward, restoring the shared household data to its pre-test state. The literal two-device race from Test 4/§13 was not re-run on a second device this gate — covered instead by `outbox_processor_test.dart`'s scripted duplicate-push scenario (T-9.4), matching Gate 4's precedent. |
| T-10.1  | done   | 2026-09-05 | `AttachmentRepository.addFromFile()` (`data/repositories/attachment_repository.dart`, new): compresses via an injectable `ImageCompressor` (default `FlutterImageCompress.compressWithFile`, `minWidth`/`minHeight` 1600, quality 80), saves to `<app-docs>/receipts/<id>.jpg`. `android/app/src/main/AndroidManifest.xml` gained the `CAMERA` permission for `image_picker`'s camera source; no iOS Info.plist change since `ios/` was dropped earlier (Android-only for now). See DECISIONS.md for why the compressor is injected rather than called statically, and why "4 MB → <400 KB" is a live-verification item, not a unit test. |
| T-10.2  | done   | 2026-09-05 | Same `AttachmentRepository.addFromFile()` inserts the `attachments` Drift row (`is_dirty = true`) and enqueues an `upload` outbox job (`op: 'upload'`, payload `{local_path, storage_path, row}`) — capture and queueing both work fully offline. Two new `AttachmentDao` methods: `countForExpense()` (max-3-per-expense guard) and `findActiveForExpense()` (T-10.5's cascade). 5 repository tests in `test/unit/data/attachment_repository_test.dart` (in-memory Drift + a `path_provider_platform_interface` fake, same technique as `migration_v2_to_v3_test.dart`, + a pass-through fake compressor). |
| T-10.3  | done   | 2026-09-05 | The `upload` outbox op was already wired structurally since Phase 4 (`OutboxProcessor._processUpload`: uploads the cached file to the `receipts` bucket, then upserts the row) — nothing changed there. `has_receipt` is maintained via new `ExpenseRepository.setHasReceipt()`, called by `AttachmentRepository` on add (→ true) and on delete when no receipts remain (→ false). See DECISIONS.md for why `AttachmentRepository` doesn't own the `expenses` row directly. |
| T-10.4  | done   | 2026-09-05 | Expense detail screen (`expense_detail_screen.dart`) grew a Receipts section (existing-expense only — see DECISIONS.md): a thumbnail row (`_ReceiptThumbnail`, resolves local cache → signed-URL download → cache, per spec §11.9) with a cloud-off "upload pending" badge on dirty attachments, an "Add photo" chip (camera/gallery via `image_picker`), long-press-to-delete. `ReceiptViewerScreen` (`features/receipts/screens/receipt_viewer_screen.dart`) built out from its Phase-2 placeholder: `InteractiveViewer` pinch-zoom, a share button (`SharePlus.instance.share`), the same cache-first resolution via `AttachmentRepository.resolveLocalFile()`. `Attachment` domain model gained an `isDirty` field (mirroring `Expense.isDirty`) to back the badge. |
| T-10.5  | done   | 2026-09-05 | `ExpenseRepository.delete()` now cascades: soft-deletes every active attachment of the expense and enqueues its removal (reaches into `_db.attachmentDao`/`_db.outboxDao` directly, same pattern as `CategoryRepository`'s usage guard — no `AttachmentRepository` dependency). `OutboxProcessor`'s `delete` case, for `entity == 'attachment'`, now best-effort deletes the actual Storage object (`client.storage.from('receipts').remove(...)`) before the row's tombstone — wrapped in try/catch so a failed storage delete never blocks the row from propagating. The Diagnostics "list orphaned objects" admin screen itself is Phase 14 work and was not built now — see DECISIONS.md. |
| **Bug found & fixed** | — | 2026-09-05 | Live two-device Gate 10 verification surfaced a push-side bug: `_pushUpsertWithCas` stamped `base_updated_at` from the outbox payload's own claimed `updated_at` instead of what Postgres's `touch_updated_at()` trigger actually stored (`GREATEST(now(), incoming)`), so a second queued edit to the same row (e.g. attach-a-receipt-right-after-create) would CAS-mismatch and be silently discarded. Fixed: `TableRemoteDataSource.upsertIfBaseMatches()` now returns the server's real resulting `updated_at` (`Future<DateTime?>`, via a trailing `.select('updated_at')`) instead of `Future<bool>`, and `markSynced` uses that. New regression test in `push_conflict_resolution_test.dart`. See DECISIONS.md for the full trace. |
| **Bug found & fixed** | — | 2026-09-05 | Same investigation surfaced a second, more severe pre-existing bug: every entity mapper's `toCompanion(dirty: true)` (expense/income/category/payment_method/budget/recurring_rule/attachment) never set `local_updated_at` — it stayed null forever except after a soft-delete. This silently defeated the entire Gate 4 CAS-conflict fix (its "is local newer" check can never be true against a null timestamp) and crashed the sync loop (`_logConflictLoss` force-unwrapped it) the first time a dirty row actually hit a CAS mismatch, misreported to the user as "Something went wrong" and retried forever without ever resolving. Fixed: all 7 mappers now stamp `localUpdatedAt` when dirty; `_logConflictLoss` made defensive against a still-null value from pre-fix data. New `test/unit/data/mapper_local_updated_at_test.dart` (7 cases) plus a new crash-regression case in `push_conflict_resolution_test.dart`. Found and root-caused by reading the real exception off a live VM-service connection — `AppLogger`'s `dart:developer` output doesn't reach `adb logcat` for an installed APK; see DECISIONS.md for the technique. |
| **Bug found & fixed** | — | 2026-09-05 | A third, deeper bug surfaced while confirming the fixes above live: `base_updated_at` round-tripped through Drift's `DateTimeColumn` at whole-**second** precision, but Postgres's `updated_at` has microsecond precision — so a CAS retry's `.eq('updated_at', ...)` could mismatch forever once a row needed a real conflict-resolution cycle (not just a first, unconditional insert). Confirmed live: one test expense's `has_receipt` push kept hitting the correct, intended `SyncConflictRetryException` retry path but never converged. Fixed: the CAS base is now a raw `String?` everywhere (`TableRemoteDataSource.upsertIfBaseMatches`, `_LocalSyncMeta`, every DAO's `markSyncedWithBase`/`updateBaseUpdatedAt`, every mapper's `toCompanion`) — the server's `updated_at` read straight off the JSON response and never parsed into a `DateTime`/reformatted, so there's no code path left that could reintroduce drift. `base_updated_at` moved from `DateTimeColumn` to `TextColumn` on all 7 syncable tables (schema v3 → v4, `m.dropColumn`+`m.addColumn`, resetting every row's base to null — safe, since the old value could never CAS-match anyway). New `test/unit/db/migration_v3_to_v4_test.dart` plus a sub-second-precision round-trip case in `push_conflict_resolution_test.dart`. **Live re-verified against the real stuck row**: rebuilt, reinstalled over the *same* on-device database that had been retrying for hours, and it converged cleanly on the next sync — `has_receipt=1`, `is_dirty=0`, `sync_status='synced'`, `base_updated_at='2026-09-05T13:32:17.259813+00:00'` (a precise, microsecond-bearing string). See DECISIONS.md for the full trace, including an unrelated emulator DNS glitch hit and resolved along the way. |
| **Bug found, unrelated, not fixed** | — | 2026-09-05 | Tapping a member's row in the Dashboard's "Per member" card (spec §6.3, T-6.3, Phase 6 — unrelated to Receipts) crashed twice with a Riverpod "modify a provider while the widget tree was building" error. Pre-existing, not investigated further; noted so it isn't mistaken for a Gate 10 regression. |
| **Gate 4** | **partial** (unchanged, higher confidence) | 2026-09-05 | The precision fix above was live-verified against a real CAS conflict-retry converging correctly end-to-end for the first time this build has observed it succeed — every earlier attempt this session hit one of the three bugs above first. That is not, however, a re-run of Gate 4's own specific pending scenario (two separate devices editing the *same* row while both offline, reconnecting, newest-edit-wins) — it was one device's own two sequential edits colliding with themselves. Gate 4 stays **partial**, still blocked only on that literal two-device re-run, but the CAS plumbing it depends on has now been proven against the real Supabase project rather than only in test fakes. |
| **Gate 10** | **passed** | 2026-09-05 | `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 198 tests. **Live-verified** on two real Android emulators (`kharcha_test` = Vineet/admin, `kharcha_test_2` = Rupesh/member) against the real Supabase project: Rupesh, fully offline (`svc wifi/data disable`), created a ₹250 "Groceries" expense and attached a photo (712 KB source compressed to 151 KB cached) — both saved locally with correct offline bookkeeping. Reconnected — the attachment uploaded and synced cleanly (`is_dirty=0`, `sync_status='synced'`, correct `<household_id>/<expense_id>/<attachment_id>.jpg` storage path). Vineet, on a separate emulator, opened the same expense and saw the receipt thumbnail (downloaded via signed URL, cached) and the full-screen pinch-zoom viewer with its share button — Gate 10's literal acceptance ("capture offline → reconnect → the image is visible on a second device") is satisfied. Held at partial pending the sub-second-precision fix above; flipped to **passed** once that fix converged the same expense's `has_receipt` live against the real project. |
| T-11.1 | done | 2026-09-06 | `ReportDao` (`core/db/daos/report_dao.dart`) gained 5 new aggregate queries for the 7 charts in spec §11.10, all live Drift streams: `watchMonthlyTrend` (12-month household expense+income, one `customSelect` UNION ALL over `expenses`/`incomes` grouped by `strftime('%Y-%m', ..., 'unixepoch')`, zero-filled for months with no rows at all — SQLite's default epoch-**seconds** `DateTimeColumn` storage, confirmed unchanged since Phase 2, is what makes the `unixepoch` modifier correct here), `watchMemberMonthlyTrend`/`watchCategoryMonthlyTrend` (same shape, grouped by `user_id`/`category_id` too, sparse — gaps filled by the caller), `watchExpenseByWeekday` (`CAST(strftime('%w', ...) AS INTEGER)`, converted from SQLite's 0=Sunday to Dart's 1=Monday..7=Sunday convention), and `watchTopMerchants` (typed `selectOnly` query, same shape as the existing `watchExpenseByCategory`/`watchExpenseByPaymentMethod`, excluding blank `merchant`). The category donut and payment-method split reuse the existing (Phase 6/8) `watchExpenseByCategory`/`watchExpenseByPaymentMethod` queries rather than needing new ones. `ReportRepository` grew matching pass-through methods. 9 new DAO tests appended to `test/unit/db/report_dao_test.dart` against the shared Phase-6 fixture (extended with `merchant` values), incl. a month with zero rows at all reading back as zero-not-missing, and an independent hand-computed weekday map (not reusing the DAO's own conversion) so the test would actually catch an off-by-one in the SQLite-to-Dart weekday mapping. |
| T-11.2/T-11.3 | done | 2026-09-06 | `features/analytics/screens/analytics_screen.dart` built out from its Phase-2 placeholder with all 7 charts: monthly trend (`fl_chart` `LineChart`, expense+income lines, built-in tap-to-tooltip via `LineTouchData`'s default `handleBuiltInTouches`), category donut (`PieChart`, top 8 + "Other", legend with amount+%), member comparison (`BarChart`, grouped, last 6 months × members, colours matching each member's `profile.colourHex`), payment-method split and top merchants (plain proportional-bar/ranked-list widgets, not forced into `fl_chart` — it has no native horizontal-bar orientation, and the Dashboard's existing per-member bar is a better fit than fighting the library), day-of-week (`BarChart`, average = weekday total ÷ how many times that weekday occurred in the period, computed client-side from the period bounds), and month-over-month (`DataTable`, category rows × last 3 month columns + a Δ% column colour-coded red/green matching the Dashboard's existing month-over-month arrow convention). The month selector and the repeated title+"See all"-card shell were pulled out of `dashboard_screen.dart` into shared `features/dashboard/widgets/month_selector.dart` (`MonthSelector`) and `section_card.dart` (`SectionCard`/`EmptySectionBody`) — spec §11.10 says the month selector must literally be "shared with the Dashboard", and the card shell was already byte-for-byte identical between the two screens. |
| T-11.4 | done | 2026-09-06 | Every chart checks its own emptiness (`totals.isEmpty` / `every(... == 0)`) and renders `EmptySectionBody` ("No data for this period.") instead of an empty/NaN chart — live-verified by paging the shared month selector back to August 2026 (a real month with zero household expenses) and confirming all 7 cards show the empty message with no exceptions, no infinite axes, and no leftover chart chrome (see Gate 11). All money-axis labels use `Money(...).format(compact: true)` (`₹1.2L`-style) per spec. No chart animation exceeds fl_chart's default 150–250ms. |
| **Gate 11** | **passed** | 2026-09-06 | `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 206 tests (9 new). **Live-verified** on the `kharcha_test` emulator against the real Supabase project and real household data (₹250 Groceries + ₹50 zomato expenses, ₹50,000 income, all in September 2026): every chart's totals reconciled exactly with the Dashboard's for the same period — donut showed Groceries 83%/₹250 and zomato 17%/₹50 matching the Dashboard's top-categories card; payment-method split showed Cash ₹250 + UPI ₹50 summing to the Dashboard's ₹300 spent; monthly trend's September point matched the Dashboard's income/expense figures exactly, with the current month's "Sep" label fixed to always render (see bug below). Paged back to August 2026 (zero data) and confirmed all 7 cards show the "No data for this period" empty state cleanly. Re-verified light **and** dark mode (`adb shell cmd uimode night yes`, full app restart) — every card's background, text, and chart colours (which read from `Theme.of(context).colorScheme`, never a hardcoded light-only colour) stayed legible in both. |
| **Bug found & fixed** | — | 2026-09-06 | The monthly trend chart's bottom-axis label-thinning logic (hide every other month label when 12 months don't fit) hid the label for the **last** index whenever the 12-month window's length was even and the last index was odd — silently dropping the current month's own label off the x-axis (the one point most relevant to "tap a point for the exact value"). Caught live on the `kharcha_test` emulator: the September 2026 trend spike rendered with no "Sep" tick underneath it. Fixed by special-casing `isLast` in `_MonthlyTrendChart`'s `getTitlesWidget` so the final label is never skipped regardless of parity. |
| T-12.1 | done | 2026-09-06 | `buildExpenseCsv`/`buildIncomeCsv` (`data/export/csv_export_builder.dart`, new): pure functions, the exact spec §11.11 header for expenses (`date,time,member,amount_inr,category,payment_method,merchant,note,has_receipt,id`) plus a second, income-shaped header (no `time`/`payment_method`/`merchant`/`has_receipt` — the `incomes` table has none of those). Built on the `csv` package's own `CsvEncoder(addBom: true)` (its v8 API replaced the old `ListToCsvConverter`/`addBom`-by-hand approach the spec's own phrasing implied). `amount_inr` is a plain, non-grouped decimal (`450.00`, never `Money.format()`'s `1,23,456.00`) — a comma inside a numeric CSV cell would force it to be quoted and stop a spreadsheet summing it directly. `ExpenseDao.getFiltered`/`IncomeDao.getFiltered` (new, one-shot `Future<List<...>>` siblings of the existing `watchFiltered`/`watchAll`) supply the unpaginated row set. 11 new tests (`csv_export_builder_test.dart`, plus a `getFiltered` case each in `expense_dao_test.dart` and the new `income_dao_test.dart`). |
| T-12.2 | done | 2026-09-06 | `buildReportPdf` (`data/export/pdf_report_builder.dart`, new): title/household/period/generation-timestamp header, a summary block (total expense/income/net), category and member breakdown tables (`pw.TableHelper.fromTextArray`) with %, and an optional full-transaction-list appendix — no images, per spec. Takes pre-resolved display strings and injected `pw.Font`s rather than touching Drift or `rootBundle` itself, so it stays unit-testable with `pw.Font.helvetica()`. **Real fonts had to be bundled**: the `pdf` package's base14 fonts (Helvetica) have no glyph for ₹ (U+20B9) and either throw a console warning and render nothing or silently drop the character — confirmed live in the first unit-test run ("Unable to find a font to draw ₹"). Fixed by embedding Noto Sans Regular/Bold (OFL-licensed, `assets/fonts/`, ~390 KB each, glyph coverage confirmed via `fontTools` before committing) and loading them via `rootBundle` in `ExportRepository`, not the builder. 2 new tests (`pdf_report_builder_test.dart`, using the Helvetica fallback since a unit test has no asset bundle). |
| T-12.3 | done | 2026-09-06 | `ExportRepository` (`data/repositories/export_repository.dart`, new) orchestrates T-12.1/T-12.2 against the local Drift cache — a pure reader, like `ReportRepository`, that never touches the outbox. `ExportScreen` (built out from its Phase-2 placeholder) per spec §11.11: date range (This month/Last month/All time/Custom, same chip pattern as the Expense List's filter sheet), member/category `FilterChip` scope, CSV/PDF format `SegmentedButton`, "Include income" (second CSV) or "Include full transaction list" (PDF appendix) toggle, delivered via `SharePlus.instance.share`. Filenames match spec exactly (`kharcha_2026-09_expenses.csv`, `kharcha_2026-09_report.pdf`; a custom range that doesn't align to one calendar month falls back to a `yyyy-MM-dd_to_yyyy-MM-dd` token). Reachable from Settings' new "Export" link (same ahead-of-Phase-14 precedent as every other Settings link added in earlier phases). |
| T-12.4 | done | 2026-09-06 | `ExportRepository.exportFullBackupJson()`: every row of every table (households/profiles/categories/payment_methods/expenses/incomes/attachments/budgets/recurring_rules) for the household as one indented JSON file, **including soft-deleted rows** — deliberately higher-fidelity than every other read in this app, since a disaster-recovery snapshot should capture full history, not just the live view. Gated admin-only in the UI (`currentProfileProvider`'s `isAdmin`), matching spec §11.11's "admin only". |
| **Gate 12** | **passed** | 2026-09-06 | `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 217 tests (11 new). **Live-verified** on the `kharcha_test` emulator against the real Supabase project and real household data (₹250 Groceries/Rupesh with a receipt, ₹50 zomato/Vineet, ₹50,000 Salary income, all September 2026). CSV: "Export CSV" with "Include income" on produced a real Android share sheet ("Sharing 2 files") for `kharcha_2026-09_expenses.csv` + `kharcha_2026-09_income.csv`; pulled both off the device (`adb exec-out run-as ... cat`, same technique as every prior gate) and confirmed the BOM (`EF BB BF`), the exact header, and **250.00 + 50.00 = 300.00** reconciling exactly against the Dashboard's "Spent ₹300.00" for the same month (Gate 12's literal acceptance line) — amounts are plain decimals, never Money.format()'s Indian-grouped/comma form. PDF: rendered via `qlmanage` (macOS Quick Look) after pulling the file off the device — a real, valid single-page PDF (11 KB, well under the spec's 500 KB cap) with the ₹ glyph rendering correctly throughout (Total expense ₹300.00, Total income ₹50,000.00, Net ₹49,700.00, and every table cell), the category/member breakdown tables reconciling with the CSV and Dashboard exactly, and the transaction appendix listing both real expenses correctly. Full backup: the admin-only JSON button produced a file with all 9 tables present and row counts matching the real household exactly (1 household, 4 profiles, 21 categories, 6 payment methods, 2 expenses, 1 income, 1 attachment, 0 budgets/recurring rules — the last two are 0 because Gate 8/9's test rows were deleted afterward, as documented there). |
| T-13.1 | done | 2026-09-06 | `flutter_local_notifications` init + Android/iOS permission requests were already brought forward in Phase 8 (T-8.5) for budget alerts — no new init code needed. `timezone`/`tz.setLocalLocation` were likewise already wired in `main()` since T-3.1. `NotificationService` (`core/notifications/notification_service.dart`) extended: `show()` now takes an explicit `channelId`/`channelName`/`channelDescription` per call site (previously hardcoded to `budget_alerts` for every caller) so each of Phase 13's new notification types gets its own Android channel. |
| T-13.2 | done | 2026-09-06 | `NotificationScheduler` (`data/repositories/notification_scheduler.dart`, new), called on every app start and every resume (`app.dart`). Only the **daily reminder** is a true OS-scheduled alarm (`NotificationService.scheduleAt`, `zonedSchedule` + `AndroidScheduleMode.inexactAllowWhileIdle` per spec — avoids the `SCHEDULE_EXACT_ALARM` permission prompt); its "skip if already logged today" decision is pure logic in `nextDailyReminderFireIst` (`core/notifications/daily_reminder_schedule.dart`, new, unit-tested standalone). The other three types (monthly summary, recurring due, sync stuck) need content only knowable at evaluation time ("family spent ₹X", "N items waiting", "stuck for how long") — no future OS alarm can carry that, so each is evaluated live and shown immediately if due, deduplicated via `shared_preferences` keys so a later resume doesn't re-fire the same one same-day/same-month. New `ExpenseDao.hasAnyOn` and `OutboxDao.oldestPendingCreatedAt` DAO methods back the daily-reminder and sync-stuck checks respectively. `NotificationSettings`/`NotificationSettingsKeys` (`core/notifications/notification_settings.dart`, new) hold the 5 on/off switches + reminder time, all defaulting enabled/21:00 per spec's table. |
| T-13.3 | done | 2026-09-06 | `NotificationSettingsController` (Riverpod, `features/notifications/controllers/`) + `NotificationSettingsScreen` (built out from its Phase-2 placeholder): 5 `SwitchListTile`s (daily reminder, budget alerts, monthly summary, recurring due, sync issues — budget *warning* and *exceeded* share one toggle, since both are `BudgetAlertService`'s one threshold-crossing feature, not two independent notification types) plus a reminder-time picker (`showTimePicker`). Every setter persists to `shared_preferences` immediately, updates UI state, then re-runs `NotificationScheduler` so a change takes effect right away rather than waiting for the next app start. `BudgetAlertService.evaluate()` now checks `budgetAlertsEnabled` before firing. Reachable via a new "Notifications" Settings link (same ahead-of-Phase-14 precedent as every other early Settings link). |
| T-13.4 | done | 2026-09-06 | `rootNavigatorKey` (`routing/root_navigator_key.dart`, new — pulled out of `app_router.dart` so it can be shared without a routing→features dependency) lets `app.dart` call `GoRouter.of(context).push(payload)` from outside the widget tree. `NotificationService` gained `onTap` (a broadcast stream fed by `onDidReceiveNotificationResponse`, for a foreground tap) and `consumeLaunchPayload()` (for a cold-start tap, read once via `getNotificationAppLaunchDetails()` after the first frame). Every notification's `payload` is literally its target route path: budget alerts → `AppRoutes.budgetDetailPath(id)` (spec's own literal example — "Tapping a budget alert opens that budget"), daily reminder → `/expense/new`, monthly summary → `/analytics`, recurring due → `/` (the Dashboard's pending-recurring card), sync stuck → `/diagnostics`. |
| **Gate 13** | **partial** | 2026-09-06 | `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 228 tests (15 new: `daily_reminder_schedule_test.dart`'s 6 pure-logic cases, `expense_dao_test.dart`'s new `hasAnyOn` group, and the new `outbox_dao_test.dart`'s `oldestPendingCreatedAt` group). **Live-verified** on the `kharcha_test` emulator against the real Supabase project: set the reminder time via the Notifications screen and confirmed via `adb shell dumpsys alarm` a real `RTC_WAKEUP` alarm registered for `ScheduledNotificationReceiver` at exactly the computed instant each time (including correctly rolling to "tomorrow" once the chosen time had already passed for today, matching `nextDailyReminderFireIst`'s branch for that case) — alarm-history entries additionally confirmed the old alarm is cleanly cancelled on every reschedule. **Budget alert + deep-link fully verified end-to-end**: created a ₹55 household budget against the real ₹300 month-to-date spend, backgrounded/foregrounded the app to trigger `BudgetAlertService.evaluate()`, confirmed the real notification ("Budget alert — Household budget exceeded by ₹245.00") in the shade, and tapping it navigated straight to that exact budget's edit screen — spec's T-13.4 acceptance line, literally. **Not verified**: the daily reminder's alarm actually *firing* and displaying a notification — the scheduled alarm was confirmed registered for the right instant, but never dispatched within its `inexactAllowWhileIdle` batching window on this emulator run (`dumpsys alarm` showed it still pending past its `maxWhenElapsed`, then gone from the pending list with no corresponding log line or posted notification — consistent with the emulator's own alarm-dispatch/App-Standby-Bucket throttling rather than an app defect, but not something this session could force or fully diagnose). Held at **partial** pending either a longer/patient re-run or a real physical-device test — which Gate 13's own acceptance line asks for anyway ("fires on a physical Android device"), never available in this sandboxed session. |
| T-0.3 (iOS half) | done | 2026-09-06 | `ios/` re-created after having been dropped in Phase 0 (`fvm flutter create --platforms=ios --org com.panicker --project-name kharcha .`, Flutter 3.47.2 — the same revision `.metadata` already pins, so the template matches the rest of the project exactly). Bundle identifier `com.panicker.kharcha`, identical to Android's `applicationId`; deployment target 15.0 (spec NFR-9, Flutter template default, so T-0.7's iOS clause needed no edit). The same `flutter create` invocation also rewrote `.gitignore` (re-introducing the blanket `.fvm/` ignore the Phase-0 entry deliberately replaced), `.metadata` (dropping the `android` platform block), and `pubspec.lock` (silently resolving `analyzer` 13.3→14.3 and friends) — all three reverted, so this branch adds `ios/` and nothing else outside `docs/`. No CocoaPods: Flutter 3.47's iOS toolchain resolves plugins via Swift Package Manager, so spec §16.3.2's `platform :ios, '15.0'` Podfile edit has nothing to apply to and no `ios/Podfile` exists. |
| T-16.3 | done | 2026-09-06 | `ios/Runner/Info.plist` gained all three usage strings from spec §16.3.3 verbatim (`NSCameraUsageDescription`, `NSPhotoLibraryUsageDescription`, `NSPhotoLibraryAddUsageDescription`) — the iOS counterpart to the `CAMERA` permission Android got in T-10.1, which DECISIONS.md recorded as having "nowhere to go until iOS work resumes". Not yet live-exercised against a real camera/picker on device — that is T-16.3's own acceptance line and stays open. |
| Splash journey (iOS parity) | done | 2026-09-06 | `ios/Runner/Base.lproj/LaunchScreen.storyboard` rewritten to mirror `android/app/src/main/res/drawable-v21/launch_background.xml`: a bare view with no logo/image, filled with the dynamic `systemBackgroundColor` — the exact iOS equivalent of Android's `?android:colorBackground` under `Theme.Light`/`Theme.Black` (`values/` + `values-night/styles.xml`), so the pre-first-frame window follows the system light/dark setting on both platforms instead of flashing hardcoded white. The template's centred `LaunchImage` reference was removed (the imageset itself is left in place, unreferenced, matching Android's commented-out `<bitmap>` block). Everything after that first frame was already platform-agnostic Dart and needed no change: `main()`'s bootstrap (§11.1 steps 1–6), `SplashScreen`'s `CircularProgressIndicator`, and the router's `redirect` sending a signed-out member to `/login`. |
| **Gate 0** | **passed** (iOS half, 5 months late) | 2026-09-06 | The `com.apple.provenance` codesign bug that blocked Gate 0's iOS half in Phase 0 is **fixed upstream** — Flutter 3.47.2's `removeExtendedAttributes` (`packages/flutter_tools/lib/src/ios/mac.dart`) now removes `com.apple.provenance` alongside `com.apple.FinderInfo` by name. Confirmed the fix is genuinely upstream and not a leftover of Phase 0's local SDK patch: this session installed the SDK from scratch (`fvm install` into an empty cache) and `git status` in `~/fvm/versions/stable` is clean. `fvm flutter run -d <iPhone 16e, iOS 26.3 simulator> --dart-define-from-file=config/dev.json` now builds (291 s Xcode build, SPM fetching SDWebImage et al.), installs, and launches with no codesign step failing at all. `fvm flutter analyze --fatal-infos` clean. Live-verified the full launch journey on the simulator: launch storyboard → `Supabase init completed` → `/splash` → auth redirect → the real Login screen ("Kharcha", email/password, Sign in, Forgot password?), rendering correctly under Impeller/Metal. Run against a **placeholder** `config/dev.json` (`https://placeholder.supabase.co`), deliberately — the journey under test ends at the signed-out redirect, which never touches the network; signing in against the real project from iOS is not covered here. |
