| Task   | Status | Date       | Notes                                                              |
|--------|--------|------------|---------------------------------------------------------------------|
| T-0.1  | done   | 2026-09-03 | CLT/Homebrew/git pre-existing. CocoaPods, Android Studio via brew. JDK 17 via manual Temurin tar.gz (see DECISIONS.md). Xcode 26.6 installed from App Store, switched via xcode-select, license accepted, iOS 26.5 simulator runtime installed. |
| T-0.2  | done   | 2026-09-03 | FVM 4.3.0 installed. Flutter 3.47.2 (stable) installed and pinned via `fvm use stable`. Version recorded in DECISIONS.md. |
| T-0.3  | done   | 2026-09-03 | Flutter project created in place at repo root (not `~/Developer`, see DECISIONS.md) with org `com.panicker`, name `kharcha`, platforms android+ios. |
| T-0.4  | done   | 2026-09-03 | `.gitignore` extended per §4.7. `docs/SPEC.md`, `docs/DECISIONS.md`, `docs/PROGRESS.md` created. |
| T-0.5  | done   | 2026-09-03 | All packages from §9.3 added except `custom_lint`/`riverpod_lint` (deferred, see DECISIONS.md). `integration_test` added as an SDK dependency. |
| T-0.6  | done   | 2026-09-03 | `flutter_lints` (from template) + default `analysis_options.yaml`. `flutter analyze --fatal-infos` passes with 0 issues. |
| T-0.7  | done   | 2026-09-03 | Android `minSdk = 26`, `compileSdk = 37` (bumped from default 36 for plugin requirements), core library desugaring enabled for `flutter_local_notifications`. iOS deployment target 15.0 (Flutter template default). |
| Gate 0 | partial | 2026-09-03 | **Android: PASSED** — empty app built, installed, and ran on emulator `kharcha_test` (Pixel 6, API 34), confirmed via live `flutter run` session. **iOS: BLOCKED**, not passed — see DECISIONS.md for the `com.apple.provenance`/codesign issue. Proceeding to Phase 1 on the Android path per user instruction; iOS to be revisited later. iOS half later resolved — see the "Gate 0" row after Gate 14 below. |
| T-1.1  | done   | 2026-09-03 | User created a Supabase account and the `kharcha` project (org "Panicker Family") in `ap-south-1` (Mumbai) via the dashboard, guided over Claude-in-Chrome. Project ref `jqorwgiowfxxgjvayznj`, URL `https://jqorwgiowfxxgjvayznj.supabase.co`. Project uses Supabase's newer key format (`sb_publishable_...`) instead of a legacy JWT anon key — used as the `SUPABASE_ANON_KEY` value. `config/dev.json` created (gitignored) with URL + publishable key; `config/example.json` created (committed placeholder). |
| T-1.2  | done   | 2026-09-03 | Supabase CLI 2.116.0 installed via `brew tap supabase/tap && brew install supabase/tap/supabase`. `supabase init` run at repo root. `supabase login`'s automatic browser flow does not work in a non-TTY/agent shell (`LegacyLoginMissingTokenError`) — worked around by having the user generate a personal access token from the dashboard and run `export SUPABASE_ACCESS_TOKEN=...` + `supabase link --project-ref jqorwgiowfxxgjvayznj` themselves in their own terminal (kept out of the chat transcript deliberately). Link succeeded. |
| T-1.3  | done   | 2026-09-03 | Migrations `0001_extensions.sql`–`0004_budgets_recurring.sql` written verbatim from spec §6.2–§6.5 and pushed to the live project via `supabase db push` (run by the user, token stayed in their shell). |
| T-1.4  | done   | 2026-09-03 | `0005_functions_triggers.sql` written from spec §6.6 and pushed live. |
| T-1.5  | done   | 2026-09-03 | `0006_rls.sql` written from spec §7 and pushed live (after the app_releases ordering fix — see DECISIONS.md). All 10 target tables show RLS enabled in the dashboard. |
| T-1.6  | done   | 2026-09-03 | `0007_views.sql` (reporting views + `get_dashboard` RPC) and `0010_app_releases.sql` pushed live. All 4 views (`v_month_household`, `v_month_user`, `v_month_category`, `v_month_income`) visible in Table Editor. |
| T-1.7  | done   | 2026-09-04 | `0008_storage.sql` (receipts bucket + policies) and `0009_seed.sql` pushed live — verified: household row + 20 categories + 6 payment methods present in Table Editor. 4 auth users created via the dashboard: Rupesh, Tanish, Trupti (`role='member'`) and Vineet (`role='admin'`), all auto-linked to the household via the `handle_new_user` trigger, `display_name`/`role` set via SQL editor. Household deliberately shipped with 4 members instead of the spec's 5 (per user — no 5th member needed right now). Sign-ups disabled: Authentication → Providers → Email → "Allow new users to sign up" turned off and saved ("Successfully updated settings"). |
| T-1.8  | done   | 2026-09-04 | All 8 RLS checks from spec §7.1 executed in the SQL editor via JWT-claim impersonation (`begin; set_config(...); set local role authenticated; <test>; rollback;`), against 2 throwaway expense rows inserted and later removed. All 8 passed as specified — see DECISIONS.md for the full per-check results. No policy changes needed. |
| **Gate 1** | **passed** | 2026-09-04 | Covered by the T-1.8 RLS checklist: RLS-1 confirmed a member JWT (Rupesh) can read all household expenses; RLS-2/3/4 confirmed a member cannot update, delete, or insert-as another member. |
| **Bug found & fixed** | — | 2026-09-03 | `0006_rls.sql` (as copied verbatim from spec §7) enabled RLS on `app_releases` before that table exists (it's created in `0010_app_releases.sql`) — `supabase db push` failed with `relation "public.app_releases" does not exist`. Fixed by moving the `app_releases` RLS-enable + policy from `0006_rls.sql` into `0010_app_releases.sql`. Recorded in DECISIONS.md as a spec correction. |
| T-2.1  | done   | 2026-09-04 | `core/config/app_config.dart` implemented verbatim from spec §5.6. |
| T-2.2  | done   | 2026-09-04 | `core/money/money.dart`: `Money` extension type over paise, Indian-grouping `format()` via `intl`'s `en_IN` locale, compact L/Cr/k formatting, `tryParse`. 15 unit tests in `test/unit/core/money_test.dart`, incl. 1 lakh, 1 crore, and 99.995 rounding. |
| T-2.3  | done   | 2026-09-04 | `core/time/app_time.dart`: fixed UTC+5:30 offset helpers (no `timezone` package needed here — that lands in Phase 13 for notification scheduling). 9 unit tests incl. the 23:55-on-the-30th boundary case. |
| T-2.4  | done   | 2026-09-04 | `Result`/`Ok`/`Err` (`core/result/result.dart`), `Failure` taxonomy (`core/errors/failure.dart`), `ErrorMapper` (`core/errors/error_mapper.dart`), `AppLogger` with a 500-entry ring buffer (`core/logging/app_logger.dart`). 6 unit tests for the mapper's classification. |
| T-2.5  | done   | 2026-09-04 | All 11 Drift tables mirroring §6 (`Households`, `Profiles`, `Categories`, `PaymentMethods`, `Expenses`, `Incomes`, `Attachments`, `Budgets`, `RecurringRules`) plus `OutboxEntries` + `SyncMeta`, each syncable table carrying `syncStatus`/`localUpdatedAt`/`isDirty`. `AppDatabase` in `core/db/app_database.dart`, `schemaVersion = 1`. `dart run build_runner build` generates cleanly. |
| T-2.6  | done   | 2026-09-04 | 9 DAOs written (`ExpenseDao`, `IncomeDao`, `CategoryDao`, `PaymentMethodDao`, `BudgetDao`, `RecurringDao`, `AttachmentDao`, `OutboxDao`, `SyncMetaDao`) under `core/db/daos/`, each with watch/find/upsert/softDelete/markSynced/hardDelete. In-memory Drift tests in `test/unit/db/expense_dao_test.dart` (insert/read, soft-delete filtering + ordering, dirty/pending flagging). |
| T-2.7  | done   | 2026-09-04 | Freezed domain models for all 9 entities + 6 enums under `domain/models/`, each with `fromJson`/`toJson` and snake_case `@JsonKey`s matching the DB columns exactly. Mapper extensions (`toDomain()` / `toCompanion()`) between Drift rows and domain models under `data/local/mappers/` — see DECISIONS.md for the naming-collision handling this required. 9 JSON round-trip tests. |
| T-2.8  | done   | 2026-09-04 | Riverpod (`flutter_riverpod` + `riverpod_generator`, code-generated) wired via `ProviderScope` in `main.dart`; `appDatabaseProvider` (keepAlive) in `core/db/database_provider.dart`. |
| T-2.9  | done   | 2026-09-04 | `go_router` set up in `routing/app_router.dart` with the full route map from spec §10.1 (`StatefulShellRoute.indexedStack` for the 4-tab bottom nav + top-level modal routes for expense/income/budget/recurring/etc.), backed by placeholder screens under `features/*/screens/`. Auth redirect logic (T-3.4) not yet wired — router boots straight to the dashboard for now. |
| **Gate 2** | **passed** | 2026-09-04 | `fvm flutter analyze --fatal-infos` clean. `fvm flutter test` green (44 tests). Verified live on the Android emulator (`kharcha_test`, Pixel 6-class, API 34) via `fvm flutter run --dart-define-from-file=config/dev.json`: app builds, installs, and boots straight to the placeholder Dashboard with working bottom-nav tabs and FAB — no crashes, no Dart exceptions. Screenshot captured via `adb screencap`. |
| T-3.1  | done   | 2026-09-04 | `Supabase.initialize()` (with `publishableKey:`, not the deprecated `anonKey:`) plus `timezone` init added to `main()`'s bootstrap, per spec §11.1 steps 1–4. Steps 6 (notifications) and 8 (kick off sync) deferred to Phase 13/Phase 4 as noted inline. |
| T-3.2  | done   | 2026-09-04 | `AuthRepository` (`data/repositories/auth_repository.dart`): sign in, sign out, reset password, `currentSession`, `onAuthStateChange`, all returning `Result<void, Failure>`. Unit tests in `test/unit/data/auth_repository_test.dart` against a mocktail-mocked `GoTrueClient`. |
| T-3.3  | done   | 2026-09-04 | Full login screen + `LoginController`: inline email/password validation, password-visibility toggle, "Forgot password?" → reset email + "Check your email" snackbar. `ErrorMapper`'s invalid-credentials message corrected to the spec-exact "Email or password is incorrect." (was "Incorrect email or password." from T-2.4, before this string was load-bearing) — see DECISIONS.md. |
| T-3.4  | done   | 2026-09-04 | Router `redirect` + `GoRouterRefreshStream` (bridges Supabase's `onAuthStateChange` to go_router's `refreshListenable`): signed-out → `/login` always; signed-in bounced off `/login`/`/splash` → dashboard. `initialLocation` changed from the dashboard to `/splash`. |
| T-3.5  | done   | 2026-09-04 | New `ProfileDao` (registered on `AppDatabase`) + `ProfileRepository` + `currentProfileProvider`: resolves the signed-in member's profile from the Drift cache first (works offline immediately), with a best-effort background refresh from Supabase's `profiles` table. |
| T-3.6  | done   | 2026-09-04 | `AppDatabase.wipeAll()` (deletes every row from every table) wired into a new `SignOutController`, triggered from a minimal Sign-out control added to the Settings placeholder (full Settings UI is Phase 14). Offline session tolerance itself needed no new code — it's `supabase_flutter`'s default persisted-session behaviour; verified live (see Gate 3). |
| **Gate 3** | **passed** | 2026-09-04 | Verified live on the `kharcha_test` emulator. Signed-out boots to `/login` (screenshot). Real sign-in (user-entered credentials, not scripted) → dashboard. Force-stopped the app, fully severed connectivity (`svc wifi disable` + `svc data disable`, confirmed 0 connected networks via `dumpsys connectivity`), cold-relaunched → straight to dashboard, still signed in, no crash. Sign-out → back to `/login`; pulled the on-device `kharcha.sqlite` via `adb exec-out run-as ... cat` and confirmed all 11 tables have 0 rows. Two real bugs were found and fixed during this live pass — see DECISIONS.md. |
| T-4.1  | done   | 2026-09-04 | `ConnectivityService` (`core/network/connectivity_service.dart`) wraps `connectivity_plus`; `isOnline` future + `onStatusChange` stream. |
| T-4.2  | done   | 2026-09-04 | `TableRemoteDataSource` (shared select-since-cursor/upsert/soft-delete) + 8 named subclasses under `data/remote/`, plus `HouseholdRemoteDataSource` (single-row fetch). See DECISIONS.md for why these are thin subclasses of one shared implementation rather than 9 independent ones. |
| T-4.3  | done   | 2026-09-04 | `OutboxProcessor` (`data/sync/outbox_processor.dart`): dependency-ordered FIFO drain, `computeBackoff()` (`data/sync/backoff.dart`, exponential + ±20% jitter, 15 min cap), permanent-vs-transient classification reusing `ErrorMapper` (see DECISIONS.md), `OutboxEntries.status` column added (schema v1→v2). `upload` op wired structurally for Phase 10. 6 unit tests. |
| T-4.4  | done   | 2026-09-04 | `PullService` (`data/sync/pull_service.dart`): pages every entity's since-cursor changes (500-row pages), advances `sync_meta` cursors, tombstone handling. 3 unit tests incl. a 501-row paged pull. Found & fixed a real Drift `DateTime` UTC-flag bug in the cursor path while writing this test — see DECISIONS.md. |
| T-4.5  | done   | 2026-09-04 | `SyncEngine` (`data/sync/sync_engine.dart`): single-flight lock, push→pull→publish algorithm exactly per spec §9.6, owns the connectivity subscription (trigger 3) and 5-min periodic timer (trigger 6); `start()`/`stop()` are idempotent and resumable (sign-out calls `stop()`, next sign-in re-arms). 5 unit tests, incl. a rapid-double-trigger test that caught and fixed a real single-flight race — see DECISIONS.md. |
| T-4.6  | done   | 2026-09-04 | D12 conflict resolution lives in each `EntitySyncAdapter.pullApply()` (`data/sync/entity_sync_adapters.dart`): a dirty-and-newer local row is kept; otherwise remote wins, and if that discards an unpushed local edit, the loss is logged via `AppLogger`. 4 unit tests covering spec §13 Test 5 both directions, plus the clean-overwrite and tombstone cases. |
| T-4.7  | done   | 2026-09-04 | `SyncState` sealed class + `SyncStateController` (Riverpod `Notifier`) + `SyncBanner` widget, wired into `AppShell` above the nav shell. Renders nothing when idle-and-clean. |
| T-4.8  | done   | 2026-09-04 | `RealtimeListener` (`data/sync/realtime_listener.dart`): subscribes to `expenses`/`incomes`/`budgets` Postgres changes filtered by `household_id`, 2s per-table debounce → single-entity pull. Guarded by `AppConfig.realtimeEnabled` (default on). |
| **Gate 4** | **partial** | 2026-09-04 (re-verified 2026-09-05, fix implemented 2026-09-05) | Full push/pull/conflict/backoff machinery built and unit-tested (22 new tests in `test/unit/sync/`, `fvm flutter test` green at 78 total, `flutter analyze --fatal-infos` clean). **Live-verified the pull path** against the real Supabase project on the `kharcha_test` emulator: after a real sign-in, `kharcha.sqlite` (pulled via `adb exec-out run-as ... cat`) shows all 20 categories, 6 payment methods, 4 profiles, and the household row synced locally, with `sync_meta` populated for all 9 entities and an empty outbox — see DECISIONS.md for full detail. **Two-device "both edit offline, reconcile online" scenario live-verified 2026-09-05** on two real emulators (Vineet/admin + Rupesh/member, both offline, editing the same expense): reconciliation converges to one consistent state on both devices — no crash, no duplicate rows, no permanent fork. But it resolved by **last-device-to-push-wins, not newest-edit-wins** — Vineet's older offline edit silently overwrote Rupesh's newer one, with no `AppLogger` conflict entry on either device, contradicting spec §13 Test 5's "no data loss... the losing version is logged" guarantee for this literal case. **Fixed 2026-09-05**: pushes now carry an optimistic-concurrency compare-and-swap against the row's last known-good server value, so a genuine cross-device conflict is detected and resolved by timestamp (newest edit wins) regardless of push order — see DECISIONS.md for the full mechanism. Covered by 4 new tests in `test/unit/sync/push_conflict_resolution_test.dart` (matching base pushes cleanly; remote-newer conflict discards and logs the local edit; local-newer conflict retries without data loss; a brand-new row pushes unconditionally) plus a new `test/unit/db/migration_v2_to_v3_test.dart` proving the schema migration preserves existing family data on upgrade. `fvm flutter test` green at 182, `flutter analyze --fatal-infos` clean. Status held at **partial** pending a live two-device re-verification of this exact fix (not done this session — no emulator available) before flipping to passed. |
| T-5.1  | done   | 2026-09-04 | `CategoryRepository` + `PaymentMethodRepository` (`data/repositories/`): local-first Drift streams, every write also enqueues an outbox entry (create/update/archive/reorder → `op:'upsert'`; delete → `op:'delete'`). `delete()` is blocked with a `ValidationFailure` while a non-deleted expense still references the id (spec §11.5), offering "Archive instead" in the UI. 8 unit tests (`category_repository_test.dart`, `payment_method_repository_test.dart`) incl. the delete-guard both ways. Admin-only write is enforced by RLS (`cat_write`/`pm_write`) plus hiding the controls for members — the repositories themselves don't gate on role. |
| T-5.2  | done   | 2026-09-04 | Category management screen (`features/categories/`): drag-to-reorder (admin-only `ReorderableListView`), archive toggle, edit sheet (name/kind-locked-after-creation/icon/16-swatch colour), delete with an "Archive" snackbar action on the usage-guard failure. Members get the same list read-only, no controls. |
| T-5.3  | done   | 2026-09-04 | Payment method management screen — same shape as T-5.2, minus the icon/colour picker (`payment_methods` has no such columns; its icon is derived from `type` via `iconForPaymentMethodType`). |
| T-5.4  | done   | 2026-09-04 | `ExpenseRepository` (`data/repositories/expense_repository.dart`): create/update/delete → Drift + outbox; `watchFiltered`/`watchFilteredTotal` back the list (grows a `limit`, not an offset — see DECISIONS.md); `hasPossibleDuplicate`, `lastUsedPaymentMethodId`, `mostUsedCategoryIds`, `recentDistinctNotes/Merchants` back the Add Expense form. `undoCreate()` implements the 5s Undo snackbar (T-5.6). 7 unit tests in `expense_repository_test.dart`. **Found & fixed a real bug** while writing these tests: every mapper's `toDomain()` was missing the `.toUtc()` normalisation already applied on the pull side in Phase 4 — see DECISIONS.md, this would have silently pushed edited rows' timestamps to the server 5:30 off (IST). Fixed in all 9 `data/local/mappers/*.dart` files. |
| T-5.5  | done   | 2026-09-04 | Add/Edit Expense screen (`features/expenses/screens/expense_detail_screen.dart`) per spec §11.2: amount field with Money validation, category chip row (8 most-used + "More" sheet), payment-method chip row (defaults to last used), Today/Yesterday chips + date/time picker (blocks >1 day future), note/merchant fields with `RawAutocomplete` from the last 20 distinct values, admin-only "Paid by" member selector. |
| T-5.6  | done   | 2026-09-04 | Duplicate guard (`ExpenseRepository.hasPossibleDuplicate`, same amount+category+user within 2 minutes) shows a non-blocking "save anyway?" dialog. Undo snackbar (5s) calls `undoCreate()`, which removes the create's outbox entry outright if it hasn't been pushed yet (server never sees it) or falls back to a real soft-delete + outbox delete if it has. |
| T-5.7  | done   | 2026-09-04 | Expense List (`features/expenses/screens/expense_list_screen.dart`): reverse-chronological, grouped by date with a day-total header, infinite scroll (limit grows by 50 on near-bottom scroll, re-subscribing the same live Drift stream rather than true offset paging — see DECISIONS.md), swipe-right to duplicate (pre-fills Add form via `GoRouterState.extra`, date reset to now), swipe-left to delete (blocked for non-owners), cloud-off badge for dirty rows (new `Expense.isDirty`, excluded from JSON both ways). |
| T-5.8  | done   | 2026-09-04 | Filter sheet (`features/expenses/widgets/expense_filter_sheet.dart`): date range (This month / All time / custom), member(s), category(ies), payment method(s), amount range, only-mine, only-with-receipts — all combinable via `ExpenseFilter` (new freezed model, not persisted). Free-text search (300ms debounce) over note/merchant. Header shows the live filtered total via `watchFilteredTotal`. |
| T-5.9  | done   | 2026-09-04 | Same `ExpenseDetailScreen` handles this: a signed-in member who is neither the expense's owner nor an admin gets `_ReadOnlyExpenseView` instead of the form — no Save, no Delete, no editable fields. |
| **Gate 5** | **passed** | 2026-09-04 | All of T-5.1–T-5.9 implemented; `flutter analyze --fatal-infos` clean; `fvm flutter test` green at 93 tests. **Live-verified** on the `kharcha_test` emulator by the user: add/edit/delete an expense, category and payment method management (add/edit/archive/reorder), and the offline test (aeroplane mode save → cloud-off badge → reconnect → syncs) all passed. One bug found and fixed during this pass — see DECISIONS.md (the Undo snackbar never auto-dismissed). Settings screen got minimal "Categories"/"Payment methods" nav links added so those Phase 5 screens are reachable ahead of their real home in Phase 14. |
| T-6.1  | done   | 2026-09-04 | `ReportDao` (`core/db/daos/report_dao.dart`, new) + `ReportRepository` (`data/repositories/report_repository.dart`): household expense/income totals, per-member, per-category, and per-payment-method breakdowns, all as live Drift streams scoped to a `[start, end)` period — month-over-month is just calling the same total query again with the previous month's bounds, not a separate query. Per-payment-method isn't surfaced on the Dashboard yet (only cards 1/3/4/5 ship this phase) but is implemented and tested now for Analytics (Phase 11) to reuse. 8 DAO tests in `test/unit/db/report_dao_test.dart` against a hand-computed fixture (mixed members/categories/payment methods, a soft-deleted row, and an out-of-period row, all asserted excluded/included correctly). |
| T-6.2  | done   | 2026-09-04 | `SelectedMonthController` (`features/dashboard/controllers/`, Riverpod `Notifier`, `keepAlive`) holds the selected month so it survives tab switches and can be shared with Analytics later. App-bar month selector (‹ / month label / ›) plus a year+month-grid picker dialog; both the `›` arrow and picker cells are disabled for any month after the current one via `AppTime.isFutureMonth`. |
| T-6.3  | done   | 2026-09-04 | Dashboard cards 1/3/4/5 per spec §11.4: household summary (spent/income/net saved + month-over-month % with a coloured arrow), per-member breakdown (horizontal bars, sorted descending, **tap filters the Expense List to that member** — see DECISIONS.md for how that cross-tab handoff works), top 5 categories with a "See all" link to Analytics, and the last 5 expenses household-wide with tap-to-detail. Cards 2 (budget progress) and 6 (pending recurring) are out of scope per T-6.3's own task list — they land with Phases 8 and 9. |
| T-6.4  | done   | 2026-09-04 | Dashboard body wrapped in a `RefreshIndicator` whose `onRefresh` calls `SyncEngine.sync()` directly; the pull spinner is Flutter's own (tied to the `Future` it awaits), not driven off `SyncState`. |
| T-6.5  | done   | 2026-09-04 | Every percentage on the Dashboard (member share, category share, month-over-month change) guards its denominator being zero and renders a plain empty-state message instead of a bar/row when a card's underlying list is empty — no `NaN`, no `Infinity`, no divide-by-zero, verified against a household with a single ₹50 expense and no income (see Gate 6). |
| **Gate 6** | **passed** | 2026-09-04 | All of T-6.1–T-6.5 implemented; `flutter analyze` clean; `fvm flutter test` green at 101 tests. **Live-verified** on the `kharcha_test` emulator (`fvm flutter run --dart-define-from-file=config/dev.json`), screenshot captured via `adb exec-out screencap`: against the real seeded household data, the Dashboard rendered "This month: Spent ₹50.00, Income ₹0.00, Net saved -₹50.00", "Per member: Vineet ₹50.00 (100%)" with its progress bar, "Top categories: zomato 100% ₹50.00", and "Recent activity: ZXC · Vineet · ₹50.00" — all four live cards correct against the Expense List's own totals for the same month, with no crashes and no `NaN`/`₹0.00`-division artifacts despite the near-empty dataset. `widget_test.dart` needed two fixes to keep passing once the Dashboard started issuing live Drift queries — see DECISIONS.md. |
| T-7.1  | done   | 2026-09-04 | `IncomeRepository` (`data/repositories/income_repository.dart`) mirroring `ExpenseRepository`'s shape: local-first Drift read, every write → Drift + outbox. Deliberately simpler than expenses (no duplicate guard, no undo snackbar, no payment method — none are in scope for income per spec §11.6). Added `IncomeDao.watchById`/`countByCategory` (the latter mirroring `ExpenseDao.countByCategory`). **Bug found & fixed in passing**: `CategoryRepository.delete()`'s usage guard (T-5.1) only ever checked `expenseDao.countByCategory` — an income-kind category with existing income entries could be silently deleted, orphaning those rows' `category_id` client-side reads. Now sums both counts. 4 repository tests + 1 new category-guard test (`category_repository_test.dart`). |
| T-7.2  | done   | 2026-09-04 | Add/Edit Income screen (`features/income/screens/income_detail_screen.dart`, built out from its Phase-2 placeholder) per spec §11.6: amount field, income-kind category chips, Today chip + date picker (blocks >1 day future, no time-of-day — income is date-only), source/note fields, admin-only "Received by" member selector, same owner-or-admin edit rule as expenses (RLS `inc_update`/`inc_delete` mirror `exp_update`/`exp_delete`) with a `_ReadOnlyIncomeView` fallback. Income List (`features/income/screens/income_list_screen.dart`, new): reverse-chronological, grouped by date with a day-total header, swipe-to-delete — simpler than the Expense List (no filter sheet/search/infinite scroll; income volume is far lower). Reachable from Settings ("Income" nav link) and from the Dashboard's Income row (now tappable). New route `/income` (list) alongside the existing `/income/new`/`/income/:id`. |
| T-7.3  | done   | 2026-09-04 | Already substantially implemented in Phase 6 (T-6.1's `ReportDao.watchIncomeTotal`, the Dashboard's `_HouseholdSummaryCard` computing `net = income − expense`) — T-7.3 added the missing piece: the Dashboard's Income row is now tappable (`_SummaryRow.onTap`) and pushes `/income`, satisfying spec §11.6's "reachable ... from the Dashboard's income figure". |
| **Gate 7** | **passed** | 2026-09-04 | Income and expense totals are computed from separate tables/queries throughout (`ReportDao.watchExpenseTotal` vs `watchIncomeTotal`) — never contaminated by construction. `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 110 tests. **Live-verified** on the `kharcha_test` emulator: added a real ₹50,000 "Salary" income (source "Employer", received by Vineet) via the live UI (amount field, category chips, date picker, source field, member picker all worked correctly), saved, landed back on the Income List showing "Total ₹50,000.00" / "Today ₹50,000.00" / the new row with its category icon. Dashboard immediately reflected it live: "Income ₹50,000.00", "Net saved ₹49,950.00" (green), while "Spent ₹50.00" and every other card stayed unchanged — expense figures were not touched by the new income row. Pulled `kharcha.sqlite` off the device (`adb exec-out run-as ... cat app_flutter/kharcha.sqlite`, same technique as prior gates — note the correct path is `app_flutter/kharcha.sqlite`, not `databases/kharcha.sqlite`) and confirmed the row pushed successfully: `is_dirty=0`, `sync_status='synced'`, empty `outbox_entries`. |
| T-8.1  | done   | 2026-09-05 | `BudgetRepository` (`data/repositories/budget_repository.dart`): local-first Drift read, every write → Drift + outbox. `isValidBudgetScopeShape()` (`domain/models/budget.dart`) mirrors the DB's `budgets_scope_shape` check constraint exactly; `create()`/`update()` return `Result.err(ValidationFailure)` for an invalid shape instead of round-tripping to Postgres to be told no. New `BudgetDao.findByScope` (exact scope-shape lookup, mirroring the `budgets_unique_scope` index) and `ReportDao.watchExpenseForScope` (the 4 budget scopes are just the 4 combinations of an optional member/category filter on the existing expense-total query). 9 repository tests + 4 new `ReportDao` scope-filter tests. |
| T-8.2  | done   | 2026-09-05 | Budget list (`features/budgets/screens/budget_list_screen.dart`) and editor (`budget_detail_screen.dart`) screens per spec §11.7: scope chips (admin sees all 4; a member sees only User/User+Category, forced to themselves — mirrors `bud_write`'s RLS shape client-side), month stepper, rollover switch, alert-threshold slider, "Also create for the next 12 months" checkbox wired to `BudgetRepository.copyToNext12Months()` (creates 12, skipping any month that already has a same-shape budget). List screen groups by scope with a live per-row progress bar and an ok/warning/exceeded summary header. |
| T-8.3  | done   | 2026-09-05 | `domain/models/budget_status.dart`: `BudgetHealth` enum + `BudgetStatus` (spent/effective-budget/remaining/overspend/pct/health) + `computeRolloverPaise()`, all pure functions per spec's "compute locally; do not store the derived number." `BudgetRepository.watchStatus()` combines a live scoped-spend stream with a one-shot previous-month rollover lookup. 13 unit tests covering ok/warning/exceeded boundaries (incl. exactly-at-threshold and exactly-at-100%) and rollover math (under/over/no-previous-budget). |
| T-8.4  | done   | 2026-09-05 | Dashboard card 2 (`_BudgetProgressCard`/`_BudgetProgressRow` in `dashboard_screen.dart`): one row per this month's budget — spent/effective-budget, a coloured progress bar, and "₹X left · ₹Y/day for N days" (daily allowance = remaining ÷ `AppTime.daysRemainingInMonth`, new helper). Renders nothing when there are no budgets for the month, per the Phase 6 precedent for absent cards. |
| T-8.5  | done   | 2026-09-05 | `NotificationService` (`core/notifications/notification_service.dart`, brought forward from Phase 13 — see DECISIONS.md) + `BudgetAlertService` (`data/repositories/budget_alert_service.dart`): evaluates every current-month budget's status, firing a local notification on crossing into warning/exceeded, deduplicated via a `shared_preferences` key `budget_alert_<budgetId>_<yyyyMM>` storing the last-*notified* status (cleared on a return to `ok` so a later re-crossing counts as a new transition and notifies again). Wired to fire after every expense save (`expense_detail_screen.dart`) and on every app resume (`app.dart`). Copy matches spec §11.7 exactly ("Household budget 91% used — ₹5.00 left for 26 days" / "... exceeded by ₹X"). |
| **Bug found & fixed** | — | 2026-09-05 | **`DateTime.parse` on a bare Postgres `date` column silently corrupted the value by the device's UTC offset.** `spent_on`/`received_on`/`period_month`/`start_date`/`end_date`/`next_due_date`/`last_posted_on` are all `date`-typed (no time/zone) server-side; PostgREST serialises them as a bare `"2026-09-01"`, and Dart's `DateTime.parse` on a string with no offset returns a **local**-flagged DateTime — on an IST device (every Kharcha device, by design) that's the wrong instant by exactly 5:30 once stored via `millisecondsSinceEpoch`. Found live: a budget created for "September 2026" was pulled back with `period_month` = 2026-08-31T18:30Z, so an exact-equality month query for it silently returned nothing. Fixed with a shared `AppTime.parseDateOnly`/`parseDateOnlyOrNull` (forces `DateTime.utc(y,m,d)`, discarding the device timezone) applied via `@JsonKey(fromJson: ...)` on all 7 affected fields across `Expense`, `Income`, `Budget`, and `RecurringRule` — this was a **latent, pre-existing bug since Phase 4/5**, not new in Phase 8; range-filtered queries (`>=`/`<`, used by every Dashboard/Expense-List total) mostly masked it, only budgets' exact-equality month lookup surfaced it. See DECISIONS.md for the full diagnosis. |
| **Gate 8** | **passed** | 2026-09-05 | `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 140 tests. **Live-verified** on the `kharcha_test` emulator against the real Supabase project: created a household budget, edited it to ₹55 against the month's real ₹50 spend (91%, threshold 80%), backgrounded and resumed the app, and confirmed via `adb shell dumpsys notification` a real system notification posted on channel `budget_alerts` — title "Budget alert", body "Household budget 91% used — ₹5.00 left for 26 days" (matches spec copy exactly). Confirmed the dedup key `budget_alert_<id>_202609` = `"warning"` was persisted in `FlutterSharedPreferences.xml`, so a second resume without a status change does not re-fire (verified by design and by the persisted key, per T-8.5's dedup logic). All test budgets deleted afterward and confirmed hard-deleted locally after the tombstone round-trip through Supabase — no leftover test data. |
| T-9.1  | done   | 2026-09-05 | `domain/models/recurring_schedule.dart`: `advanceDueDate` mirrors the SQL `advance_due_date()` function's daily/weekly/monthly/yearly branches exactly, incl. the monthly clamp-to-last-valid-day; the SQL's own dead `p_weekday` parameter isn't carried into Dart (see DECISIONS.md). 15 unit tests incl. 31 Jan → 28 Feb (non-leap) and → 29 Feb (leap year), and yearly's deliberately-unclamped 29 Feb → 1 Mar overflow. |
| T-9.2  | done   | 2026-09-05 | `RecurringRepository` (`data/repositories/recurring_repository.dart`): local-first Drift read, every write → Drift + outbox; `create()` seeds `next_due_date = start_date`. `previewOccurrences()` wraps a new pure `dueOccurrencesFor`/`previewOccurrences` pair in `recurring_schedule.dart`. Rule editor (`features/recurring/screens/recurring_detail_screen.dart`, built out from its Phase-2 placeholder) per spec §11.8: title, kind (locked after creation, same precedent as category kind), amount, category (filtered by kind), payment method (expense only), note, frequency + interval + day-of-month (monthly only), start/end date pickers, Auto-post toggle, admin-only member picker, and the live "Next occurrences" preview. Recurring List (`recurring_list_screen.dart`, also built out): every household rule sorted active-first then by `next_due_date`, swipe-to-delete gated by owner-or-admin (`rec_write`'s RLS shape). New "Recurring" link on Settings. 6 repository tests. |
| T-9.3  | done   | 2026-09-05 | `RecurringPostingEngine` (`data/sync/recurring_posting_engine.dart`): runs inside `SyncEngine.sync()` right after the pull, pushes again immediately after. Auto-post rules catch up via `dueOccurrencesFor`, capped at 24 occurrences/run (T-9.3's own cap); non-auto-post rules are left untouched (next_due_date frozen at the earliest pending occurrence) except for silently skipping anything more than 30 days stale, per spec §11.8's last bullet. Idempotent per (rule, occurrence) via `ExpenseDao.findByOccurrence`/`IncomeDao.findByOccurrence` (new DAO methods) against a local crash-recovery re-run. A posted occurrence's `merchant`/`source` carries the rule's `title` (see DECISIONS.md — expenses/incomes have no title field of their own). 10 unit tests in `recurring_posting_engine_test.dart` (in-memory Drift) incl. the 24-occurrence cap across two runs and end-date deactivation. |
| T-9.4  | done   | 2026-09-05 | `OutboxProcessor._handleRecurrenceDuplicate`: a `23505` unique-violation on an expense/income upsert whose payload carries a non-null `recurring_rule_id` is caught, the local phantom row hard-deleted, and the outbox entry dropped as a success (no retry, no `'failed'` state) — the winning device's row arrives on the next pull. Distinct from T-9.3's same-device idempotency check; see DECISIONS.md for how the two races differ. 3 new tests in `outbox_processor_test.dart`, incl. a scripted "device A succeeds, device B's push is discarded" scenario against the same local outbox (see DECISIONS.md re: why this stands in for a literal two-device run, same precedent as Gate 4). |
| T-9.5  | done   | 2026-09-05 | Dashboard card 6 (`_PendingRecurringCard`/`_PendingRecurringRow` in `dashboard_screen.dart`): every active, non-auto-post, currently-due rule (mirrors `RecurringDao.dueOn`'s own filter, read off the same `householdRecurringRulesProvider` stream the list screen uses), with Post/Skip buttons wired to new `RecurringRepository.postPending`/`skipPending` → `RecurringPostingEngine.postOneOccurrence`/`skipOneOccurrence` (single-step: create-and-advance, or advance-only). Renders nothing when nothing is pending, same empty-state precedent as the budget card. |
| **Bug found & fixed** | — | 2026-09-05 | The Auto-post `SwitchListTile`'s subtitle was a `const Text` hardcoded to the "Off" copy — toggling the switch on during live verification left the helper text still reading "Off: ...". Fixed by keying the subtitle off `_autoPost`. |
| **Gate 9** | **passed** | 2026-09-05 | `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 176 tests (36 new, across `recurring_schedule_test.dart`, `recurring_posting_engine_test.dart`, `recurring_repository_test.dart`, and 3 new `outbox_processor_test.dart` cases). **Live-verified** on the `kharcha_test` emulator against the real Supabase project: created an auto-post "Netflix" ₹499/month rule (day 5, today) — the very next sync cycle posted it automatically with the correct category ("Subscriptions") and merchant ("Netflix"), and the rule's `next_due_date` advanced 5 Sep → 5 Oct. Created a second, manual "Rent" ₹15,000/month rule — the Dashboard's "Pending confirmations" card appeared with Skip/Post; tapping Post created the expense, advanced `next_due_date`, and the card disappeared once nothing was left pending. Both rules and their posted expenses were deleted afterward, restoring the shared household data to its pre-test state. The literal two-device race from Test 4/§13 was not re-run on a second device this gate — covered instead by `outbox_processor_test.dart`'s scripted duplicate-push scenario (T-9.4), matching Gate 4's precedent. |
| T-10.1  | done   | 2026-09-05 | `AttachmentRepository.addFromFile()` (`data/repositories/attachment_repository.dart`, new): compresses via an injectable `ImageCompressor` (default `FlutterImageCompress.compressWithFile`, `minWidth`/`minHeight` 1600, quality 80), saves to `<app-docs>/receipts/<id>.jpg`. `android/app/src/main/AndroidManifest.xml` gained the `CAMERA` permission for `image_picker`'s camera source; no iOS Info.plist change since `ios/` was dropped earlier (Android-only for now). See DECISIONS.md for why the compressor is injected rather than called statically, and why "4 MB → <400 KB" is a live-verification item, not a unit test. |
| T-10.2  | done   | 2026-09-05 | Same `AttachmentRepository.addFromFile()` inserts the `attachments` Drift row (`is_dirty = true`) and enqueues an `upload` outbox job (`op: 'upload'`, payload `{local_path, storage_path, row}`) — capture and queueing both work fully offline. Two new `AttachmentDao` methods: `countForExpense()` (max-3-per-expense guard) and `findActiveForExpense()` (T-10.5's cascade). 5 repository tests in `test/unit/data/attachment_repository_test.dart` (in-memory Drift + a `path_provider_platform_interface` fake, same technique as `migration_v2_to_v3_test.dart`, + a pass-through fake compressor). |
| T-10.3  | done   | 2026-09-05 | The `upload` outbox op was already wired structurally since Phase 4 (`OutboxProcessor._processUpload`: uploads the cached file to the `receipts` bucket, then upserts the row) — nothing changed there. `has_receipt` is maintained via new `ExpenseRepository.setHasReceipt()`, called by `AttachmentRepository` on add (→ true) and on delete when no receipts remain (→ false). See DECISIONS.md for why `AttachmentRepository` doesn't own the `expenses` row directly. |
| T-10.4  | done   | 2026-09-05 | Expense detail screen (`expense_detail_screen.dart`) grew a Receipts section (existing-expense only — see DECISIONS.md): a thumbnail row (`_ReceiptThumbnail`, resolves local cache → signed-URL download → cache, per spec §11.9) with a cloud-off "upload pending" badge on dirty attachments, an "Add photo" chip (camera/gallery via `image_picker`), long-press-to-delete. `ReceiptViewerScreen` (`features/receipts/screens/receipt_viewer_screen.dart`) built out from its Phase-2 placeholder: `InteractiveViewer` pinch-zoom, a share button (`SharePlus.instance.share`), the same cache-first resolution via `AttachmentRepository.resolveLocalFile()`. `Attachment` domain model gained an `isDirty` field (mirroring `Expense.isDirty`) to back the badge. |
| T-10.5  | done   | 2026-09-05 | `ExpenseRepository.delete()` now cascades: soft-deletes every active attachment of the expense and enqueues its removal (reaches into `_db.attachmentDao`/`_db.outboxDao` directly, same pattern as `CategoryRepository`'s usage guard — no `AttachmentRepository` dependency). `OutboxProcessor`'s `delete` case, for `entity == 'attachment'`, now best-effort deletes the actual Storage object (`client.storage.from('receipts').remove(...)`) before the row's tombstone — wrapped in try/catch so a failed storage delete never blocks the row from propagating. The Diagnostics "list orphaned objects" admin screen itself is Phase 14 work and was not built now — see DECISIONS.md. |
| **Bug found & fixed** | — | 2026-09-05 | Live two-device Gate 10 verification surfaced a push-side bug: `_pushUpsertWithCas` stamped `base_updated_at` from the outbox payload's own claimed `updated_at` instead of what Postgres's `touch_updated_at()` trigger actually stored (`GREATEST(now(), incoming)`), so a second queued edit to the same row (e.g. attach-a-receipt-right-after-create) would CAS-mismatch and be silently discarded. Fixed: `TableRemoteDataSource.upsertIfBaseMatches()` now returns the server's real resulting `updated_at` (`Future<DateTime?>`, via a trailing `.select('updated_at')`) instead of `Future<bool>`, and `markSynced` uses that. New regression test in `push_conflict_resolution_test.dart`. See DECISIONS.md for the full trace. |
| **Bug found & fixed** | — | 2026-09-05 | Same investigation surfaced a second, more severe pre-existing bug: every entity mapper's `toCompanion(dirty: true)` (expense/income/category/payment_method/budget/recurring_rule/attachment) never set `local_updated_at` — it stayed null forever except after a soft-delete. This silently defeated the entire Gate 4 CAS-conflict fix (its "is local newer" check can never be true against a null timestamp) and crashed the sync loop (`_logConflictLoss` force-unwrapped it) the first time a dirty row actually hit a CAS mismatch, misreported to the user as "Something went wrong" and retried forever without ever resolving. Fixed: all 7 mappers now stamp `localUpdatedAt` when dirty; `_logConflictLoss` made defensive against a still-null value from pre-fix data. New `test/unit/data/mapper_local_updated_at_test.dart` (7 cases) plus a new crash-regression case in `push_conflict_resolution_test.dart`. Found and root-caused by reading the real exception off a live VM-service connection — `AppLogger`'s `dart:developer` output doesn't reach `adb logcat` for an installed APK; see DECISIONS.md for the technique. |
| **Bug found & fixed** | — | 2026-09-05 | A third, deeper bug surfaced while confirming the fixes above live: `base_updated_at` round-tripped through Drift's `DateTimeColumn` at whole-**second** precision, but Postgres's `updated_at` has microsecond precision — so a CAS retry's `.eq('updated_at', ...)` could mismatch forever once a row needed a real conflict-resolution cycle (not just a first, unconditional insert). Confirmed live: one test expense's `has_receipt` push kept hitting the correct, intended `SyncConflictRetryException` retry path but never converged. Fixed: the CAS base is now a raw `String?` everywhere (`TableRemoteDataSource.upsertIfBaseMatches`, `_LocalSyncMeta`, every DAO's `markSyncedWithBase`/`updateBaseUpdatedAt`, every mapper's `toCompanion`) — the server's `updated_at` read straight off the JSON response and never parsed into a `DateTime`/reformatted, so there's no code path left that could reintroduce drift. `base_updated_at` moved from `DateTimeColumn` to `TextColumn` on all 7 syncable tables (schema v3 → v4, `m.dropColumn`+`m.addColumn`, resetting every row's base to null — safe, since the old value could never CAS-match anyway). New `test/unit/db/migration_v3_to_v4_test.dart` plus a sub-second-precision round-trip case in `push_conflict_resolution_test.dart`. **Live re-verified against the real stuck row**: rebuilt, reinstalled over the *same* on-device database that had been retrying for hours, and it converged cleanly on the next sync — `has_receipt=1`, `is_dirty=0`, `sync_status='synced'`, `base_updated_at='2026-09-05T13:32:17.259813+00:00'` (a precise, microsecond-bearing string). See DECISIONS.md for the full trace, including an unrelated emulator DNS glitch hit and resolved along the way. |
| **Bug found, unrelated, not fixed** | — | 2026-09-05 | Tapping a member's row in the Dashboard's "Per member" card (spec §6.3, T-6.3, Phase 6 — unrelated to Receipts) crashed twice with a Riverpod "modify a provider while the widget tree was building" error. Pre-existing, not investigated further; noted so it isn't mistaken for a Gate 10 regression. |
| **Gate 4** | **partial** (unchanged, higher confidence) | 2026-09-05 | The precision fix above was live-verified against a real CAS conflict-retry converging correctly end-to-end for the first time this build has observed it succeed — every earlier attempt this session hit one of the three bugs above first. That is not, however, a re-run of Gate 4's own specific pending scenario (two separate devices editing the *same* row while both offline, reconnecting, newest-edit-wins) — it was one device's own two sequential edits colliding with themselves. Gate 4 stays **partial**, still blocked only on that literal two-device re-run, but the CAS plumbing it depends on has now been proven against the real Supabase project rather than only in test fakes. |
| **Gate 10** | **passed** | 2026-09-05 | `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 198 tests. **Live-verified** on two real Android emulators (`kharcha_test` = Vineet/admin, `kharcha_test_2` = Rupesh/member) against the real Supabase project: Rupesh, fully offline (`svc wifi/data disable`), created a ₹250 "Groceries" expense and attached a photo (712 KB source compressed to 151 KB cached) — both saved locally with correct offline bookkeeping. Reconnected — the attachment uploaded and synced cleanly (`is_dirty=0`, `sync_status='synced'`, correct `<household_id>/<expense_id>/<attachment_id>.jpg` storage path). Vineet, on a separate emulator, opened the same expense and saw the receipt thumbnail (downloaded via signed URL, cached) and the full-screen pinch-zoom viewer with its share button — Gate 10's literal acceptance ("capture offline → reconnect → the image is visible on a second device") is satisfied. Held at partial pending the sub-second-precision fix above; flipped to **passed** once that fix converged the same expense's `has_receipt` live against the real project. |
| T-11.1 | done | 2026-09-06 | `ReportDao` (`core/db/daos/report_dao.dart`) gained 5 new aggregate queries for the 7 charts in spec §11.10, all live Drift streams: `watchMonthlyTrend` (12-month household expense+income, one `customSelect` UNION ALL over `expenses`/`incomes` grouped by `strftime('%Y-%m', ..., 'unixepoch')`, zero-filled for months with no rows at all — SQLite's default epoch-**seconds** `DateTimeColumn` storage, confirmed unchanged since Phase 2, is what makes the `unixepoch` modifier correct here), `watchMemberMonthlyTrend`/`watchCategoryMonthlyTrend` (same shape, grouped by `user_id`/`category_id` too, sparse — gaps filled by the caller), `watchExpenseByWeekday` (`CAST(strftime('%w', ...) AS INTEGER)`, converted from SQLite's 0=Sunday to Dart's 1=Monday..7=Sunday convention), and `watchTopMerchants` (typed `selectOnly` query, same shape as the existing `watchExpenseByCategory`/`watchExpenseByPaymentMethod`, excluding blank `merchant`). The category donut and payment-method split reuse the existing (Phase 6/8) `watchExpenseByCategory`/`watchExpenseByPaymentMethod` queries rather than needing new ones. `ReportRepository` grew matching pass-through methods. 9 new DAO tests appended to `test/unit/db/report_dao_test.dart` against the shared Phase-6 fixture (extended with `merchant` values), incl. a month with zero rows at all reading back as zero-not-missing, and an independent hand-computed weekday map (not reusing the DAO's own conversion) so the test would actually catch an off-by-one in the SQLite-to-Dart weekday mapping. |
| T-11.2/T-11.3 | done | 2026-09-06 | `features/analytics/screens/analytics_screen.dart` built out from its Phase-2 placeholder with all 7 charts: monthly trend (`fl_chart` `LineChart`, expense+income lines, built-in tap-to-tooltip via `LineTouchData`'s default `handleBuiltInTouches`), category donut (`PieChart`, top 8 + "Other", legend with amount+%), member comparison (`BarChart`, grouped, last 6 months × members, colours matching each member's `profile.colourHex`), payment-method split and top merchants (plain proportional-bar/ranked-list widgets, not forced into `fl_chart` — it has no native horizontal-bar orientation, and the Dashboard's existing per-member bar is a better fit than fighting the library), day-of-week (`BarChart`, average = weekday total ÷ how many times that weekday occurred in the period, computed client-side from the period bounds), and month-over-month (`DataTable`, category rows × last 3 month columns + a Δ% column colour-coded red/green matching the Dashboard's existing month-over-month arrow convention). The month selector and the repeated title+"See all"-card shell were pulled out of `dashboard_screen.dart` into shared `features/dashboard/widgets/month_selector.dart` (`MonthSelector`) and `section_card.dart` (`SectionCard`/`EmptySectionBody`) — spec §11.10 says the month selector must literally be "shared with the Dashboard", and the card shell was already byte-for-byte identical between the two screens. |
| T-11.4 | done | 2026-09-06 | Every chart checks its own emptiness (`totals.isEmpty` / `every(... == 0)`) and renders `EmptySectionBody` ("No data for this period.") instead of an empty/NaN chart — live-verified by paging the shared month selector back to August 2026 (a real month with zero household expenses) and confirming all 7 cards show the empty message with no exceptions, no infinite axes, and no leftover chart chrome (see Gate 11). All money-axis labels use `Money(...).format(compact: true)` (`₹1.2L`-style) per spec. No chart animation exceeds fl_chart's default 150–250ms. |
| **Gate 11** | **passed** | 2026-09-06 | `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 206 tests (9 new). **Live-verified** on the `kharcha_test` emulator against the real Supabase project and real household data (₹250 Groceries + ₹50 zomato expenses, ₹50,000 income, all in September 2026): every chart's totals reconciled exactly with the Dashboard's for the same period — donut showed Groceries 83%/₹250 and zomato 17%/₹50 matching the Dashboard's top-categories card; payment-method split showed Cash ₹250 + UPI ₹50 summing to the Dashboard's ₹300 spent; monthly trend's September point matched the Dashboard's income/expense figures exactly, with the current month's "Sep" label fixed to always render (see bug below). Paged back to August 2026 (zero data) and confirmed all 7 cards show the "No data for this period" empty state cleanly. Re-verified light **and** dark mode (`adb shell cmd uimode night yes`, full app restart) — every card's background, text, and chart colours (which read from `Theme.of(context).colorScheme`, never a hardcoded light-only colour) stayed legible in both. |
| **Bug found & fixed** | — | 2026-09-06 | The monthly trend chart's bottom-axis label-thinning logic (hide every other month label when 12 months don't fit) hid the label for the **last** index whenever the 12-month window's length was even and the last index was odd — silently dropping the current month's own label off the x-axis (the one point most relevant to "tap a point for the exact value"). Caught live on the `kharcha_test` emulator: the September 2026 trend spike rendered with no "Sep" tick underneath it. Fixed by special-casing `isLast` in `_MonthlyTrendChart`'s `getTitlesWidget` so the final label is never skipped regardless of parity. |
| T-12.1 | done | 2026-09-06 | `buildExpenseCsv`/`buildIncomeCsv` (`data/export/csv_export_builder.dart`, new): pure functions, the exact spec §11.11 header for expenses (`date,time,member,amount_inr,category,payment_method,merchant,note,has_receipt,id`) plus a second, income-shaped header (no `time`/`payment_method`/`merchant`/`has_receipt` — the `incomes` table has none of those). Built on the `csv` package's own `CsvEncoder(addBom: true)` (its v8 API replaced the old `ListToCsvConverter`/`addBom`-by-hand approach the spec's own phrasing implied). `amount_inr` is a plain, non-grouped decimal (`450.00`, never `Money.format()`'s `1,23,456.00`) — a comma inside a numeric CSV cell would force it to be quoted and stop a spreadsheet summing it directly. `ExpenseDao.getFiltered`/`IncomeDao.getFiltered` (new, one-shot `Future<List<...>>` siblings of the existing `watchFiltered`/`watchAll`) supply the unpaginated row set. 11 new tests (`csv_export_builder_test.dart`, plus a `getFiltered` case each in `expense_dao_test.dart` and the new `income_dao_test.dart`). |
| T-12.2 | done | 2026-09-06 | `buildReportPdf` (`data/export/pdf_report_builder.dart`, new): title/household/period/generation-timestamp header, a summary block (total expense/income/net), category and member breakdown tables (`pw.TableHelper.fromTextArray`) with %, and an optional full-transaction-list appendix — no images, per spec. Takes pre-resolved display strings and injected `pw.Font`s rather than touching Drift or `rootBundle` itself, so it stays unit-testable with `pw.Font.helvetica()`. **Real fonts had to be bundled**: the `pdf` package's base14 fonts (Helvetica) have no glyph for ₹ (U+20B9) and either throw a console warning and render nothing or silently drop the character — confirmed live in the first unit-test run ("Unable to find a font to draw ₹"). Fixed by embedding Noto Sans Regular/Bold (OFL-licensed, `assets/fonts/`, ~390 KB each, glyph coverage confirmed via `fontTools` before committing) and loading them via `rootBundle` in `ExportRepository`, not the builder. 2 new tests (`pdf_report_builder_test.dart`, using the Helvetica fallback since a unit test has no asset bundle). |
| T-12.3 | done | 2026-09-06 | `ExportRepository` (`data/repositories/export_repository.dart`, new) orchestrates T-12.1/T-12.2 against the local Drift cache — a pure reader, like `ReportRepository`, that never touches the outbox. `ExportScreen` (built out from its Phase-2 placeholder) per spec §11.11: date range (This month/Last month/All time/Custom, same chip pattern as the Expense List's filter sheet), member/category `FilterChip` scope, CSV/PDF format `SegmentedButton`, "Include income" (second CSV) or "Include full transaction list" (PDF appendix) toggle, delivered via `SharePlus.instance.share`. Filenames match spec exactly (`kharcha_2026-09_expenses.csv`, `kharcha_2026-09_report.pdf`; a custom range that doesn't align to one calendar month falls back to a `yyyy-MM-dd_to_yyyy-MM-dd` token). Reachable from Settings' new "Export" link (same ahead-of-Phase-14 precedent as every other Settings link added in earlier phases). |
| T-12.4 | done | 2026-09-06 | `ExportRepository.exportFullBackupJson()`: every row of every table (households/profiles/categories/payment_methods/expenses/incomes/attachments/budgets/recurring_rules) for the household as one indented JSON file, **including soft-deleted rows** — deliberately higher-fidelity than every other read in this app, since a disaster-recovery snapshot should capture full history, not just the live view. Gated admin-only in the UI (`currentProfileProvider`'s `isAdmin`), matching spec §11.11's "admin only". |
| **Gate 12** | **passed** | 2026-09-06 | `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 217 tests (11 new). **Live-verified** on the `kharcha_test` emulator against the real Supabase project and real household data (₹250 Groceries/Rupesh with a receipt, ₹50 zomato/Vineet, ₹50,000 Salary income, all September 2026). CSV: "Export CSV" with "Include income" on produced a real Android share sheet ("Sharing 2 files") for `kharcha_2026-09_expenses.csv` + `kharcha_2026-09_income.csv`; pulled both off the device (`adb exec-out run-as ... cat`, same technique as every prior gate) and confirmed the BOM (`EF BB BF`), the exact header, and **250.00 + 50.00 = 300.00** reconciling exactly against the Dashboard's "Spent ₹300.00" for the same month (Gate 12's literal acceptance line) — amounts are plain decimals, never Money.format()'s Indian-grouped/comma form. PDF: rendered via `qlmanage` (macOS Quick Look) after pulling the file off the device — a real, valid single-page PDF (11 KB, well under the spec's 500 KB cap) with the ₹ glyph rendering correctly throughout (Total expense ₹300.00, Total income ₹50,000.00, Net ₹49,700.00, and every table cell), the category/member breakdown tables reconciling with the CSV and Dashboard exactly, and the transaction appendix listing both real expenses correctly. Full backup: the admin-only JSON button produced a file with all 9 tables present and row counts matching the real household exactly (1 household, 4 profiles, 21 categories, 6 payment methods, 2 expenses, 1 income, 1 attachment, 0 budgets/recurring rules — the last two are 0 because Gate 8/9's test rows were deleted afterward, as documented there). |
| T-13.1 | done | 2026-09-06 | `flutter_local_notifications` init + Android/iOS permission requests were already brought forward in Phase 8 (T-8.5) for budget alerts — no new init code needed. `timezone`/`tz.setLocalLocation` were likewise already wired in `main()` since T-3.1. `NotificationService` (`core/notifications/notification_service.dart`) extended: `show()` now takes an explicit `channelId`/`channelName`/`channelDescription` per call site (previously hardcoded to `budget_alerts` for every caller) so each of Phase 13's new notification types gets its own Android channel. |
| T-13.2 | done | 2026-09-06 | `NotificationScheduler` (`data/repositories/notification_scheduler.dart`, new), called on every app start and every resume (`app.dart`). Only the **daily reminder** is a true OS-scheduled alarm (`NotificationService.scheduleAt`, `zonedSchedule` + `AndroidScheduleMode.inexactAllowWhileIdle` per spec — avoids the `SCHEDULE_EXACT_ALARM` permission prompt); its "skip if already logged today" decision is pure logic in `nextDailyReminderFireIst` (`core/notifications/daily_reminder_schedule.dart`, new, unit-tested standalone). The other three types (monthly summary, recurring due, sync stuck) need content only knowable at evaluation time ("family spent ₹X", "N items waiting", "stuck for how long") — no future OS alarm can carry that, so each is evaluated live and shown immediately if due, deduplicated via `shared_preferences` keys so a later resume doesn't re-fire the same one same-day/same-month. New `ExpenseDao.hasAnyOn` and `OutboxDao.oldestPendingCreatedAt` DAO methods back the daily-reminder and sync-stuck checks respectively. `NotificationSettings`/`NotificationSettingsKeys` (`core/notifications/notification_settings.dart`, new) hold the 5 on/off switches + reminder time, all defaulting enabled/21:00 per spec's table. |
| T-13.3 | done | 2026-09-06 | `NotificationSettingsController` (Riverpod, `features/notifications/controllers/`) + `NotificationSettingsScreen` (built out from its Phase-2 placeholder): 5 `SwitchListTile`s (daily reminder, budget alerts, monthly summary, recurring due, sync issues — budget *warning* and *exceeded* share one toggle, since both are `BudgetAlertService`'s one threshold-crossing feature, not two independent notification types) plus a reminder-time picker (`showTimePicker`). Every setter persists to `shared_preferences` immediately, updates UI state, then re-runs `NotificationScheduler` so a change takes effect right away rather than waiting for the next app start. `BudgetAlertService.evaluate()` now checks `budgetAlertsEnabled` before firing. Reachable via a new "Notifications" Settings link (same ahead-of-Phase-14 precedent as every other early Settings link). |
| T-13.4 | done | 2026-09-06 | `rootNavigatorKey` (`routing/root_navigator_key.dart`, new — pulled out of `app_router.dart` so it can be shared without a routing→features dependency) lets `app.dart` call `GoRouter.of(context).push(payload)` from outside the widget tree. `NotificationService` gained `onTap` (a broadcast stream fed by `onDidReceiveNotificationResponse`, for a foreground tap) and `consumeLaunchPayload()` (for a cold-start tap, read once via `getNotificationAppLaunchDetails()` after the first frame). Every notification's `payload` is literally its target route path: budget alerts → `AppRoutes.budgetDetailPath(id)` (spec's own literal example — "Tapping a budget alert opens that budget"), daily reminder → `/expense/new`, monthly summary → `/analytics`, recurring due → `/` (the Dashboard's pending-recurring card), sync stuck → `/diagnostics`. |
| **Gate 13** | **partial** | 2026-09-06 | `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 228 tests (15 new: `daily_reminder_schedule_test.dart`'s 6 pure-logic cases, `expense_dao_test.dart`'s new `hasAnyOn` group, and the new `outbox_dao_test.dart`'s `oldestPendingCreatedAt` group). **Live-verified** on the `kharcha_test` emulator against the real Supabase project: set the reminder time via the Notifications screen and confirmed via `adb shell dumpsys alarm` a real `RTC_WAKEUP` alarm registered for `ScheduledNotificationReceiver` at exactly the computed instant each time (including correctly rolling to "tomorrow" once the chosen time had already passed for today, matching `nextDailyReminderFireIst`'s branch for that case) — alarm-history entries additionally confirmed the old alarm is cleanly cancelled on every reschedule. **Budget alert + deep-link fully verified end-to-end**: created a ₹55 household budget against the real ₹300 month-to-date spend, backgrounded/foregrounded the app to trigger `BudgetAlertService.evaluate()`, confirmed the real notification ("Budget alert — Household budget exceeded by ₹245.00") in the shade, and tapping it navigated straight to that exact budget's edit screen — spec's T-13.4 acceptance line, literally. **Not verified**: the daily reminder's alarm actually *firing* and displaying a notification — the scheduled alarm was confirmed registered for the right instant, but never dispatched within its `inexactAllowWhileIdle` batching window on this emulator run (`dumpsys alarm` showed it still pending past its `maxWhenElapsed`, then gone from the pending list with no corresponding log line or posted notification — consistent with the emulator's own alarm-dispatch/App-Standby-Bucket throttling rather than an app defect, but not something this session could force or fully diagnose). Held at **partial** pending either a longer/patient re-run or a real physical-device test — which Gate 13's own acceptance line asks for anyway ("fires on a physical Android device"), never available in this sandboxed session. |
| T-14.0 (prerequisite) | done | 2026-09-06 | `household`/`profile` gained push support ahead of T-14.2/T-14.3, which need to write both: schema v4 → v5 adds `base_updated_at` (TEXT CAS column, same shape as the other 7 push-capable tables) to `Households`/`Profiles`; `HouseholdDao`/`ProfileDao` gained `markSynced`/`markSyncedWithBase`/`updateBaseUpdatedAt`; both mappers' `toCompanion` now stamp `localUpdatedAt` when dirty (the same latent bug Gate 10 fixed elsewhere, never hit here before because neither table had a write path); `HouseholdSyncAdapter`/`ProfileSyncAdapter` flip `supportsPush` to `true` and route through the shared `_pushUpsertWithCas` helper, and their `pullApply` methods gained the D12 dirty-local-wins guard every other entity's pull already had (needed now that either row can actually be dirty). Neither gets a delete path — no `deleted_at` column, no delete RLS policy, and no repository code ever enqueues one. `migration_v2_to_v3_test.dart`/`migration_v3_to_v4_test.dart`'s hand-built fixtures needed `households`/`profiles` tables added (they now sit in the same upgrade chain) — see DECISIONS.md. New `migration_v4_to_v5_test.dart` covers the v5 step itself. |
| T-14.1 | done | 2026-09-06 | `SettingsScreen` rebuilt into the spec §11.13 sections (Profile, Household, Manage, Notifications, Data, About, Diagnostics) — the Manage/Notifications/Export links added ahead of schedule in Phases 5–13 slot into their real home unchanged. |
| T-14.2 | done | 2026-09-06 | `AuthRepository.updatePassword()` (Supabase `updateUser`, no current-password check — it operates on the live session). `ProfileRepository` gained a `triggerSync` callback (it was read-only until T-14.0) plus `updateDisplayName`/`updateColourHex`, both Drift-write-then-outbox-enqueue like every other repository. New `edit_profile_sheet.dart` (name + colour, reusing the new `ColourSwatchPicker`) and `change_password_dialog.dart`. `ColourSwatchPicker` (`core/widgets/`) is `_ColourChoice` pulled out of `category_editor_sheet.dart` once a second consumer needed the identical picker, same dedup precedent as Phase 11's `MonthSelector`/`SectionCard` extraction. |
| T-14.3 | done | 2026-09-06 | `ProfileRepository.setActive()` — callable on any member's id; RLS's `pr_update_self` (`id = auth.uid() or is_admin()`) is the real enforcement. `MembersScreen` built out: role chip + colour dot for every member, an admin gets a `Switch` per row, a member sees the same list read-only. |
| T-14.4 | done | 2026-09-06 | Settings → Data gained "Sync now" (`SyncEngine.sync()`) and "Clear local cache and re-download" (confirm dialog → `AppDatabase.wipeAll()`, the same method sign-out already used, → `sync()`) — unlike sign-out, the Supabase session is untouched. |
| T-14.5 | done | 2026-09-06 | `OutboxDao` gained `watchPending`/`watchFailed` (today only counts existed) and `retry()` (un-parks a `'failed'` entry). `DiagnosticsScreen` built out: sync-queue list, failed-items list with Retry/Discard, `AppLogger.instance.recentEntries` colour-coded by level, and a "Share logs" action (writes `exportAsText()` to a temp file, `SharePlus` — same mechanism `ExportRepository` already uses for CSV/PDF). |
| T-14.6 | done | 2026-09-06 | New `AppRelease` domain model (read live, not mirrored into Drift — a tiny admin-controlled row, not household data) + `AppReleaseRemoteDataSource` (`app_releases`, newest row per platform) + `UpdateCheckRepository`/`UpdateCheckController`: `UpToDate`/`UpdateAvailable`/`Blocked`, throttled to once per 24h via a `shared_preferences` timestamp unless `force`d, a dismissed banner remembered per build number. Checked at app start and every resume (`app.dart`, mirroring `notificationSchedulerProvider`'s wiring) and from a new Settings → About "Check for updates" button. `UpdateBanner` sits atop the Dashboard's card list for `UpdateAvailable`; `Blocked` shows a non-dismissible blocking dialog instead (`app.dart`, via `rootNavigatorKey`). No separate iOS "instructions" page — no iOS build exists yet (blocked since Gate 0) — see DECISIONS.md. |
| T-14.7 (hardening) | done | 2026-09-06 | Widget tests added for all 6 new/changed screens (`test/widget/`, new): `settings_screen_test.dart` (9 cases — section rendering, admin-vs-member household-name gating, Members nav, Sync now, Clear cache, opening the profile sheet/change-password dialog), `members_screen_test.dart` (3), `diagnostics_screen_test.dart` (4), `edit_profile_sheet_test.dart` (4), `change_password_dialog_test.dart` (4), `update_banner_test.dart` (3) — 27 cases total, plus a shared `widget_test_helpers.dart` (mocktail Supabase mocks, a no-op `FakeSyncEngine`, and DB-seeding helpers). Found and fixed 3 real test-authoring bugs in the process, all specific to `testWidgets`'s FakeAsync semantics rather than app code — see DECISIONS.md: (1) household/profile providers are keyed to the fixed `AppConstants.seedHouseholdId`, not an arbitrary test id; (2) `await someDriftStream.watch(...).first` deadlocks forever under `testWidgets` because Drift's stream-invalidation plumbing needs a real `Timer` tick that nothing pumps while a bare `await` blocks — a one-shot `Future` query sidesteps it; (3) Settings' ~20-tile list exceeds `ListView`'s default build/cache extent at the default test viewport, so lower sections are silently absent from `find.text` until the viewport is grown. |
| **Gate 14** | **partial** | 2026-09-06 | `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 273 tests (24 unit-test additions from T-14.0–T-14.6 plus 27 new widget tests from T-14.7 — see their own rows). **Not live-verified**: this session's sandbox has no `adb`/emulator/physical device (`which adb`/`flutter devices` both came back empty) — every prior gate's live pass on `kharcha_test` was unavailable here. Widget tests substantially close the gap the first Gate 14 pass flagged (every screen has now actually been built and interacted with, just not on a device), but held at **partial** pending a live pass covering: editing a member's own name/colour and seeing it sync to a second device; an admin toggling `is_active` and confirming RLS blocks the same action from a member's session; "Clear cache and re-download" actually rebuilding the local DB against the real Supabase project; a deliberately-failed outbox entry showing up in Diagnostics and clearing on Retry; and the update banner/blocking dialog actually appearing against a real `app_releases` row (this also needs `T-16.7`-style rows seeded, which don't exist yet — Phase 16 hasn't shipped a first release row). |
| (housekeeping) | done | 2026-09-06 | Re-ran `dart format` across the whole repo ahead of Phase 15 — the project's formatter had drifted from the pinned SDK's version since early phases (62 files, reflow only, no behavioural change). Needed so Gate 15's "formatter clean" check starts from a clean baseline. |
| T-15.5 | done (written, unverified) | 2026-09-06 | `integration_test/golden_path_test.dart`: the spec §13 golden path verbatim — sign in → add expense → appears in list → appears in dashboard total → toggle offline → add another → back online → both sync. Boots the real `KharchaApp` (real `Supabase.initialize`, real Drift DB, real router) against real test credentials supplied only via `--dart-define` at run time (`TEST_USER_EMAIL`/`TEST_USER_PASSWORD` — never committed, same secrets-handling discipline as `config/dev.json`); the test fails loudly with a clear message if they're absent rather than silently skipping. The "toggle offline"/"back online" step doesn't flip the device's real radio — every prior live-device gate in this project toggled airplane mode externally via `adb shell svc wifi/data disable`, never something an automated `integration_test` script drove itself — so this instead overrides `connectivityServiceProvider` with a fake the test fully controls, while the real `SyncEngine`/`OutboxProcessor`/`PullService` still run unmocked end to end; see docs/DECISIONS.md. **Not executed**: this sandbox has no `adb`/emulator/physical device (same constraint noted at every prior gate needing live verification), so the suite is structurally complete and `flutter analyze --fatal-infos` clean, but has never actually run. Running it is a user action per spec §13's own command (`fvm flutter test integration_test --device-id=<id>`), left for whenever a device is available — see this file's own doc comment for the exact invocation including the two extra `--dart-define`s. |
| T-15.1 | done | 2026-09-06 | Filled the remaining gaps toward spec §13's "≥90% of `core/` and `domain/`" target (excluding generated `.g.dart`/`.freezed.dart`/Drift table-schema files, which no unit test names directly — see T-15.3's note on the same convention). New test files for previously **completely untested** pure-logic units: `notification_settings_test.dart` (copyWith, defaults, `SharedPreferences` load round-trip), `app_logger_test.dart` (every log level, the 500-entry ring buffer's oldest-first eviction, `exportAsText`, `LogEntry.toString` with/without an error), `category_visuals_test.dart` (`colourFromHex`, `iconForKey`'s fallback, every `PayMethodType` icon), `result_test.dart` (`Ok`/`Err`'s `fold`/`map`/`valueOrNull`/`failureOrNull`), `app_config_test.dart` (defaults, and `assertValid()` actually throwing in a `flutter test` run — no `--dart-define` means the Supabase config is genuinely empty), `connectivity_service_test.dart` (online/offline classification via a mocked `Connectivity`, first coverage of this file). Extended existing files: `app_time_test.dart` (+8: `nowIst`, `isFutureMonth`'s 3 branches, `daysRemainingInMonth`'s past/future/current-month branches), `money_test.dart` (+6: comparison operators, `isPositive`/`isNegative`, the sub-1000 and negative compact-format branches, `withSymbol: false`), `outbox_dao_test.dart` (+13: `pendingCount`/`watchPendingCount`/`watchFailedCount`, `hasStuckEntries`'s 3 branches, `dueEntries`'s backoff-still-waiting branch, `remove`/`removePendingUpsert`'s 3 branches), `budget_status_test.dart` (+1: the `spent`/`remaining`/`effectiveBudget` Money getters), and a new `amount_getters_and_filter_test.dart` (`Income`/`Budget`/`RecurringRule.amount`, and `ExpenseFilter.hasActiveFilters`' every clause — previously 0% covered). `core/` line coverage: 76.7%→**90.8%**; `domain/`: 83.5%→**98.9%**. `fvm flutter test` green at 427 (up from 404), `fvm flutter analyze --fatal-infos` clean. Remaining `core/` gap is almost entirely DAO methods with no current call site to exercise them from (e.g. `household_dao`/`profile_dao`'s push-related helpers, mirroring T-15.3's household/profile network-mocking trade-off) — judged not worth chasing to the literal percentage point. |
| T-15.3 | done | 2026-09-06 | Sync-layer branch coverage (spec §13: "100% of `sync/` branches"), which stood at 46% (`entity_sync_adapters.dart` alone at 22%) — every concrete `EntitySyncAdapter` subclass duplicates the same `pullApply`/`pushUpsert` bodies (by design, see the file's own doc comment), so line-coverage tooling needs each of the 9 classes exercised directly, not just `ExpenseSyncAdapter` (already thoroughly covered since Gate 4/10). New `test/unit/sync/entity_sync_adapters_test.dart` (51 tests): for `Category`/`PaymentMethod`/`Income`/`Budget`/`RecurringRule`/`Attachment` (each via a hand-rolled fake remote data source, same precedent as `FakeExpenseRemoteDataSource`) and `Household`/`Profile` (pull-only, no injectable remote — their `pushUpsert` needing a full Postgrest-chain mock was skipped, matching this codebase's established aversion to mocking that chain; see docs/DECISIONS.md), exercises pullApply's tombstone/local-wins/remote-wins-and-log branches, a pushUpsert success path, pushSoftDelete (incl. the `UnsupportedError` for household/profile), markLocalSynced, and selectSince. Extended `outbox_processor_test.dart` (+8 tests: unknown entity, unknown op, an entity outside the known dependency order, income's recurrence-duplicate branch, and the attachment delete/upload storage side-effects via new `MockSupabaseStorageClient`/`MockStorageFileApi` mocktail mocks — first use of storage-chain mocking in this codebase). Extended `sync_engine_test.dart` (+8: the stuck-outbox-entries `SyncError` branch, the catch-block `SyncError` branch, a mid-cycle `stop()` abort, and a new `start()`/`stop()` group exercising the periodic timer and the offline→online connectivity-triggered sync via a real `StreamController`). New `realtime_listener_test.dart` (6 tests, first coverage of this file at all): subscribe-to-3-tables wiring, the 2s debounce (collapsing rapid same-table changes into one pull), re-`start()` tearing down the previous channel, and `stop()`'s cleanup — via `MockRealtimeChannel`/`MockSupabaseClient` with `onPostgresChanges`'s callback captured off the mock invocation. Plus one `backoff.dart` case (the `random ?? Random()` default branch) and a new `sync_state_controller_test.dart` (the generated Riverpod notifier itself, previously only exercised via a plain function double in `sync_engine_test.dart`). `sync/` line coverage (excluding generated `.g.dart`): 46.4% → **87.2%** (`entity_sync_adapters.dart` 22%→80%, `outbox_processor.dart` 73%→~95% by inspection, `sync_engine.dart` 77%→95%, `realtime_listener.dart` 37%→100%). Remaining gap is almost entirely `Household`/`ProfileSyncAdapter.pushUpsert` (network-mocking skipped, as above) and two minor `entity_sync_adapters.dart` edge branches (the "remote row vanished between CAS and fetch" path, and `AttachmentSyncAdapter`'s cached-file-exists-and-delete success path, which needs a registered `PathProviderPlatform` fake). `fvm flutter test` green at 347 (up from 273), `fvm flutter analyze --fatal-infos` clean. |
| T-15.4 | done | 2026-09-06 | Widget tests for key screens, per spec §13's Widget row: `test/widget/sync_banner_test.dart` (new, 6 cases — every `SyncState` branch: idle-renders-nothing, running, offline singular/plural pending count, error). `test/widget/dashboard_screen_test.dart` (new, 2 cases — an empty household renders zeroed totals with no crash/NaN per T-6.5, and a seeded expense's total/category/note/payer render correctly across cards). `test/widget/expense_detail_screen_test.dart` gained a 4th case — the Add-expense form's amount validation (empty/invalid amount → inline "Enter a valid amount." error, no row created) — alongside T-15.2's 3 owner/admin/read-only cases, together covering the spec's "Add-expense form validation" and "empty states"/"offline banner states" bullets. Expense-list grouping/filters remain uncovered by a dedicated widget test (existing coverage there is functional/live-verification only, per Gate 5); noted as the one remaining gap against this table row. `fvm flutter test` green at 360 (up from 351), `fvm flutter analyze --fatal-infos` clean. |
| T-15.6 | done (CI half) | 2026-09-06 | `.github/workflows/ci.yml` and `release.yml` added verbatim from spec §14.1/§14.2, with one deliberate change: `ci.yml`'s push trigger targets `master` (this repo's actual default branch since T-0.4), not the spec's literal `main` — see docs/DECISIONS.md. Verified locally end to end as CI would run it: `flutter pub get`, `dart run build_runner build --delete-conflicting-outputs` (a harmless deprecation warning — the flag is now the default and a no-op — not a failure, exit 0), `dart format --set-exit-if-changed .` clean, `flutter analyze --fatal-infos` clean, `flutter test --coverage` green at 351. Needs no secrets and touches no real Supabase project, matching spec's "tests use a fake Supabase client" note. **Not yet verified**: an actual GitHub Actions run — this repo has a real `origin` remote (`github.com/Vineet2102/Kharcha-App`) but pushing is a user action, not taken this session. `release.yml` is structurally complete but will fail if actually triggered by a `v*` tag today — it depends on Android release-signing config (`android/key.properties`, T-16.1) and the `KEYSTORE_*`/`SUPABASE_*` GitHub secrets, neither of which exist yet (Phase 16 not started) — same "wired now, functional later" precedent as T-4.3's `upload` outbox op. |
| T-15.2 | done | 2026-09-06 | Spec §13's 8 mandatory test cases, audited one by one against the existing suite: cases 1 (float precision), 2 (23:55 IST boundary), 3 (31st-in-Feb recurring), 4 (two-device recurring dedup), 5 (offline-edit conflict/no-loss), 6 (delete-category-in-use blocked), 8 (sign-out wipes DB) were already covered (Phases 2/4/5/9/10); case 1 got an explicit test added (`money_test.dart`: `0.1 + 0.2` → exactly 30 paise) since the existing suite proved the *mechanism* (integer paise) but never the literal case verbatim. Case 7 ("a member's attempt to edit another member's expense is rejected and shows a friendly message") had no direct test at all — the client never lets that save attempt happen (T-5.9's `_ReadOnlyExpenseView`), and the friendly-message half is generic RLS→`ErrorMapper` mapping (`error_mapper_test.dart`), but the two were never proven wired together on the actual screen. New `test/widget/expense_detail_screen_test.dart` (3 cases: a non-owner/non-admin gets the read-only view, the owner gets the editable form, an admin gets the editable form for someone else's expense) — writing the admin case **caught a real bug**, not a test artifact: `ExpenseDetailScreen`/`IncomeDetailScreen`'s `_isAdmin` resolved `currentProfileProvider` via `ref.read` instead of `ref.watch`, so a widget built before that (keepAlive, usually-already-warm) provider had resolved would permanently see `isAdmin: false` — no future rebuild — incorrectly denying an admin editor access. Fixed both getters to `ref.watch`; see docs/DECISIONS.md for the full trace. Also fixed a latent gap in the shared widget-test harness (`widget_test_helpers.dart`'s `stubSignedInAs`): it stubbed `GoTrueClient.currentSession` but not the separate `.currentUser` getter, which any Mock leaves `null` regardless — silently broken for any future test whose screen reads `.currentUser` directly, as this one does. `fvm flutter test` green at 351 (up from 347), `fvm flutter analyze --fatal-infos` clean. |
| T-15.5 (live run) | done | 2026-09-06 | The golden-path integration test actually run on a real Android emulator (`kharcha_test`, launched fresh this session) against the real Supabase project, with a real household member's credentials supplied only via `--dart-define` (never committed) — **passed**, 18s end to end, after fixing several real bugs the first live attempt exposed that no amount of static analysis or mocked widget-testing could have caught: (1) `_addExpense` never selected a category/payment method before Save, tripping the form's own "Pick a category"/"Pick a payment method" validation guards; (2) the Save button sits below the fold on a real device and needs `dragUntilVisible`, not just `pumpAndSettle`; (3) going offline alone does **not** trigger a sync attempt — only an offline→online transition does (`SyncEngine.start()`'s own trigger) — so the "Offline" banner only ever appears once something else (the next expense's own `_triggerSync()`) actually attempts a cycle and finds no network, meaning the assertion order in the test itself was wrong, not the app; (4) `SyncEngine.sync()`'s single-flight lock meant toggling offline and writing again too soon after the first (online) write could silently no-op the second trigger against a still in-flight cycle; (5) the sync banner's own singular/plural copy ("1 change waiting" vs "N changes waiting") meant a substring check for the plural form alone missed the singular case; (6) fixed-duration `pumpAndSettle` calls, appropriate for a mocked widget test, are not reliable against a real network's variable latency — replaced throughout with a polling `_pumpUntil` helper. None of these were app bugs — all five were the test's own assumptions failing to survive contact with a real device and a real backend, which is exactly what this gate exists to catch. `test/widget_test_helpers.dart`/app code are unchanged; only `integration_test/golden_path_test.dart` was hardened. Real household data now carries two extra test expenses (₹111.00, ₹222.00) from the passing run, left for a human to clean up per this file's own documented precedent. |
| **Gate 15** | **passed** | 2026-09-06 | All 6 tasks (T-15.1–T-15.6) done, and Gate 15's full 3-part literal acceptance line now **passed and live-verified**, not merely locally simulated: **`flutter analyze --fatal-infos` clean**. **Formatter clean** (`dart format --set-exit-if-changed .`, 0 changes). **CI green on `master`** — a real GitHub Actions run ([34028401309](https://github.com/Vineet2102/Kharcha-App/actions/runs/34028401309)) completed with `conclusion: success` on every step, confirmed via the Actions API. Beyond the gate's own 3 bullets, the golden-path integration test (T-15.5) — spec §13's own explicit "must pass on a real Android device" requirement — **actually ran and passed** on a real emulator against the real Supabase project (see the row above), closing out the one item every prior live-device gate since Gate 4 had been blocked on for lack of a device in this sandbox. `fvm flutter test` green at 427 (up from 273 at Phase 15's start). Coverage: `core/` 90.8%, `domain/` 98.9% (both ≥ spec's 90% target); `sync/` 87.2% (short of the spec's literal 100%, with every remaining gap identified and its trade-off documented in T-15.3/docs/DECISIONS.md — network-mocking `household`/`profile`'s rarely-written push path was judged not worth the effort). |
| T-M1.1 | done | 2026-09-06 | `0011_multitenant_core.sql` pushed to production. `household_id` nullable confirmed; `household_invites`/`feedback` tables exist; existing 4 profiles still have `household_id` set. See DECISIONS.md. |
| T-M1.2 | done | 2026-09-06 | Verified live via a throwaway signup during T-M1.7's test — new account got `household_id: null`, not the Panicker household. |
| T-M1.3 | done | 2026-09-06 | `0012_household_functions.sql` pushed. `gen_invite_code()` returns 8 restricted-alphabet chars; `seed_household_defaults()` on a scratch household produced exactly 20 categories + 6 payment methods. |
| T-M1.4 | done | 2026-09-06 | `0013_multitenant_rls.sql` pushed. `trg_guard_profile_membership` confirmed installed/enabled; its `household_id_immutable` raise confirmed for real during T-M1.10's MT-7. |
| T-M1.5 | done | 2026-09-06 | `0014_backfill_existing.sql` pushed. Existing household has non-null `created_by`, all members have `joined_at`, exactly 1 active invite exists. |
| T-M1.6 | done | 2026-09-06 | `0015_deletion.sql` pushed. `delete_household()`'s success path exercised live in T-M1.7 and T-M1.10 (MT-16); the `household_not_empty` refusal path implicit in every test never hitting it unexpectedly. |
| T-M1.7 | done | 2026-09-06 | `supabase/functions/delete-account/index.ts` built and deployed (`--use-api`, no Docker). Tested live end-to-end against a throwaway account: signup → household → expense → `delete-account` → `{"ok":true}` → verified auth user, profile, household, and expense all gone, real household's 4 members untouched. See DECISIONS.md for the one deliberate ordering deviation from spec prose (refusal check before destructive steps). |
| T-M1.8 | done | 2026-09-06 | Supabase Auth dashboard configured via browser automation: sign-ups ON, confirm-email already ON, min password length fixed 6→8 (spec-required, was never actually set), redirect URL `io.supabase.kharcha://login-callback/` added. Branded templates step blocked on custom SMTP (dashboard won't allow template edits without it) — see T-M1.9. |
| T-M1.9 | **not done** | 2026-09-06 | Custom SMTP deferred: user has no domain to verify with the chosen provider (Resend), and buying one is a real recurring cost against the spec's ₹0 budget target — left as the user's call. Default Supabase SMTP remains active. **Must be revisited before any real distribution** (spec flags this as "not optional" before sending the APK to anyone). |
| T-M1.10 | done | 2026-09-06 | All 16 §7.2 cross-tenant tests executed live against production via real user JWTs (not SQL-editor impersonation) using 4 throwaway accounts across 2 scratch households, MT-7 ("the single most important test in the document") included. **All 16 passed.** Full per-test results in DECISIONS.md. All test data cleaned up afterward; real household's 4 members confirmed unaffected. |
| **Gate M1** | **passed (T-M1.9 excepted)** | 2026-09-06 | Every T-M1.x task done and verified live against the production project except T-M1.9 (custom SMTP), deliberately deferred pending a domain decision. §7.2's 16-test release gate — the spec's own highest-priority correctness property — passes in full. Nothing blocks starting Phase M2 (client) on the strength of this backend; T-M1.9 must close before real users are onboarded. |
| T-M2.1 | done | 2026-09-06 | `AppConstants.seedHouseholdId` deleted. New `currentHouseholdIdProvider` (`data/repositories/profile_repository.dart`) derives the household id from the cached profile (`ref.watch(currentProfileProvider).value?.householdId`) — null while signed out, before the profile resolves, or (once onboarding exists) for an account with no household yet. Every household-scoped Riverpod stream provider (`household`, `categories`, `paymentMethods`, `householdIncomes`, `budgetsForMonth`, `householdRecurringRules`, `householdProfiles`) now watches it and returns an empty stream on null instead of crashing. Every screen/controller call site (`app.dart`, `expense_list_screen.dart`, `expense_detail_screen.dart`, `income_detail_screen.dart`, `dashboard_screen.dart`, `budget_detail_screen.dart`, `export_screen.dart`, `notification_settings_controller.dart`, `category_editor_sheet.dart`, `payment_method_editor_sheet.dart`, `recurring_detail_screen.dart`, `analytics_screen.dart`) now reads it (`ref.watch`/`ref.read` as appropriate to build vs. action methods), falling back to `''` in read/write paths that need a non-null `String` today (a real household id never matches, so this fails closed) and to a genuine early-return in `app.dart`'s boot/resume hooks and `SyncEngine.sync()` (new `getHouseholdId` constructor callback — a minimal non-crashing guard, not T-M2.7's full no-household UX, which is explicitly out of scope here). `grep -r seedHouseholdId lib/` returns nothing; `flutter analyze --fatal-infos` clean; `fvm flutter test` still green at 427 (`widget_test_helpers.dart` gained a `testHouseholdId` constant so seeded test profiles resolve the same provider automatically — no explicit provider override needed in any of the 5 affected widget test files). See DECISIONS.md for the household_repository.dart/profile_repository.dart/sync_engine.dart circular-import question this raised, and how it resolved. |
| T-M2.2 | done | 2026-09-06 | `HouseholdRemoteDataSource` (`data/remote/household_remote_ds.dart`) gained one thin method per spec §6.9.2 RPC: `createHousehold`, `joinHousehold`, `leaveHousehold`, `setMemberRole`, `setMemberActive`, `removeMember`, `createInvite`, `revokeInvites`, `touchActivity`, `deleteHousehold` — each a single flat `_client.rpc(...)` call, no `.select()`/`.eq()` chaining. `HouseholdRepository` gained a matching `Result<T, Failure>`-returning wrapper per RPC (new `CreateHouseholdOutcome`/`JoinHouseholdOutcome` record typedefs for the two that return more than one value), every one just try/catch → `ErrorMapper.map(error)` — no Drift write, no outbox entry, no `_triggerSync()` call, since a membership change needs a live round trip to mean anything (closer in shape to `AuthRepository` than to the outbox-based CRUD repos). `HouseholdRepository`'s constructor gained the remote data source as a new first parameter (matching `ProfileRepository`'s client-first ordering); the existing `household_repository_test.dart` and its provider were updated accordingly. 40 new unit tests (`household_repository_rpc_test.dart`) against a mocked `HouseholdRemoteDataSource` — one success case per RPC plus one case per named server error from §6.9.2 (`not_admin`, `already_in_household`, `invalid_invite`, `last_admin`, `household_not_empty`, etc. — 24 named-error cases total), asserting `Result.isErr`/`isOk` and the exact RPC arguments rather than a specific `Failure` subtype, since `ErrorMapper` doesn't recognise these v2.0 codes yet (that's T-M2.3's job — see DECISIONS.md for why mocking the thin data-source seam was chosen over mocking `SupabaseClient.rpc()` directly). `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 467 (up from 427). |
| T-M2.3 | done | 2026-09-06 | `ErrorMapper._postgrestFailure` gained a `_namedRpcFailure` lookup, checked first for any `PostgrestException` with `code == 'P0001'` (the SQLSTATE every bare `raise exception '<code>'` in §6.9.2's RPCs defaults to, with `message` set to exactly the code string) — covers all 11 codes in the task list: `already_in_household`/`invalid_invite`/`household_inactive`/`last_admin` use the spec-exact copy from F-15/F-16; `promote_someone_first`/`not_admin`/`not_a_member`/`not_in_household`/`cannot_deactivate_self`/`use_leave_household`/`household_not_empty` have no literal quoted copy anywhere in the spec (only the bare code name, in the RPC bodies and the §7.2 test table), so their wording is this task's own, matching the spec's plain/direct tone (see DECISIONS.md). `not_admin` maps to `PermissionFailure` (a role-permission denial, matching that type's existing meaning); every other code maps to `ValidationFailure` (a business-rule rejection, matching how that type is already used for constraint violations). `household_id_immutable`/`role_immutable` (raised by the `guard_profile_membership` trigger) are deliberately left unmapped — reaching the client would mean a client bug writing `profiles` outside the RPCs, not a normal user-facing outcome. 12 new tests in `error_mapper_test.dart` (one per code plus one confirming an unrecognised P0001 message still falls back to `UnknownFailure`). `fvm flutter analyze --fatal-infos` clean (one `unintended_html_in_doc_comment` info from a literal `<name>` in a doc comment, fixed by escaping the brackets); `fvm flutter test` green at 479 (up from 467). |
| T-M2.4 | done | 2026-09-06 | `AuthRepository.signUp()` (`data/repositories/auth_repository.dart`): calls `GoTrueClient.signUp` with `data: {'display_name': ...}` so `handle_new_user` picks it up; special-cases `NetworkFailure` to the sign-up-specific "Creating an account needs an internet connection." (spec F-15), same pattern as `signIn()`'s existing offline override. `ErrorMapper._authMessage` gained `user_already_exists`/`weak_password`/rate-limit (`over_email_send_rate_limit`/`over_request_rate_limit`/HTTP 429) branches, checked by `AuthException.code` first and by message text as a fallback; the already-registered and rate-limit copy is spec-exact (F-15), weak-password's is this task's own (spec names the case but gives no copy); the function's final fallback string changed from "Could not sign in..." to "Something went wrong..." since it's now shared between sign-in and sign-up. New `SignUpController` (`features/auth/controllers/signup_controller.dart`, mirrors `LoginController`'s shape/`keepAlive` rationale exactly) and `SignUpScreen` (`features/auth/screens/signup_screen.dart`): display name (2–40 chars)/email/password (min 8, helper text)/confirm-password fields, the "Create account" button sitting above a tappable (not-yet-linked — T-M3.1 hasn't published the pages) Terms/Privacy Policy line per spec's explicit "no checkbox" instruction, navigating to the new `/verify-email` route on success. `LoginScreen` gained the spec-mandated "New here? Create an account" link (D2 explicitly supersedes v1.0's instruction to omit one) via `context.push(AppRoutes.signup)`, giving `/signup` a free back-arrow. `app_router.dart`'s `redirect` extended to treat `/signup`/`/verify-email` as signed-out-reachable alongside `/login` — without it a brand-new, not-yet-confirmed account (no session at all, confirm-email being ON per T-M1.8) would bounce straight back to `/login` before ever seeing `/verify-email`; still v1.0's plain binary redirect otherwise, explicitly left for T-M2.8's three-state gate to replace. `/verify-email` itself is a `PlaceholderScreen` for now — real content is T-M2.5. No widget test for `SignUpScreen` (T-M2.13 owns "widget tests for sign-up/onboarding/household screens", and `LoginScreen` itself has never had one either — same precedent). 10 new unit tests (4 in `auth_repository_test.dart`'s new `signUp` group, 6 in `error_mapper_test.dart`). `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 489 (up from 479). |
| T-M2.5 | done (unverified live) | 2026-09-06 | `VerifyEmailScreen` (`features/auth/screens/verify_email_screen.dart`) replaces the T-M2.4 placeholder: shows the address passed via router `extra` from `SignUpScreen` (there is no session to read it from — see T-M2.4), a "Check your spam folder" line, a Resend button with a 60s cooldown (per-second countdown text), and "Wrong address? Start again" (signs out — a no-op if no session ever existed — and routes to `/signup`, since the typo'd address's account can't be edited in place). Polls via a `WidgetsBindingObserver` (`didChangeAppLifecycleState` → resumed) and a 5s `Timer.periodic` while mounted, both calling new `AuthRepository.tryRefreshSession()` (wraps `GoTrueClient.refreshSession()`, swallows every failure — including "no session to refresh yet", the expected steady state — down to a plain `bool`). The screen itself never navigates on confirmation; the already-existing `GoRouterRefreshStream`/`redirect` (T-3.4, extended in T-M2.4) does that the moment a session materialises and fires `onAuthStateChange`, exactly like sign-in's redirect-driven navigation. New `AuthRepository.resendConfirmationEmail()` wraps `GoTrueClient.resend(type: OtpType.signup)`. 5 new unit tests (`auth_repository_test.dart`'s `resendConfirmationEmail`/`tryRefreshSession` groups) — also needed `registerFallbackValue(OtpType.signup)`, whose absence produced a second, unrelated-looking mocktail failure in the next test down (a corrupted argument-matcher stack cascading from the first failure, not a separate bug — see DECISIONS.md). `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 494 (up from 489). **Held at "unverified live"**: whether `signUp()` with confirm-email ON actually leaves anything for `refreshSession()` to refresh (vs. throwing `AuthSessionMissingException` on every poll until the confirmation link itself establishes the session via deep link) can only be settled against the real Supabase project with a real inbox — no device/emulator in this sandbox, same constraint as every prior gate needing live verification. See DECISIONS.md for the full reasoning and what a live pass needs to confirm. |
| T-M2.6 | done | 2026-09-07 | Onboarding gate + Create + Join screens (spec F-15, `lib/features/onboarding/`). `OnboardingGateScreen` (`/onboarding`): two equally-weighted cards (Create/Join) plus the "Not sure? Create one..." helper line, and a sign-out icon in the app bar (via `SignOutController` — spec's three-state gate table lists sign-out as always reachable from this state, but the written screen mock has no visible control for it, so this is this task's own addition rather than pinned copy). `CreateHouseholdScreen` (`/onboarding/create`): one field prefilled `"<display name>'s household"` once the cached profile resolves (a solo user can press Continue immediately); on success the *same* screen flips to an invite-code reveal (large/spaced/`SelectableText`, Copy via `Clipboard.setData`, Share via `SharePlus.instance.share(ShareParams(text: ...))`, "I'll do this later" → `context.go(dashboard)`) rather than navigating to a separate route, since the code only exists once creation has already succeeded and the route map has no further route for it. `JoinHouseholdScreen` (`/onboarding/join`): a single field with a custom `_InviteCodeFormatter` `TextInputFormatter` (uppercases, strips non-alphanumerics so a pasted "abcd efgh" and "abcd-efgh" land the same, inserts the dash after the 4th character, caps at 8 significant characters) — the dash is stripped again before the RPC call, which expects the bare 8-char code; on success shows "You've joined `<name>`" → Continue → dashboard. Both controllers (`CreateHouseholdController`/`JoinHouseholdController`) are thin `keepAlive` Riverpod notifiers wrapping the existing T-M2.2 `HouseholdRepository` methods, same shape/rationale as `SignUpController`. New routes `AppRoutes.onboarding`/`onboardingCreate`/`onboardingJoin` (`routing/routes.dart`), wired into `app_router.dart` alongside T-M2.8's gate below — the two tasks landed together since the screens have nowhere to be reached from without the gate, and the gate has nothing to send a no-household user to without the screens. `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green (see T-M2.8's count below — both tasks' tests ran in the same pass). Comprehensive widget-test coverage for these 3 screens themselves is explicitly T-M2.13's job, not duplicated here — only the router-level "which screen do I land on" behaviour is covered so far (see T-M2.8). |
| T-M2.8 | done | 2026-09-07 | Router three-state gate (spec §10.1, `app_router.dart`), replacing the v1.0 binary signed-in/signed-out `redirect` in place. Resolved in order: no session → `/login` (else `/login`/`/signup`/`/verify-email` allowed); session but `currentUser.emailConfirmedAt == null` → `/verify-email` (else only `/verify-email` allowed); confirmed but `currentHouseholdIdProvider` (T-M2.1, reads the **locally cached** profile — resolves correctly offline) is null → `/onboarding` (else anywhere under `/onboarding/*` allowed, so `/onboarding/create`/`/onboarding/join`'s post-success reveal views aren't yanked away the instant the household id flips non-null mid-flow — only the bare `/onboarding` gate itself is blocked once a household exists, and `/login`/`/signup`/`/verify-email` are also blocked at every state once you're past it); otherwise (confirmed, in a household) every route is reachable except the three signed-out/unconfirmed ones and the bare `/onboarding` gate, which bounce to the dashboard. New `_HouseholdChangeNotifier` (a `ChangeNotifier` wrapping `ref.listen(currentHouseholdIdProvider, ...)`) merged via `Listenable.merge` alongside the existing auth-event `GoRouterRefreshStream` into `refreshListenable` — without it, a household appearing/disappearing through anything other than this app's own screens explicitly navigating afterward (e.g. `SyncEngine`'s `refreshOwnProfile` noticing a member was removed on another device, T-M2.7) would have no navigation attempt or auth event to trigger a redirect re-check at all. **Bug caught and fixed during this task**: an initial draft literally short-circuited `redirect` to `return null` whenever `loc == AppRoutes.splash` (misreading spec's "`/splash` is exempt from all four states" as "never redirect away from splash") — `SplashScreen`'s indeterminate `CircularProgressIndicator` then animates forever with nothing ever navigating off it, which both of `widget_test.dart`'s existing gate tests caught immediately as a `pumpAndSettle` timeout. Fixed by letting splash fall through the normal per-state checks like every other unlisted route (matching pre-T-M2.8 behaviour, which folded splash into the signed-in/signed-out branches the same way) — see the router's own doc comment for the corrected reasoning and the one deliberately-unimplemented spec nuance (holding on `/splash` specifically for "no cached profile and offline", rather than falling through to `/onboarding`). 4 new/updated tests in `test/widget_test.dart`: the pre-existing signed-in case now seeds a household+profile and a confirmed `emailConfirmedAt` (previously a bare session was enough — no longer true, and correctly so), plus 2 new cases (unconfirmed → verify-email, confirmed-no-household → onboarding) — both needed a `_disposeAndFlush(tester)` call the first draft omitted, since `VerifyEmailScreen`'s periodic poll `Timer` (T-M2.5) trips the test framework's "a Timer is still pending after dispose" invariant otherwise. `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 503 (up from 501). **Not implemented**, deliberately scoped out for now: the exact "no cached profile + offline → hold on `/splash` with a retry" carve-out from spec's own prose — that account state currently falls through to `/onboarding` instead of holding; noted in the router's doc comment as a candidate revisit rather than silently dropped. |
| T-M2.9-prereq | done | 2026-09-07 | `profiles.joined_at` wired into the local mirror for the first time — it's existed server-side since T-M1.1's `0011_multitenant_core.sql`, but nothing on the client ever read it (spec F-16's members list needs a "joined date" per member). Drift schema v6 → v7 (`profiles_table.dart`, plain nullable `DateTimeColumn`), `domain/models/profile.dart` gained `joinedAt` (`@JsonKey(name: 'joined_at')`), `profile_mapper.dart`'s `toDomain`/`toCompanion` updated — `toCompanion` writes it unconditionally (`Value(joinedAt)`, never `Value.absent()` on null), since `leave_household`/`remove_member` genuinely reset it to null server-side and a stale non-null local value must not survive that pull. New `test/unit/db/migration_v6_to_v7_test.dart` (hand-built v6 file, same precedent as every prior schema-bump test this phase). `fvm flutter analyze --fatal-infos` clean. |
| T-M2.9 | done | 2026-09-07 | Household & member management screen (spec F-16, `/household`, `lib/features/household/screens/household_management_screen.dart`) — supersedes and **deletes** the Phase-14 `MembersScreen`/`/members` route outright rather than keeping both, since F-16 fully subsumes what it did (role chip, colour dot, active toggle) plus adds everything else this task needs. **Real, currently-live bug fixed in passing**: spec's own D17 callout (KHARCHA_SPEC.md line 1367) warned that v2.0's narrowed `pr_update_self` RLS policy (`id = auth.uid()` only, shipped in T-M1.4's `0013_multitenant_rls.sql`, 2026-09-06) silently breaks any admin write to *another* member's `profiles` row — and the Phase-14 `MembersScreen`'s admin toggle called `ProfileRepository.setActive()` (a plain direct Drift/outbox write against `profiles`), meaning **every admin's deactivate/reactivate toggle for another member has been silently failing against the real Supabase project since T-M1.4 landed**, with no error surfaced (RLS just filters the update to zero matched rows, not a rejection). Fixed by routing entirely through `HouseholdRepository.setMemberActive()` (the RPC, already built in T-M2.2) instead. Screen sections: header (name with an admin-only inline rename via a dialog, member count, "Created `<date>`"); invite section (admin-only, new `activeInviteProvider` family — a live per-open Supabase fetch, deliberately uncached since there's no local mirror for `household_invites` and a stale value would drift the moment another admin device regenerates/revokes it) showing the code as `ABCD-EFGH` with Copy (`Clipboard.setData`)/Share (`SharePlus`, spec-exact template text), the "Expires in N days · used X of Y" meta line, Regenerate (a small dialog picking 1/7/30/90/365 days × 1/5/20/100 uses, confirming "Anyone who has the old code won't be able to use it" first) and Revoke; members list (colour-dot avatar, "(you)" marker, role, joined date via the prerequisite above, Inactive marker) with a `PopupMenuButton` overflow menu on every *other* member's row for an admin (Make admin/member, Deactivate/Reactivate, Remove — Remove confirms with the spec-exact consequence line); Leave household (every member, spec-exact confirmation copy, routes to `/onboarding` on success — not left to the reactive `SyncEngine` wipe alone, since spec's own acceptance line wants it immediate); Delete household (visible only once `isAdmin && members.length == 1`, two-step: an explanation dialog offering "Export a full backup first" — wired directly to the existing `ExportRepository.exportFullBackupJson` + `SharePlus`, spec's own explicit ask — then a second dialog requiring the household's exact name typed before the RPC fires). New `HouseholdRemoteDataSource.fetchActiveInvite`/`HouseholdRepository.fetchActiveInvite` (a plain `household_invites` select scoped by RLS's `inv_select`) and a reshaped `HouseholdRepository.createInvite` (now takes `householdId`, returns the new `HouseholdInvite` domain model via one extra read instead of the bare code string the RPC itself returns, so Regenerate can show the same meta line without a second code path) — both new methods covered by 5 new/updated cases in `household_repository_rpc_test.dart`. Settings' "Household" section collapsed from two tiles (an inline-rename "Household name" tile + a separate "Members" tile) to one "Household" tile pointing at `/household` — rename now lives in the destination screen's own header per spec's literal placement. `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 502. Widget-test coverage for this screen's own interactions (invite actions, member overflow menu, leave/delete flows) is T-M2.13's job, not duplicated here — this pass only re-pointed/updated the pre-existing Settings-level navigation test. **Not live-verified** (no device in this sandbox): the invite section's live Supabase read, the corrected `setMemberActive` RPC path, and the leave/delete flows all need a real project to confirm end-to-end — T-M2.11/T-M2.12 own that. |
| T-M2.10 | done | 2026-09-07 | Solo-household polish (spec D20/C9). Dashboard's per-member breakdown card (`_MemberBreakdownCard`) now hidden entirely when `householdProfilesProvider` has 1 or fewer members — a solo user comparing themselves to themselves is noise, per D20's own wording, matching spec §11.4's card-3 description literally ("Hidden entirely when the household has exactly one member"). **Copy audit** (`grep -rni family lib/`) found two real user-facing violations of D20's "copy never says 'family'": the Create-household card's subtitle (`onboarding_gate_screen.dart`, T-M2.6) quoted F-15's own v1.0-era literal text verbatim ("...your family's spending...") without reconciling it against D20, added later in the same spec revision — fixed to "...your household's spending..." (documented inline as a deliberate spec-conflict resolution, D20 overriding the older literal quote); and the monthly-summary notification body (`notification_scheduler.dart`, T-13.2, also predating D20) said "family spent ₹X" — fixed to "household spent ₹X" (spec's own §11.12 table still shows the old wording verbatim; not updated there since docs/SPEC.md is a frozen historical record of what was built when, not living UI copy). Every other hit was either a doc comment, a Riverpod-generated `...Family` provider class (an unrelated codegen naming convention), or seed/test data (`Panicker Family`) — none are user-facing app copy. **Empty-state text**: Expense List's `_EmptyState` said "No expenses match these filters." unconditionally, reading oddly for a genuinely empty household with no filters applied at all — now branches on `hasFilters`, showing "No expenses yet — tap + to add your first one." when there are none. `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 502 (no count change — no new tests added; the existing single-profile dashboard fixture in `dashboard_screen_test.dart` already exercises the newly-hidden card's absence without needing a new assertion, and no test pinned the old unconditional empty-state string). Not touched, deliberately out of scope: `README.md`/`pubspec.yaml`'s own "family expense tracker" project description — that's project branding/metadata the user wrote, not in-app UI copy, which is what D20 governs. |
| T-M2.13 (widget tests) | done | 2026-09-07 | Widget-test coverage for the sign-up/onboarding/household screens T-M2.6–T-M2.9 shipped without any (deferred at the time, per each task's own PROGRESS note). New `MockHouseholdRemoteDataSource` added to `widget_test_helpers.dart` (mirrors `household_repository_rpc_test.dart`'s convention — mock the thin RPC seam, exercise the real `HouseholdRepository` on top of it). Four new test files: `onboarding_gate_screen_test.dart` (both cards render with no "family" wording per D20, each navigates correctly, the sign-out confirm dialog signs out and wipes local data), `create_household_screen_test.dart` (prefills from the cached display name, success reveals the formatted invite code and "I'll do this later" reaches the dashboard, empty-name validation, a server error surfaces inline), `join_household_screen_test.dart` (the formatter's uppercase/strip/dash/8-char-cap behaviour, the Join button's enabled state, a successful join strips the dash before the RPC call and "You've joined `<name>`" → Continue reaches the dashboard, `invalid_invite` maps to its exact plain-language copy), and `household_management_screen_test.dart` (12 cases — the invite section is admin-only and renders its formatted code/expiry/use-cap meta line, the members list's overflow menu is admin-only and absent from the admin's own row, Deactivate calls the RPC directly rather than repeating Phase 14's now-fixed direct-write bug, Remove/Leave both confirm before calling their RPC with spec-exact copy, Delete is gated to `isAdmin && members.length == 1`, and the two-step typed-name confirmation only fires the RPC on an exact match). **Two real test-authoring bugs caught and fixed while writing these** (both `testWidgets`/formatter interaction quirks, not app bugs — same category as T-14.7's precedent): (1) `JoinHouseholdScreen`'s code field's `hintText` is itself `'ABCD-EFGH'`, and Flutter keeps a `TextField`'s hint widget in the tree (opacity-animated, not removed) once real text is entered — asserting `find.text('ABCD-EFGH')` after typing that exact code found two matches; fixed by testing a different code in that assertion. (2) Calling `tester.tap` on a button immediately after `tester.enterText` without an intervening `pump()` taps the button in its *pre-input* enabled state, since the `setState` the input triggers hasn't been applied to the tree yet — two "Join" taps were silently no-ops against a still-disabled button; fixed by adding the missing `pump()`. `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 524 (up from 502). Remaining T-M2.13 items — unit tests for the sync engine's household-change wipe (already covered, see T-M2.7's own new test group) and extending the golden-path integration test to start from sign-up/household creation — tracked separately below. |
| T-M2.13 (integration test) | done (structurally — unverified live) | 2026-09-07 | `integration_test/golden_path_test.dart` gained a second `testWidgets` case: sign up → verify email → create a household → add an expense, per this task's own acceptance line ("the golden path passes on a real device starting from a brand-new account"). Bootstrap (tz data, `Supabase.initialize`, `NotificationService.instance.init()`) was pulled out of the original test's body into a shared `ensureBootstrapped()` guarded by a `bootstrapped` flag, since `Supabase.initialize` throws if called twice in the same process and both tests now share one `main()`. The new case needs a fresh `--dart-define=TEST_SIGNUP_EMAIL` (a real, never-before-used, deliverable address — sign-up itself fails otherwise) and, structurally unavoidable, **a human present to click the real confirmation email** during its 5-minute polling window: T-M1.8 requires email confirmation, and there is no way to automate clicking a link in a real inbox from an automated test — the exact same wall T-M2.5 hit and left "unverified live" for the same reason. `fvm flutter analyze --fatal-infos` clean (this file is analyzed as part of the package even though `flutter test` doesn't run it). **Not executed** — no device/emulator and no disposable real inbox available in this sandbox, so this is "wired now, functional later" per this project's own established precedent (T-4.3's `upload` outbox op, T-15.6's `release.yml`) rather than a claim that it has passed. Closes out T-M2.13's stated scope in full: widget tests (above), sync-engine household-change unit tests (already covered by T-M2.7's own new test group, not duplicated), and this integration-test extension. |
| T-M2.11 | **attempted live — real bug found, not fixed** | 2026-09-07 | Finally ran Gate 4's literal two-device scenario, for the first time since the 2026-09-05 CAS fix, on this laptop's two real Android emulators (`kharcha_test`/`kharcha_test_2`) against the real Supabase project, using two of the household's actual real member accounts (Vineet/admin, Rupesh/member) rather than throwaway ones. Both devices taken offline; Vineet edited the household's real ₹300 Groceries expense to ₹111 at 07:13 IST; ~3 minutes later Rupesh edited the *same* row to ₹222 at 07:16:21 IST (the genuinely newer edit); Vineet reconnected first and pushed cleanly; Rupesh reconnected ~1 minute later and hit a CAS conflict that correctly triggered the retry path — but resolved in **Vineet's** favour, discarding Rupesh's objectively newer edit. Root cause: `touch_updated_at()`'s `GREATEST(now(), incoming)` stamps a reconnecting device's push with server-receipt time whenever that's later than the edit's own claimed timestamp (true after any real offline stretch), so Vineet's 3-minutes-old edit got stamped ~07:16:42 (its actual push time) — 21 seconds *newer* than Rupesh's accurate, untouched 07:16:21 `local_updated_at`. Net result: conflict resolution is sensitive to each device's own reconnect delay, not truly "newest-edit-wins regardless of push order" as the 2026-09-05 fix intended — see docs/DECISIONS.md for the full trace and fix options considered. The *other* half of Gate 4's requirement (no fork/duplicate, both devices converge to one value) still held — both devices showed ₹111 afterward, confirmed live. **Not fixed this session** — needs a real design decision on the trigger/comparison logic, not a quick patch. Gate 4 stays open (not `passed`); this is a genuine, previously-undiscovered correctness gap, not a live-verification formality. The production household's real "Groceries" expense (originally ₹300) is left at ₹111 from this test, per this project's own established precedent of leaving live-test artifacts for a human to reconcile afterward (matches Gate 15's own ₹111/₹222 test expenses). |
| T-M2.12 | **not done — blocked** | — | Spec's own task: live-verify the Settings/admin/diagnostics screens M2 rewrote (Household management, onboarding, the corrected `setMemberActive` RPC path) plus the ones it didn't, to finally flip Gate 14 from `partial` to `passed`. Same blocker as T-M2.11 and every gate needing live verification this build — no device/emulator in this sandbox. Widget-test coverage for the rewritten screens (T-M2.13) substantially narrows the gap the same way T-14.7's widget tests did for the original Gate 14 attempt, but a real device pass is what the gate itself requires. Gate 14 stays `partial`. |
| T-M2.14 | done | 2026-09-07 | Drift schema v5 → v6: `sync_meta` gains a nullable `household_id` TEXT column (`core/db/tables/sync_meta_table.dart`), stamped on every pull (`PullService._pullEntity`) and read back via the new `SyncMetaDao.anyStoredHouseholdId()` T-M2.7 depends on. The migration itself (`app_database.dart`) can't backfill a real value — no pre-v6 install ever recorded which household its cursors belonged to — so it instead `DELETE FROM sync_meta` after adding the column, resetting every cursor to force exactly one full pull on the next sync (which then stamps the real household id itself); the household's existing `expenses`/etc. rows are untouched, only the cursors deciding "what's new since last time" are cleared. New `test/unit/db/migration_v5_to_v6_test.dart` against a hand-built v5 sqlite file (following the T-14.0-era precedent of `migration_v2_to_v3_test.dart`/`migration_v3_to_v4_test.dart`/`migration_v4_to_v5_test.dart`), asserting both properties directly. Doing this **before** T-M2.7 exposed that all three older migration tests' hand-built fixtures had never included a `sync_meta` table at all — harmless while schemaVersion topped out at 5, since none of those tests' `onUpgrade` chains ever reached a step touching it, but opening any of them now runs the full chain up to v6 in one go, so this task added a bare `sync_meta` table to each of their fixtures (same category of fixup Phase 14 itself needed for `households`/`profiles`, per that phase's own PROGRESS entries). `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green (folded into the 501 total reported under T-M2.7, since both landed in the same pass). |
| **Gate M2** | **partial** | 2026-09-07 | T-M2.1–T-M2.10, T-M2.13 (widget/unit tests), and T-M2.14 are done and verified by static analysis/the full test suite (`fvm flutter analyze --fatal-infos` clean, `fvm flutter test` green at 524). Two real Android emulators became available on this laptop mid-phase, enabling T-M2.11's live two-device attempt (see its own row) — which surfaced a genuine, previously-undiscovered correctness gap in cross-device conflict resolution (reconnect-delay sensitivity via `touch_updated_at()`), not merely a lack-of-hardware blocker. That finding means Gate 4 cannot flip to `passed` on the strength of this session regardless of further device availability — it needs a design fix first. T-M2.12 (Gate 14 live screen check) and Gate M2's own full literal acceptance line (a brand-new account signing up, confirming via a real inbox, and joining by code) remain not attempted — the emulators are available, but T-M2.12 was deprioritised in favour of investigating the T-M2.11 finding, and Gate M2's sign-up flow needs a live human-in-the-loop email step. |
| T-M2.7 | done (unverified live) | 2026-09-07 | Household-aware `SyncEngine` (spec §9.6 rules 2–3), built on T-M2.14's `sync_meta.household_id` column above. `SyncEngine.sync()` now: (1) always calls a new `refreshOwnProfile` callback (→ `ProfileRepository.refresh(userId)`) before trusting `getHouseholdId()` for the rest of the cycle — the spec's stated exception to "no household → no sync", so the app notices a household appearing (joined elsewhere) or disappearing (removed elsewhere) without waiting for a household-scoped pull that, by definition, can no longer see this user; (2) compares `SyncMetaDao.anyStoredHouseholdId()` against the freshly-resolved current household id, *before* `pushOutbox()` runs — a stored value that's non-null and different (covering household→different-household, and household→null on a leave/removal alike) means this device's rows no longer belong to it, so a new `wipeHouseholdData` callback (`AppDatabase.wipeAll()` + the receipt cache) runs first, and no stale outbox entry for the old household is ever pushed. A fresh install (`anyStoredHouseholdId()` returns null — nothing pulled yet) never wipes. Extracted `SignOutController`'s sign-out-only receipt-cache-clearing helper into a shared top-level `clearReceiptCache()` (`data/local/receipt_cache.dart`) so it and `SyncEngine`'s provider wiring both call the one implementation. `HouseholdRepository.createHousehold`/`joinHousehold`/`leaveHousehold`/`deleteHousehold` now call `_triggerSync()` on success (previously only `updateName` did) — the RPC itself changes the caller's `household_id` server-side, and this fires the reactive check above immediately (full pull after create/join; wipe-and-return-to-idle after leave/delete) instead of waiting up to 5 minutes for the periodic timer. 11 new tests in `sync_engine_test.dart`'s new "household change" group (mismatch wipes before push, no-stored-id doesn't wipe, same-id doesn't wipe, current-id-null-from-a-leave still wipes, profile always refreshed even with no household, offline skips both the refresh and the household check) plus 4 new `expect(triggerSyncCalls, 1)` assertions added to `household_repository_rpc_test.dart`'s existing create/join/leave/delete success cases. `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 501 (up from 494). **Held at "unverified live"**: the actual multi-device join/leave/wipe sequence (T-M2.7's own acceptance line: "pulling the file off the device" after joining, leaving, and joining a different household) needs two physical devices, not available in this sandbox — same constraint as every gate needing live verification this build. |
| Gate 4 fix | done | 2026-09-07 | Fixes T-M2.11's reconnect-delay bug. New `client_edited_at` column (`supabase/migrations/0016_client_edited_at.sql`) on all 9 syncable tables, added purely to carry a client's genuine edit timestamp through `touch_updated_at()` untouched — `updated_at` itself is left exactly as before (still `GREATEST(now(), incoming)`, still what `selectSince()`'s pull cursor pages on), since clamping it against the row's own previous value instead of `now()` was considered and rejected: that would reopen a lost-update bug on the pull side (an offline device's edit landing behind another device's already-advanced cursor would never be seen by that device again). `entity_sync_adapters.dart`'s shared `_updatedAtOf()` — the one helper both the push-CAS conflict check and every `pullApply()`'s D12 check route through — now reads `client_edited_at` first, falling back to `updated_at` for a pre-migration row; `_pushUpsertWithCas()` stamps it onto every outgoing payload from the payload's own `updated_at` (the domain model's client-side edit time, already accurate — the value the old comparison was trying and failing to use). `0016` also re-declares `0012`'s 6 membership RPCs (`create_household`/`join_household`/`leave_household`/`set_member_role`/`set_member_active`/`remove_member`) to stamp `client_edited_at = now()` alongside their existing `updated_at = now()` — those write `profiles` directly outside the client's push path, so nothing else would ever stamp it for them, and a stale value there could make an older unpushed local profile edit wrongly compare as newer than one of these authoritative changes. New regression test in `push_conflict_resolution_test.dart` reproduces T-M2.11's exact live timestamps (Vineet's true 07:13 IST edit, server-inflated to 07:16:42; Rupesh's true, genuinely newer 07:16:21 edit) — confirmed to fail against the pre-fix code (verified by temporarily reverting the adapter change) and pass against the fix. `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 525 (up from 524). See docs/DECISIONS.md for the full reasoning, including why the two options floated in T-M2.11's own entry were both rejected in favour of this one. Migration pushed to production by the user and **live-verified**: T-M2.11's exact two-device scenario re-run on the same two real emulators against the same real household (Vineet edited the real Groceries expense to ₹150 offline, Rupesh edited the same row to ₹275 offline ~4m47s later — the genuinely newer edit), Vineet's device reconnected first and pushed cleanly, Rupesh's device reconnected ~1 minute later and hit the CAS conflict — which this time correctly resolved in **Rupesh's favour** (the genuinely newer edit), confirmed via both devices converging on ₹275.00 with no fork. See docs/DECISIONS.md for the full trace. |
| **Gate 4** | **passed** | 2026-09-07 | T-M2.11's live two-device re-run (above) confirms cross-device conflict resolution is genuinely newest-edit-wins, independent of reconnect order — the exact scenario that failed on 2026-09-07 (Vineet's older, receipt-time-inflated edit winning over Rupesh's real newer one) now resolves correctly in the other direction. No fork/duplicate on either device, both converge to one identical value (₹275.00), matching Gate 4's full acceptance line. The production household's real "Groceries" expense is left at ₹275 from this test, per this project's own established precedent of leaving live-test artifacts for a human to reconcile afterward. |
| **Bug found & fixed** | — | 2026-09-07 | Live T-M2.12 verification reproduced the pre-existing, previously-unfixed Gate 10 finding ("Tapping a member's row in the Dashboard's Per member card crashed... pre-existing, not investigated further"): `_ExpenseListScreenState.initState` (`features/expenses/screens/expense_list_screen.dart`) called `ExpenseListPresetFilterController.clear()` synchronously when a Dashboard-handoff preset filter was present — a Riverpod provider mutation during `initState`'s build phase, which Riverpod forbids and threw `Bad state: Tried to modify a provider while the widget tree was building`, replacing the whole Expenses tab with the red error widget. Fixed by deferring the `clear()` call via `WidgetsBinding.instance.addPostFrameCallback`, matching Riverpod's own suggested fix. Live-verified on `kharcha_test`: tapping "Rupesh" on the Dashboard's Per-member card now cleanly navigates to a correctly-filtered Expense List with no crash; navigating away and back via the bottom tabs preserves the filtered view correctly (`IndexedStack` keeps the screen's `State` alive, so `initState` — and the now-deferred `clear()` — only ever runs once per branch mount, as intended). |
| T-M2.12 | **partial — live-verified** | 2026-09-07 | Live-verified against the real Supabase project on `kharcha_test` (Vineet/admin): Household management screen (T-M2.9) — header, live invite section (`7MWT-YDX4`, "Expires in 364 days · used 0 of 50", Copy/Share/Regenerate/Revoke all rendering from a real fetch), member list with role/joined-date, admin-only overflow menu (Make admin/Deactivate/Remove), and the inline household-rename dialog (opened and cancelled without committing, to avoid touching production data) — all correct. **The corrected `setMemberActive` RPC path (T-M2.9's headline fix) confirmed genuinely working end-to-end for the first time**: Deactivating Rupesh from Vineet's device, then "Sync now", showed Rupesh correctly marked "Inactive" (re-sorted to the bottom of the list); Reactivate correctly restored him — both via the RPC, not the old silently-failing direct write. Diagnostics screen (sync queue / failed items / recent logs) renders correctly against real data. **Not covered this pass**: Categories/Payment methods/Income/Budgets/Recurring/Notifications/Export/About screens, the Delete-household flow, and Remove-member — deprioritised once the T-M2.7 two-device test (below) surfaced two higher-severity findings that consumed the rest of the session. Gate 14 stays `partial`. |
| T-M2.7 | **attempted live — two real bugs found, not fixed** | 2026-09-07 | Ran the literal two-device leave/rejoin scenario for the first time, on the same two real emulators against the real Supabase project, using two of the household's actual real member accounts (Vineet/admin, Rupesh/member). Rupesh backed "Leave household" (spec-exact confirmation dialog) — the RPC call itself succeeded silently (client received no exception, navigated toward `/onboarding`) but the router immediately bounced back to the Dashboard, which kept showing full household data. Sync now / waiting / app resume on both Rupesh's and Vineet's devices never converged: Rupesh's own `kharcha.sqlite` (pulled via `adb exec-out run-as ... cat`) still showed his `profiles` row at the original `household_id`, `is_dirty=0` — i.e. never updated at all, on either side. **Root cause 1**: `domain/models/profile.dart`'s `householdId` field is `required String` (non-nullable) in both the Freezed model and the mirroring Drift column (`profiles_table.dart`'s `householdId => text()()`, also non-nullable) — but Postgres's `profiles.household_id` has been nullable since T-M1.1, and `leave_household()` genuinely sets it to `null` server-side. `ProfileRepository.refresh(userId)`'s `domain.Profile.fromJson(json)` therefore throws `type 'Null' is not a subtype of type 'String' in type cast` the moment it tries to deserialize a household-less member's own row — confirmed via 3 matching entries in the Diagnostics screen's Recent logs ("Profile refresh failed for `<rupesh-id>`"). The exception is caught and only logged (by design, per `refresh()`'s own doc comment — "failures are logged and swallowed, the local cache stays the source of truth"), so the local profile is silently never updated, `SyncEngine.sync()`'s stored-vs-current household-id comparison (T-M2.7's own mismatch-detection logic) never sees a change, and `wipeHouseholdData()` never runs. **This means leaving a household can never actually converge locally for the leaving member, on any build with this schema** — not a live-verification gap but a genuine, previously-undiscovered correctness bug blocking T-M2.7's entire premise for the "leave" direction. (The same absence of any delete/tombstone path for `profiles`, already called out in T-14.0's own PROGRESS note, means *other* members' devices have no way to learn of the departure via incremental pull either — confirmed live: Vineet's device kept showing Rupesh as a normal member, unprompted, until a full resync after Rupesh had already rejoined.) Attempted recovery via sign-out (wipes local cache) → sign back in as Rupesh (credentials entered by the user, not this session) → correctly landed on the onboarding gate (no cached profile at all, sidestepping the crash) → rejoined via the still-valid invite code (`7MWT-YDX4`) → "You've joined Panicker Family" confirmed the RPC succeeded — but tapping Continue again bounced back to the onboarding gate, and the local DB (checked directly via the pulled sqlite file) stayed completely empty (0 rows in every table) no matter how long we waited, forced "Sync now", or backgrounded/foregrounded the app. **Root cause 2, distinct**: `SyncEngine.start()` is called exactly once in the whole codebase (`app.dart`'s `initState`, at process boot). `SignOutController` calls `.stop()` on sign-out (setting `_stopped = true`, cancelling the periodic timer and connectivity subscription); the only sign-in-triggered hook (`app.dart`'s `ref.listen(currentSessionProvider, ...)`, "trigger 1") calls `.sync()` only — and `sync()`'s very first line is `if (_syncing || _stopped) return;`, so once `_stopped` is set it silently no-ops forever, for every future call (periodic, connectivity, manual "Sync now", `_triggerSync()` after any RPC), until the process is fully killed and relaunched. This directly contradicts this project's own documented design intent for T-4.5 ("`start()`/`stop()` are idempotent and resumable... next sign-in re-arms") — the re-arm was never actually wired, only `.sync()` was. **Confirmed as the cause, not a guess**: force-stopping and relaunching the app (`adb shell am force-stop` + fresh `flutter run`, re-running `initState`'s `engine.start()`) immediately fixed it — Rupesh landed on the Dashboard, and "Sync now" pulled the full real household data (₹275/₹50 expenses, ₹50,000 income) cleanly. **Production state fully restored**: Rupesh is genuinely back in Panicker Family, confirmed independently on both devices with real synced data — no bad state left behind, unlike this project's usual precedent of leaving live-test artifacts (there was nothing reversible-but-left to leave here; the recovery *was* the fix). **Neither bug fixed this session** — the `householdId` nullability fix touches the Drift schema (a new migration), the Freezed domain model, and every call site that assumes a non-null `String` (`currentHouseholdIdProvider` and its many consumers); the `SyncEngine` re-arm fix is small and contained (call `.start()`, not just `.sync()`, on the sign-in trigger) but was left alongside it per the user's explicit instruction to record findings rather than fix this session. T-M2.7 stays "unverified live" and now additionally blocked; Gate M2 cannot close until both are addressed. |
| **Gate M2** | **partial — blocked by 2 new correctness bugs** | 2026-09-07 | T-M2.12's live pass (above) substantially confirms Gate 14's Household-management/Diagnostics slice and the T-M2.9 `setMemberActive` fix, but does not cover every screen, so Gate 14 stays `partial`. T-M2.7's live attempt (above) — the session's main goal — surfaced two genuine, previously-undiscovered bugs rather than merely confirming the already-built mechanism: (1) `profiles.householdId`'s non-nullable Drift/domain typing crashes `ProfileRepository.refresh()` for any household-less member, permanently preventing the leave-household local wipe from ever converging; (2) `SyncEngine` never re-arms `start()` after a sign-out→sign-in cycle within the same process, silently disabling all sync thereafter — a regression against this project's own documented Gate-4-era design intent, and not specific to Phase M2's new code paths. Gate M2 cannot flip to `passed` until both are fixed and the two-device leave/rejoin scenario is re-run clean. The brand-new-signup path (a real inbox + a human clicking the confirmation link) remains entirely unattempted this session. |
| Bug fix (T-M2.7 blockers) | done (unverified live) | 2026-09-07 | Both bugs above fixed in code, ahead of a friend's planned Gate M2 test pass. (1) `Profile.householdId` (`domain/models/profile.dart`) and the Drift column (`profiles_table.dart`) changed to nullable, matching Postgres; schema bumped v7 → v8 via drift's `alterTable(TableMigration(profiles))` (SQLite can't drop a NOT NULL constraint via a plain `addColumn`) — new `migration_v7_to_v8_test.dart`, plus `migration_v2_to_v3_test.dart`/`migration_v3_to_v4_test.dart`'s minimal `profiles` fixtures widened to the full column set the recreate step now needs (same class of fixup as T-M2.14's `sync_meta` addition). (2) `app.dart`'s sign-in `ref.listen` now calls `engine.start()` before `engine.sync()`, re-arming `_stopped` on every sign-in (idempotent, matches `initState()`'s own boot pattern) — new regression test in `sync_engine_test.dart`'s `start()/stop()` group. See docs/DECISIONS.md for the full trace, including why `currentHouseholdIdProvider` needed no change. `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 527 (up from 525). **Not live-verified**: needs the same two-device leave/rejoin re-run T-M2.7 already attempted once, against a real Supabase project — no device/emulator in this sandbox. Gate M2 stays `partial` until that passes. |
| T-M2.7 (live re-verify) | **done — both bugs confirmed fixed** | 2026-09-07 | Re-ran the exact two-device leave/rejoin scenario from T-M2.11/T-M2.7 against the real Supabase project, on the same two real emulators and real member accounts (Vineet/admin, Rupesh/member). Most of the session was consumed by an unrelated environment problem — persistent emulator-side DNS failures on both devices, eventually traced to a combination of corrupted local device/resolver state (fixed by a `-wipe-data` factory reset of Rupesh's emulator) and the Android emulator's virtual Wi-Fi radio dropping its own simulated signal (`wpa_supplicant: CTRL-EVENT-BEACON-LOSS` in `adb logcat`; fixed by disabling Wi-Fi and forcing cellular-only) — see docs/DECISIONS.md for the full diagnostic trace, including confirming Supabase itself was healthy throughout via direct `curl` calls, and the reminder that a release APK's handled failures never reach `adb logcat` (had to run the debug build to see real exceptions live). Once the network was stable: **bug 1 confirmed fixed** — Rupesh's "Leave household" now navigates cleanly to the onboarding gate and stays there, no crash, no bounce back to Dashboard. **Bug 2 confirmed fixed** — after a fresh sign-in (forced by the device wipe), sync worked correctly on the very next "Sync now" with no app restart needed. **One pre-existing, already-documented gap re-confirmed, not a new bug**: Vineet's device, after a clean zero-error sync, still shows Rupesh as a member — there's no `profiles` tombstone, and RLS simply hides a departed member's row from the remaining members' queries once `household_id` goes null, so there's no negative signal for their devices to act on. Left unfixed per the user's explicit instruction to record findings only this session. |
| **Gate M2** | **partial — both target bugs now live-verified; other items still open** | 2026-09-07 | The specific blocker on Gate M2 (T-M2.7's two-device leave/rejoin scenario failing) is now cleared — both the nullable-`householdId` crash and the `SyncEngine` re-arm bug are confirmed fixed on a real device against the real Supabase project. Gate M2 stays `partial` because other previously-identified items remain open and untouched by this session: T-M2.12's full Gate 14 screen coverage (Categories/Payment methods/Income/Budgets/Recurring/Notifications/Export/About/Delete-household/Remove-member never live-tested), the brand-new-signup path (needs a real inbox and a human clicking a confirmation link), and the newly-re-confirmed profiles-tombstone gap (other members' devices don't learn of a departure via incremental sync) — a real, separate, already-documented limitation, not a regression from today's fixes. |
| **Bug found, root-caused, not fixed** | — | 2026-09-07 | Followed up on the profiles-tombstone gap with both emulators still live. Root cause confirmed precisely: `TableRemoteDataSource.selectSince()`'s shared `.eq('household_id', householdId)` pull filter structurally excludes a departed member's row from any future pull for their former housemates' devices — not a stale-cursor bug, a cursor reset doesn't help, since Postgres RLS + this filter together mean the row simply stops being addressable once `household_id` goes null (confirmed live via a direct authenticated `curl` against `/rest/v1/profiles` showing Rupesh's real `household_id: null`). Confirmed the data model itself is otherwise sound: a genuinely fresh full pull (via "Clear local cache and re-download") correctly did **not** bring Rupesh back, while Tanish/Trupti/real expenses all repopulated fine — the only broken thing is a pre-existing local cache with no invalidation path for one specific stale row. A real fix needs a tombstone/removed-member signal or a periodic re-verify of cached member rows; not attempted, per the user's explicit instruction to document only. See docs/DECISIONS.md for the full trace. |
| **Bug found, not fixed** | — | 2026-09-07 | Found while testing the above: "Clear local cache and re-download" (Settings → Data) doesn't complete a full re-sync on its first attempt. `SettingsScreen._clearCacheAndResync` awaits `wipeAll()` then `SyncEngine.sync()` — but `sync()`'s `getHouseholdId()` read (a Riverpod provider derived from a Drift-stream-backed `currentProfileProvider`) can race the just-awaited `refreshOwnProfile()` write, seeing a stale/null household id and silently skipping the entire household-scoped pull on this first call (confirmed directly against the pulled sqlite file: `sync_meta` completely empty, 0 expenses, right after the wipe's own `sync()` call returned) — while still showing a misleading "Cache cleared and re-synced." success message. A second, separate "Sync now" tap immediately afterward pulled everything correctly, confirming this is a one-shot race rather than a broken sync path. Not fixed this session, per the user's explicit instruction. See docs/DECISIONS.md for the full trace. |
| **Bug fix (profiles-tombstone gap)** | **done — live-verified** | 2026-09-07 | `TableRemoteDataSource.selectSince()` gained a `filterByHousehold` param (default true); `ProfileSyncAdapter` passes `false`, relying on RLS's `profile_visible_to_me()` — already correct server-side — instead of a client filter narrower than what RLS allows. `fvm flutter analyze --fatal-infos` clean, `fvm flutter test` green at 527. **Live-verified** on the real two-device setup (`emulator-5554`, real Supabase project): seeded a synthetic stale "Rupesh still an active member" row into Vineet's real `kharcha.sqlite`, relaunched the fixed app, and confirmed the app's own startup sync corrected it to `household_id: null` with no manual action — `sync_meta`'s profile cursor advanced to exactly Rupesh's real server-side departure timestamp, and the Dashboard's per-member card reflected the correction. **New related gap found and documented, not fixed**: departed members' names now show as "Unknown" on their own old expense rows instead of rendering (contradicts `profile_visible_to_me()`'s stated RLS design intent, never actually implemented client-side) — see docs/DECISIONS.md. |
| **Bug fix ("Unknown" display gap)** | **done — live-verified** | 2026-09-07 | Added `ProfileDao.watchAllKnown()` (every cached profile regardless of household) and `allKnownProfilesProvider`; swapped every *display-only* name-lookup site (expense/income read-only views and list rows, Dashboard's per-member/budget/recent-activity cards, budget list, Analytics member-comparison chart, PDF/CSV export) from the household-scoped `householdProfilesProvider` to it, while every *selection* site (Paid-by pickers, budget/recurring member pickers, household management, expense filter sheet) deliberately kept the household-scoped list. New DAO test in `profile_dao_test.dart`. `fvm flutter analyze --fatal-infos` clean, `fvm flutter test` green at 528. **Live-verified**: relaunched the fixed app on the same real device/session as the tombstone-gap fix (no re-seeding needed) — the Dashboard's per-member card now shows "Rupesh" instead of "Unknown" for his old expense. See docs/DECISIONS.md. |
| **Bug fix (cache-resync race)** | **done — live-verified** | 2026-09-07 | Fixed the last open item from Gate M2's live-testing session: "Clear local cache and re-download" not completing a full re-sync on its first attempt. Root cause: `SyncEngine.sync()`'s `getHouseholdId` callback synchronously read a Riverpod-cached provider value (`currentHouseholdIdProvider`, itself derived from `currentProfileProvider`'s Drift stream) which is not guaranteed to have caught up with `refreshOwnProfile()`'s own write earlier in the same `sync()` call — a genuine async gap, not a slow path. Fixed by making `getHouseholdId` `Future<String?> Function()`, backed by a one-shot `profileDao.findById(userId)` Drift query instead of the cached provider — since this runs immediately after `refreshOwnProfile()`'s `await`ed upsert, it's guaranteed to see whatever that write just wrote. New ordering regression test in `sync_engine_test.dart`. `fvm flutter analyze --fatal-infos` clean, `fvm flutter test` green at 529. **Live-verified** on `emulator-5554` (Vineet/admin) against the real Supabase project: tapped "Clear local cache and re-download" once, no follow-up "Sync now", then pulled `kharcha.sqlite` and confirmed a genuinely complete first-attempt re-sync (2 expenses, 1 income, 4 profiles, 21 categories, all 9 `sync_meta` rows populated) — where the pre-fix code left `sync_meta` empty and 0 expenses after the same single action. See docs/DECISIONS.md. |
| (ad hoc) | done | 2026-09-08 | First physical iOS device install (real iPhone 15, iOS 26.6.1) — every prior iOS test was simulator-only (Gate 0). Cleared, in order: Xcode device pairing, on-device Developer Mode, a missing iOS 26.5 Xcode platform component (`xcodebuild -downloadPlatform iOS`, 8.5 GB), no Xcode account signed in, and a stale `DEVELOPMENT_TEAM` in `ios/Runner.xcodeproj/project.pbxproj` left over from Gate 0 (fixed to the freshly-created Personal Team `F83DHF57GX`). **`flutter run` itself is broken** on this device/macOS 26.6/Xcode 26.6 combination — flutter_tools' internal ad-hoc pre-sign step (`_signFramework`) fails codesigning `Flutter.framework` on a `com.apple.provenance` extended attribute that its own documented fix (flutter/flutter#189734) cannot actually strip here, confirmed even against a byte-copied fresh-inode file. Filed as product feedback. Worked around by building via plain `xcodebuild ... -configuration Release -allowProvisioningUpdates build` (Xcode's own final sign step never hits the broken pre-sign path) and installing/launching directly via `xcrun devicectl device install app` / `device process launch` — trusted the developer profile on-device once, then it launched cleanly with the real Supabase config baked in (confirmed via `Generated.xcconfig`'s `DART_DEFINES`). A debug-mode build also hit Flutter's own separate, expected "can only be launched from Flutter tooling" restriction; Release mode has no such restriction. Full trace, all commands, and the exact repeat-in-7-days sequence (this is a free Apple ID, no paid Developer Program) in docs/DECISIONS.md. |
| T-M3.2/T-M3.3/T-M3.4/T-M3.5/T-M3.6 | done — unverified live | 2026-09-08 | Phase M3's code-based tasks (spec §11.17/§11.18), skipping the ops-only ones (T-M3.1 GitHub Pages publish, T-M3.7 friend docs, T-M3.8 owner checklist, T-M3.9 Gate 13, T-M3.10 release) per explicit user instruction to build the app code now and defer all live testing to a later combined pass with Gate M2. **T-M3.2/T-M3.3 (Feedback, F-17)**: new `FeedbackRemoteDataSource` (thin `feedback` insert seam, mirroring T-M2.2's rationale for mocking a thin data source over Postgrest's builder-returning `insert()` directly) + `FeedbackRepository.submit()`; `/feedback` screen (1–5 star row, category chips, free-text, auto-attached version/platform); reachable from Settings (permanent "Feedback" section), Diagnostics (an app-bar action, "so a bug report can follow a log share"), and a new dismissible `FeedbackPromptBanner` on the Dashboard, driven by `FeedbackPromptController` (watches a new `ExpenseDao.watchCountByUser()` stream, fires once at 10, persists a `shared_preferences` flag on dismiss-or-answer so it never reappears). **T-M3.4 (liveness, D21)**: `HouseholdRepository` already had a bare `touchActivity()` RPC wrapper from T-M2.2 — this task only added the once-per-hour throttle (`touchActivityIfDue()`, same shape as `UpdateCheckRepository`'s own throttle) and wired it into `app.dart`'s launch/resume hooks, guarded on a live session. **T-M3.5 (Account & deletion, F-18)**: new `AccountDeletionRepository` (`reauthenticate()` via a plain `signInWithPassword` re-check; `deleteAccount()` a thin wrapper over the already-deployed `delete-account` Edge Function, mapping its `FunctionException`/`details.error` codes the same way `ErrorMapper` maps named Postgres RPC codes) + `AccountDeletionController` (stop sync → reauthenticate → call the function → wipe local DB/receipt cache → sign out, mirroring `SignOutController`'s race-avoidance and re-arming sync if the function call itself is refused); `/account` screen implements the spec's literal 3-step flow (explain → export-or-skip → type DELETE + password); new `ExportRepository.exportMyDataJson()` scopes the existing full-backup shape down to rows the caller personally authored (own profile + `userId`/`uploadedBy`/`createdBy`-matched expenses/incomes/attachments/budgets/recurring rules) rather than reusing the admin-only household-wide backup. A new `/account/deleted` route + `AccountDeletedScreen` give the post-deletion "plain confirmation screen" spec explicitly asks for (not a bare bounce to `/login`) — added to `app_router.dart`'s signed-out-reachable set so the sign-out this flow performs internally doesn't redirect past it first. **T-M3.6 (in-app legal links)**: `AppConfig` gained `PRIVACY_POLICY_URL`/`TERMS_URL` dart-defines (both blank until T-M3.1 actually publishes something) and a shared `openLegalPage()` helper (opens the URL, or a "not published yet" snackbar if blank) — wired into `SignUpScreen`'s previously-inert Terms/Privacy line (T-M2.4 had stubbed it to always show "not published yet") and two new Settings → About tiles. **T-M3.1 partial**: wrote the actual privacy-policy/terms content (`docs/legal/PRIVACY.md`/`TERMS.md`, covering every point spec §15 item 11/§11.18 requires) but did not publish them anywhere — enabling GitHub Pages (or any other public host) is a repo-settings action left for the user, and the dart-define URLs stay blank until that happens. 11 new unit tests (`feedback_repository_test.dart`, `account_deletion_repository_test.dart`, plus a `watchCountByUser` case in `expense_dao_test.dart`); no widget tests for the 4 new screens, deferred to a later consolidated pass, same precedent T-M2.6 set for onboarding screens shipping ahead of their own widget-test task. `fvm flutter analyze --fatal-infos` clean; `dart format --set-exit-if-changed .` clean on every touched file; `fvm flutter test` green at 540 (up from 529). **Not live-verified**: none of this has run on a device yet — folded into the same future combined live pass as the still-open Gate M2 items (`docs/TESTING_M2.md`). |
| T-M3.1 | **done** | 2026-09-08 | User enabled GitHub Pages (repo Settings → Pages → Deploy from branch → `gh-pages`) — confirmed live: `https://vineet2102.github.io/Kharcha-App/`, `/privacy.html`, and `/terms.html` all return HTTP 200. In-app links (T-M3.6) now resolve correctly with no further change needed. Published the privacy policy and terms as static HTML (`privacy.html`/`terms.html`/`index.html`, plain CSS, light/dark via `prefers-color-scheme`) on a new orphan `gh-pages` branch, pushed to `origin` — deliberately isolated from `master`'s content rather than serving from a `/docs` folder on the main branch, since `docs/PROGRESS.md`/`DECISIONS.md` are internal build logs (real member names, a live Supabase project ref, bug traces) that have no business being dressed up as a public website even though the repo itself is already public on GitHub. **One step left, deliberately not taken this session**: enabling GitHub Pages itself (repo Settings → Pages → Source: Deploy from a branch → `gh-pages` / root) is a repo-settings change with no `gh` CLI or API token available in this session to do it non-interactively — left for the user, a ~10-second toggle. `AppConfig.privacyPolicyUrl`/`termsUrl` (T-M3.6) already point at the resulting URLs (`https://vineet2102.github.io/Kharcha-App/privacy.html`/`terms.html`) via the local, gitignored `config/dev.json` — the in-app links start working the moment Pages is switched on, no further code or config change needed. `config/example.json` documents the two new keys as placeholders. |
| T-M3.7 | done | 2026-09-08 | `docs/ONBOARDING.md` (spec §16.5.1's friend-invite message template verbatim, plus the "why each line stays" rationale, the iPhone deflection line from §16.5.2, the three questions friends actually ask, and the §16.4 rollout-ring table) and `INSTALL.md` (repo root, a friend-facing, non-technical sideload walkthrough: download → the "unknown sources" warning → install → first launch → notifications/battery-optimisation checklist → updating → uninstalling ≠ deleting your account → contact) written per this task's own acceptance line ("a friend can follow it without asking a question"). Both link to the `gh-pages` URLs from T-M3.1. |
| T-M3.8 | done — documented, first run not performed | 2026-09-08 | Added an "Owner maintenance (monthly)" section to `README.md`: check Supabase DB size/storage usage, read new `feedback` rows, scan `last_seen_at`/`last_active_at`, take a full backup via the existing Export screen. **Not performed this session** — every item needs the Supabase dashboard, which this sandbox has no credentials for; this task's own acceptance line's "first run performed" half is left for the user, same category of gap as every other live/manual-only item this phase. |
| T-16.1 (brought forward) | done | 2026-09-08 | Android release keystore generated (`~/kharcha-upload-keystore.jks`, alias `kharcha`, PKCS12, 10,000-day validity) and backed up by the user off this machine. Signing wired into `android/app/build.gradle.kts` (a `release` `signingConfigs` block reading `android/key.properties`, falling back to the debug config when that file is absent so `flutter run --release` keeps working without it) plus `isMinifyEnabled`/`isShrinkResources` and a new `proguard-rules.pro` keeping `flutter_local_notifications`'s classes and Gson reflection intact — see docs/DECISIONS.md for why (R8 could otherwise silently break scheduled-alarm restore after reboot). **Live-verified**: built the signed+shrunk release APK against `config/prod.json` (new, gitignored, same real Supabase project as `config/dev.json`), confirmed via `apksigner verify` it's signed with the new key (not debug), force-reinstalled over the differently-signed debug build already on `kharcha_test`, and confirmed it launches cleanly to the real Login screen with no crash. `fvm flutter analyze --fatal-infos` clean. Pulled forward from Phase 16 because T-M3.10 needs a real signed build to point its GitHub Release at — see docs/DECISIONS.md for the full rationale and why the rest of Phase 16 was deliberately *not* pulled forward. |
| T-M3.10 | done | 2026-09-08 | `pubspec.yaml` bumped to `2.0.0+1`. `docs/RELEASE_NOTES.md` written (user-facing, not a build log). `v2.0.0` tag pushed, triggering `.github/workflows/release.yml` — needed one real fix caught live: the default `GITHUB_TOKEN` is read-only, so `softprops/action-gh-release` failed with "Resource not accessible by integration" on the first attempt; fixed by adding `permissions: contents: write` to the workflow, and re-tagging after the bad tag (which triggered a failed run but never published anything) was deleted with the user's help — see docs/DECISIONS.md. **Live-verified**: the second run built and published a real GitHub Release — [v2.0.0](https://github.com/Vineet2102/Kharcha-App/releases/tag/v2.0.0), APK asset (77.9 MB) confirmed publicly downloadable via an anonymous ranged `curl` (`HTTP 206`, no auth). Inserted the first-ever `app_releases` row via `supabase db query --linked` (android, `2.0.0`, build 1, `min_supported=1`, `download_url` pointing at the release asset, `release_notes` from the same file) — confirmed via a `select` immediately after. iOS gets no row: there is still no real sideload-distribution path for iOS (D3/§16.3's free-Apple-ID model only covers the owner's own two phones), so publishing one would be a broken "Get it" link — left for whenever that changes. **Not verified this session**: the in-app banner actually rendering for a genuinely newer `build_number` — inserting/updating a test row against the real `app_releases` table (even a disposable one) was blocked by the auto-mode safety classifier as a production-database write, so F-14's comparison logic is verified by its existing unit tests (T-14.6) and by this real row's correct shape, not by a live banner sighting. `fvm flutter analyze --fatal-infos` clean throughout. **⚠️ Superseded same day**: this v2.0.0 build turned out unable to sign in at all (see the `INTERNET` permission bug below) — a corrected v2.0.0 build/release/`app_releases` row must replace it before distribution. |
| **Bug found & fixed** | — | 2026-09-08 | **Critical: the published v2.0.0 release APK could not sign in at all.** `android/app/src/main/AndroidManifest.xml` never declared `android.permission.INTERNET` — only the `debug`/`profile` manifest overlays had it (Flutter's own template default, added there for dev tooling), so every prior test in this project's history (Phase 0 through M3) used a debug-permission-carrying build and never noticed. First live sign-in attempt against the actual release build failed with the generic "Something went wrong" every time; ruled out R8 (temporarily disabled `isMinifyEnabled`/`isShrinkResources` — still failed identically) and ruled out credentials/backend (a direct `curl` against the real GoTrue token endpoint with the same email/password returned `200` with a valid session). Fixed by adding the permission to the `main` manifest. **Live-verified**: rebuilt the same signed+minified+shrunk release APK, fresh install, same credentials — signs in cleanly to a real Dashboard with live data. See docs/DECISIONS.md for the full diagnostic trace. `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 540. |
| **Bug found & fixed** | — | 2026-09-08 | D21/T-M3.4's liveness ping never fires for "sign in and keep using the app within its first foreground session" — only for boot-with-existing-session or an actual background→foreground resume. Found immediately after the fix above, first time this project ever had a real signed-in session on a release-shaped build to test with: `profiles.last_seen_at` stayed `NULL` through a real sign-in. Fixed by also calling `touchActivityIfDue()` from `app.dart`'s existing sign-in-transition `ref.listen(currentSessionProvider, ...)` block. **Live-verified**: relaunching and signing in immediately populated `last_seen_at` with a fresh timestamp, confirmed via the Supabase dashboard. See docs/DECISIONS.md. |
| T-M3.2/T-M3.3/T-M3.4/T-M3.6 (live verification) | done | 2026-09-08 | Live-verified on `kharcha_test` against the real Supabase project, using the real Vineet/admin account (with the user's own involvement re-confirming the sign-in symptom that led to the bug above). **T-M3.2**: submitted a real 5-star "General" feedback entry from the app; confirmed via the Supabase dashboard the `feedback` row has the correct message, `app_version: "2.0.0+1"`, `platform: "android"` — spec's literal acceptance line. **T-M3.3**: not separately re-tested this session (already covered by its own dedicated test at 9/10 expenses per its PROGRESS entry). **T-M3.4**: confirmed via the liveness-ping bug fix above — `last_seen_at` now advances on sign-in. **T-M3.6**: tapped "Privacy policy" in Settings → About; confirmed `url_launcher` handed off to Chrome correctly (first-run Chrome account picker appeared, proving the intent fired) — combined with the standalone `curl` check confirming the live page returns `200`, this closes the loop end-to-end. |
| T-M3.10 (re-release) | done | 2026-09-08 | `pubspec.yaml` bumped to `2.0.1+2`; `v2.0.1` tagged and pushed, producing a second [GitHub Release](https://github.com/Vineet2102/Kharcha-App/releases/tag/v2.0.1) containing the `INTERNET`-permission fix. Kept `v2.0.0`'s tag/release in place rather than deleting it (it was already genuinely public by this point) — instead edited its description via `gh release edit` to prepend a "broken, do not use — see v2.0.1" warning. `app_releases` row updated in place (`version_name: '2.0.1'`, `build_number: 2`, **`min_supported: 2`** — deliberately blocking build 1 via F-14's emergency-brake dialog, not just superseding it) via `supabase db query --linked`, confirmed via a `select`. **Live-verified against the real distributed artifact, not just a local build**: downloaded `app-release.apk` from the actual public release URL with `curl`, confirmed its signature with `apksigner verify`, installed that exact file fresh on `kharcha_test`, and signed in successfully — the full chain from "tag pushed" to "a real download works" is now proven end-to-end. `docs/RELEASE_NOTES.md` documents both versions. Gate M3's own full acceptance line (a stranger installing, signing up, and using the app) remains untested — this closes T-M3.10 specifically, not Gate M3. |
| T-M3.9 | **attempted live on real hardware — real bug found, not fixed** | 2026-09-08 | First-ever test on a genuine physical Android device (Samsung Galaxy M56, One UI, Android 16), not an emulator. `dumpsys alarm` confirms the daily-reminder alarm registers and fires exactly on schedule (~73s inexact-alarm delay, expected), and `ActivityManager` confirms the broadcast is dispatched to `ScheduledNotificationReceiver` — but no notification ever posts and no `daily_reminder` Android notification channel is ever created, on two separate attempts. Ruled out, in order: standard Android Doze whitelist (was missing, fixed live via Battery → Unrestricted, retested — still failed identically), Samsung Device Care's Sleeping/Deep-sleeping apps lists (not present), Samsung Auto Blocker (off). Root cause not conclusively identified — likely a fourth, undocumented One UI background-execution restriction, or something specific to a debug-mode install. `monthly_summary`'s immediate-notification path fired correctly in the same session, ruling out a blanket permission problem. Full diagnostic trace and suggested next steps (retest against a signed release build; consider a `flutter_local_notifications` major-version bump) in docs/DECISIONS.md. **Gate 13 stays `partial`** — this is a real, reproducible finding, not a pass. |
| (session note) | deferred | 2026-09-08 | `docs/TESTING_M2.md`'s live tester checklist (sign-up-with-real-email-confirmation through invite/join/leave/remove/delete) was attempted this session but blocked immediately by Supabase's default-SMTP `over_email_send_rate_limit`, hit after only 2-3 sign-up attempts and still active ~30 minutes later — first live confirmation of T-M1.9's already-known gap, worse in practice than the app's own "try again in a few minutes" copy suggests. Deferred to a session with a longer untouched cooldown (hours, not minutes) before retrying — see docs/DECISIONS.md. One harmless leftover: an unconfirmed throwaway auth user now exists under the user's bare real email (no `+alias`) from the first attempt, safe to ignore or delete later via the Supabase dashboard. |
| T-16.2 | done | 2026-09-08 | Custom launcher icon for both platforms, replacing the default Flutter icon flagged as "genuinely required before ring 3" (spec §16, D-series Phase-16 revision table). Per spec's own "generic — no design work" instruction: a flat ₹ glyph (Noto Sans Bold, already bundled for PDF export) in the app's existing brand green (`AppColors.green` `#55D88A`) on its existing ink background (`AppColors.ink` `#090B0A`) — reuses the live in-app palette rather than inventing a new one. Two source PNGs generated at `assets/icon/` (`icon.png` — flat, opaque, for iOS/legacy Android; `icon_foreground.png` — transparent, glyph confined to the ~66% adaptive-icon safe circle, for Android's foreground layer) via a one-off Pillow script (the script itself not checked in, but both PNGs are — `flutter_launcher_icons`' config references them by path, so they're the real source of truth for ever regenerating the icon, not a disposable intermediate). Wired via `flutter_launcher_icons` (`pubspec.yaml`, new dev-dependency + config block: `adaptive_icon_background`/`adaptive_icon_foreground` for Android, `remove_alpha_ios: true` for iOS's flattened-icon requirement, `min_sdk_android: 26` matching T-0.7), then generated for both platforms in one run. Manually reverted one unrelated side effect of the generator's iOS pbxproj rewrite (`ASSETCATALOG_COMPILER_GENERATE_SWIFT_ASSET_SYMBOL_EXTENSIONS` flipped from `YES` to `AppIcon`, scoping Swift asset-symbol generation away from `LaunchImage`) back to its original value, keeping this change scoped to the icon only. Deleted the now-dead default `ic_launcher.png` fallbacks the generator's Android output superseded (`AndroidManifest.xml` now points at `@mipmap/launcher_icon`). `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 540 (no test-count change — icon assets aren't unit-testable); `fvm flutter build apk --debug` succeeds with the new resources. **Not live-verified**: seeing the actual icon rendered on a home screen (Android launcher, iOS springboard) needs a device/emulator, not available in this sandbox — same category as every other device-only check this project defers. This closes out Phase 16's only remaining real build task; T-16.1/T-16.3/T-16.4 were already done (T-16.4 done ad hoc, never logged under its own task ID — see the "First physical iOS device install" row), and T-16.5/T-16.6/T-16.7 are superseded by Phase M3's rollout-ring model, README extension, and T-M3.10 respectively. |
| T-17.1 | **partial — §7.1 half only** | 2026-09-08 | Re-ran all 8 RLS checks from spec §7.1 against the real production household ("Panicker Family", `11111111-1111-1111-1111-111111111111`) and its real rows — not a disposable test household like T-1.8's original pass or T-M1.10's MT-1..16 suite. Impersonated real member/admin accounts (Trupti, Vineet) and a real no-longer-in-a-household account (Rupesh — his profile's `household_id` is now `null` after presumably leaving, yet a historical expense of his still correctly lives on in the household, which doubles as a live example for RLS-8's "forged household" case) purely via `set local role authenticated` + `select set_config('request.jwt.claims', ...)`, the same mechanism the Supabase SQL editor's own "impersonate" feature uses — no real passwords needed. Every mutating check (RLS-2..6) ran inside `begin; ...; rollback;` so no real family data was ever at risk; verified clean afterward by re-reading both real expenses' `note` fields (unchanged) and confirming zero `RLS-%`-named category rows leaked. All 8 passed: RLS-1 (member sees both real household rows), RLS-2/3 (member's update/delete of another member's real expense: 0 rows affected), RLS-4 (member forging `user_id` on insert: rejected), RLS-5 (member inserting a category: rejected), RLS-6 (admin's update of another real member's expense: succeeded), RLS-7 (anon role: 0 rows), RLS-8 (a real account with no current household, filtering by the production household's real id: 0 rows). Full methodology and exact SQL in docs/DECISIONS.md. **Incomplete relative to KHARCHA_SPEC.md's §17.M4 amendment** (discovered *after* this row was first written as "done" — see docs/DECISIONS.md addendum): T-17.1 in the real v2.0 spec requires re-running **both** §7.1 (this row) **and** §7.2's 16-test cross-tenant MT suite against production data with **at least two real households**. Production has exactly one real household — the MT suite cannot be run against production data at all until a second one exists (a friend actually joining, not a fabricated one), so that half is blocked, not merely undone. |
| T-17.2 | tool built, not yet run | 2026-09-08 | Per spec's own "manual entry or a CSV import script" instruction, and user's explicit choice of the CSV-importer path: new `scripts/import_historical_expenses.dart`, a standalone `dart run` script (not part of the Flutter app — added the pure-Dart `supabase` package, already a transitive dependency via `supabase_flutter`, as an explicit `dev_dependency` for it) that signs in as the household admin and bulk-inserts real historical expenses from a CSV shaped like the app's own export format (spec §11.11) minus `has_receipt`/`id`. Validates every row (date/time/amount format, and that `member`/`category`/`payment_method` names resolve to real rows in the household) before writing anything; prints a total + count and asks for interactive confirmation before inserting; `--skip-invalid` and `--yes` flags for scripted use; inserts in chunks of 50 with automatic per-row fallback so one bad row doesn't sink an otherwise-good batch. Relies on RLS itself for correctness rather than bypassing it (admin's `exp_insert` policy already allows writing any member's `user_id`; a non-admin sign-in is restricted client-side to their own rows to match what RLS would allow anyway) and on the existing `set_ist_date` trigger to derive `spent_on` from the `spent_at` the script sends, so it needed no server-side changes. Dry-run tested against the structural-validation and sign-in-failure paths only (fake credentials against the real `config/dev.json` project, confirming a clean rejection with no crash) — **not yet run for a real import**: that needs the user's actual 3 months of family expense history, which doesn't exist anywhere to type into a CSV yet. Documented in `README.md`. `fvm flutter analyze --fatal-infos` clean. |
| T-17.3 | done — restore proven live | 2026-09-08 | Took the first real full JSON backup of production (via direct SQL, matching `exportFullBackupJson`'s exact shape — see docs/DECISIONS.md; sent to the user directly rather than committed, since the repo is public and this is real family data). Wrote `scripts/restore_backup.dart`, which turns that JSON into a plain SQL restore script, and with the user's explicit go-ahead, proved it for real: created a disposable scratch Supabase project (`kharcha-restore-scratch`, `ap-south-1`), pushed every migration, ran the restore, and verified every table's row count matched the backup exactly (1 household, 4 profiles, 21 categories, 6 payment methods, 3 recurring rules, 14 expenses, 1 income, 4 budgets, 3 attachments), zero orphaned foreign keys, the expense total matched production's real total (₹18,448.00) exactly, and a live RLS impersonation check against the *restored* project still enforced correctly — then deleted the scratch project and confirmed the repo's `supabase` CLI link was back on production. Building the restore script by actually running it (not just reading the schema) surfaced three real ordering/constraint issues, all documented and fixed in docs/DECISIONS.md: `profiles.id`'s FK to `auth.users` needing placeholder auth rows inserted first, `handle_new_user()`'s trigger-created placeholder profile requiring the real profile insert to be an upsert (with `guard_profile_membership()`'s `kharcha.allow_membership_change` escape hatch), and `0009_seed.sql`'s default categories/payment methods colliding by name with the real ones on any freshly-migrated target (now cleared automatically before restoring). Restore procedure documented in `README.md`. **Known, by-design limitation**: restores database rows only — receipt images live in Storage, not the JSON backup, so restored `attachments` rows have no backing object until receipts are separately restored/re-uploaded. |
| (ad hoc) | done | 2026-09-08 | Second physical iOS install, this time via the documented `xcodebuild`/`devicectl` workaround from the first ad hoc entry (this sandbox has no interactive access to the device screen, so the user drove the phone directly while narrating results back). Built Release against `config/prod.json` (real production project — same one every other live gate in this project uses), `xcodebuild -workspace ios/Runner.xcworkspace -scheme Runner -configuration Release -destination 'id=<device-id>' -allowProvisioningUpdates build` succeeded cleanly (properly codesigned with the real "Apple Development" identity + provisioning profile, not flutter_tools' broken ad-hoc pre-sign path), `xcrun devicectl device install app` + `device process launch` installed and launched with no re-trust prompt needed (the device already trusted this signing identity from the first ad hoc session). App was already signed in as Vineet/admin from the prior install, straight to the Dashboard against live production data. |
| T-M2.12 (iOS pass) | **done — the 8 screens Gate 14's own note flagged as "not covered" are now clean** | 2026-09-08 | Live-verified on the real iPhone 15 against production data, closing the exact gap the 2026-09-07 Android pass left open ("Not covered this pass: Categories/Payment methods/Income/Budgets/Recurring/Notifications/Export/About screens"). All 8, user-driven, no crashes: **Categories** (drag-reorder, edit icon/colour, archive/unarchive). **Payment methods** (add "Test Wallet", rename, delete — delete succeeded since it had no expenses attached, confirming the usage-guard's happy path). **Income** (added a ₹1 test entry, confirmed it appeared in the Income list and the Dashboard's income figure updated, deleted it, confirmed both reverted). **Budgets** (created a ₹1 household budget for the month, confirmed the Dashboard's budget-progress card and its bar appeared, edited the amount, deleted it, confirmed it disappeared from both places). **Recurring** (created a manual, non-auto-post monthly test rule, confirmed the list and its "Next occurrence" preview, deleted it — auto-post deliberately left off so nothing was posted against real data). **Notifications** (toggled several switches off/on with no crash and correct persistence; see the dedicated finding below for the daily-reminder test specifically). **Export** (CSV and PDF, "This month" — both produced a correct iOS share sheet). **About** (version/build/Supabase host rendered correctly, "Check for updates" didn't crash, Privacy policy and Terms both opened the real hosted `gh-pages` pages). **Deliberately not attempted**, per the same safety judgement Gate 14's own note already flagged: **Delete-household** (not reachable — the real household has 4 members, and the button only renders for a sole-member admin) and **Remove-member** (would genuinely remove a real family member from the household; skipped rather than risk it without a clear win to justify it). Test data created during this pass (the ₹1 income entry, ₹1 budget, "Test Wallet" payment method, the recurring rule) was deleted live during the same session — no leftover test artifacts on this pass, unlike several earlier gates' documented precedent of leaving them for later cleanup. |
| **Finding — T-M3.9's daily-reminder bug is Android/One-UI-specific, not a general scheduling bug** | — | 2026-09-08 | While testing Notifications settings above, set the daily reminder for ~1 minute out and backgrounded the app on the real iPhone. **The notification posted exactly on time, no delay** — a clean pass, on the very code path (`NotificationScheduler`/`NotificationService.scheduleAt`, the same `zonedSchedule`/`AndroidScheduleMode.inexactAllowWhileIdle`-shaped call, iOS just uses its own native scheduling under the hood via the same plugin) that has now failed twice on the real Samsung Galaxy M56 (T-M3.9). This narrows the suspect list considerably: the bug is not in `NotificationScheduler`'s scheduling logic, `nextDailyReminderFireIst`'s time computation, or `flutter_local_notifications`' cross-platform API surface — all of that ran identically here and worked. It's specifically something in Android's (and likely specifically Samsung One UI's) delivery path for a scheduled `AlarmManager` broadcast reaching `ScheduledNotificationReceiver`, consistent with T-M3.9's own suspicion of "a fourth, undocumented One UI background-execution restriction." Does not close Gate 13 — the bug is real and unfixed on the platform it actually needs to work on (all of Vineet's actual family are on Android) — but meaningfully redirects where to look next: Android-specific OEM behavior, not app-level scheduling code. |
| T-M1.9 (revisited) | **done — live-verified** | 2026-09-09 | Custom SMTP finally set up, closing the one item explicitly flagged as "not optional before real distribution" (R13). Switched providers from the originally-selected Resend (blocked on not owning a domain — its free tier only delivers to arbitrary recipients from a verified custom domain) to **Brevo**: free forever at 300 emails/day, and verifies individual sender *addresses* rather than domains, so no DNS/domain purchase needed. Set up via Claude-in-Chrome with the same split of responsibility as T-1.1/T-1.2 (assistant drives navigation; user handles account creation, password, and Brevo's required phone/SMS verification themselves) — assistant generated a no-expiry SMTP key and wired `smtp-relay.brevo.com:587` into Supabase Dashboard → Authentication → Emails → SMTP Settings, sender "Kharcha" `<vineetpanicker2002@gmail.com>`. Caught one real mismatch before it could cause a silent failure: the sender address initially entered didn't match what Brevo had actually auto-verified from the account's sign-up email (one letter off) — checked Brevo's Senders list directly rather than trusting the typed value, and corrected it. **Live-verified end-to-end**, not just configured: triggered a real password-recovery email against the project's own `vineetiimabc@gmail.com` test-admin account (chosen so no family member got an unexpected email) and confirmed three independent signals — Supabase Auth Logs show `/recover` completed with `status: 200` in ~773ms (GoTrue's SMTP send is synchronous, so this means the send itself succeeded); the same logs show `env GOTRUE_RATE_LIMIT_EMAIL_SENT changed, updating Email limiter from 2/1h to 30`, confirming the exact default-SMTP throttle behind the earlier real rate-limit failure (2026-09-08 session note above) is gone; and Brevo's own transactional Logs page shows the email progressing `Sent` → `Delivered` within the same minute. Full trace, including the two real mistakes caught along the way, in docs/DECISIONS.md. **Known trade-off, accepted deliberately**: sender is a personal Gmail address, not a branded domain — cosmetic only, not a blocker. |
| **Gate M1** | **passed (in full)** | 2026-09-09 | T-M1.9 (above) was the only exception on Gate M1's original 2026-09-06 "passed (T-M1.9 excepted)" row. With it now done and live-verified, every T-M1.x task is complete and the exception no longer applies. |
| **Bug found, plan written, not fixed** | — | 2026-09-09 | Every auth email's link (sign-up confirmation, password reset, resend) points at `http://localhost:3000` — Site URL's Phase 0 default, never changed, and never overridden per-call since `AuthRepository`'s `signUp()`/`resetPasswordForEmail()`/`resend()` don't pass `emailRedirectTo`. Confirmed by reading the actual code and native manifests, not just prior notes: neither `AndroidManifest.xml` nor `Info.plist` registers `io.supabase.kharcha://login-callback/` as a URL scheme at all, so the OS has nowhere to hand the link even if the redirect target were fixed — and there is no `/reset-password` screen/route anywhere in the app, so the password-reset link would have nowhere to land regardless. The account *does* get confirmed server-side the moment the link is tapped (Supabase's `/verify` endpoint confirms before redirecting), so it isn't a total dead end — force-quitting and reopening the app lands on a working `/login` where the same email/password signs in immediately — but nothing in the UI tells a friend to do that, so in practice this is a real Ring-3/R13 blocker. Full root-cause trace and the 8-point implementation plan (Android + iOS manifest changes, explicit `emailRedirectTo` on every auth call, Supabase Site URL update, a new `/reset-password` screen, and a two-part fallback safety-net UX on `VerifyEmailScreen` — a permanent "Sign in instead" link plus a resume-triggered snackbar, both proposed by the user this session) are in docs/DECISIONS.md. **Not implemented — documented per the user's explicit request to plan now and build later.** |
| **Bug fixed — auth email deep-link** | — | 2026-09-09 | All 8 points of the plan above implemented in code: `AndroidManifest.xml`/`Info.plist` register the `io.supabase.kharcha://login-callback/` scheme; `AuthRepository.signUp()`/`resend()`/`resetPasswordForEmail()` pass it explicitly as `emailRedirectTo`/`redirectTo` (new `AppConstants.authCallbackUrl`); new `/reset-password` route + `ResetPasswordScreen`, reached via a new `AuthChangeEvent.passwordRecovery` listener in `app.dart`, with `app_router.dart`'s `redirect` exempting it unconditionally once signed in so a member who's left every household can still reach it; `VerifyEmailScreen` gained the permanent "Already tapped the link? Sign in" fallback plus the 20s-grace resume snackbar. The Supabase Dashboard's Site URL was also switched from `http://localhost:3000` to the same custom scheme, live on production, via Claude-in-Chrome with the user's explicit go-ahead — the Redirect URLs allow-list already had this exact entry from T-M1.8, only the actual default had never been changed. `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 546 (6 new: `reset_password_screen_test.dart`, `verify_email_screen_test.dart`; 6 existing `auth_repository_test.dart` cases updated for the new named args). `dart format` clean on every touched file. `fvm flutter build apk --debug` succeeds with the new intent-filter confirmed present in the merged manifest. **Not live-verified**: an actual tapped email link on a device — needs a new signed build first, deferred per this project's batch-then-test-live precedent; still gates Gate M2's brand-new-signup item and Ring 3 (§16.4). Full detail in docs/DECISIONS.md. |
| **Bug fix live-verified — auth email deep-link, real sign-up on a physical device** | — | 2026-09-09 | Same-day, later session. Built Release via the established `xcodebuild` + `devicectl` workaround (Generated.xcconfig regenerated first via a `flutter run --dart-define-from-file=config/prod.json` that fails at its known ad-hoc-codesign step but still writes the dart-defines) and installed fresh on the real iPhone. The account deleted just before this test (`vineetiimabc@gmail.com`, the project's own test-admin address — see the row below) was used to sign up genuinely fresh: real email arrived via Brevo, tapping its confirmation link on-device triggered the OS's "Open in Kharcha?" prompt (the new intent-filter/`CFBundleURLTypes` working exactly as intended — this exact link previously dead-ended at `localhost:3000`), and opening it landed straight on the create/join-household gate, correctly reflecting the now-confirmed session. Created a real new household ("Test Household"). **Verified server-side, not just by the on-device outcome**: `auth.users` shows `confirmed_at` 19s after `created_at`; `public.profiles` shows the new admin profile correctly linked to the new household; `public.households` now has a genuine second row alongside "Panicker Family"; the new household auto-seeded its own 20 categories + 6 payment methods, isolated from the original household's. **Cross-tenant RLS spot-checked live in both directions** on this — the first time ever two real households have coexisted in production: impersonating the new account sees exactly 1 household / 20 categories / 1 profile (zero leakage of Panicker Family's rows); impersonating a real Panicker Family member (Tanish) sees only "Panicker Family" (Test Household invisible). This closes the "not live-verified" gap on the row above, closes Gate M2's specific brand-new-signup blocker (a real inbox + a human tapping a real confirmation link), and — per spec §16.4 — is genuinely **Ring 1** of the rollout (a second throwaway account creating a second household), the step immediately before a real friend (Ring 3). Not yet re-tested: Gate M2's fuller two-device literal acceptance line (account A invites, account B *joins by code*, both see synced expenses within 10s, B leaves cleanly) — today's test created a household rather than joining one, so that half is still open. |
| **Production account deleted, to free the address for the live test above** | — | 2026-09-09 | Per the user's explicit request, deleted the throwaway `vineetiimabc@gmail.com` auth account used for T-M1.9's Brevo verification (id `1a9a0efc-...`, display name "Test Admin") so the same address could be reused for a genuinely fresh sign-up. Checked before deleting, not assumed safe: the account had no household, 0 expenses, 0 incomes, and every relevant foreign key (`profiles.id → auth.users.id` and every standard `auth.*` table) is `ON DELETE CASCADE` — the same outcome the Dashboard's own "Delete user" button would produce. Deleted via a direct `DELETE FROM auth.users` through `supabase db query --linked` (not via the Dashboard UI); confirmed 0 rows left in both `auth.users` and `public.profiles` afterward. |
| **Gate M2's join-by-code gap closed — real second member joins the real household, live, on two physical/emulated devices** | — | 2026-09-09 | Same day, immediately after the row above. Deleted a second throwaway account the same verified-safe way (`vineetpanicker2002@gmail.com`, display name "vintya", no household/expenses/incomes, cascade-only FKs) to free it for reuse. Relaunched `kharcha_test` (Android emulator) with its existing real session intact — Vineet/admin, signed into the real Panicker Family household, deliberately left untouched throughout — and read its real invite code (`7MWT-YDX4`) from Settings → Household. Signed up fresh as `vineetpanicker2002@gmail.com` on the real iPhone (a second physical/emulated device, not the same one that generated the code), confirmed via a real Brevo email and the same working deep-link, and **joined by invite code** rather than creating a new household — the specific half of Gate M2's literal acceptance line ("account B signs up, confirms, joins with the code") that the previous row's test hadn't covered. The admin's emulator, synced, immediately showed a 4th real member ("Vintya") in the household roster; the iPhone's own dashboard independently showed the same Panicker Family data (same spend/income/budget figures) — a live cross-device sync match, not just a one-sided report. **Verified server-side too**: `auth.users` shows the new account confirmed 13s after signup; `public.profiles` shows `household_id` = Panicker Family's real id, `role: member`, `is_active: true`; a full roster query shows exactly the 4 real active members (Trupti, Tanish, Vineet/admin, Vintya) — Rupesh stays correctly absent (he'd already left in an earlier session, per the profiles-tombstone fix). One near-miss caught and corrected live, not shipped: navigating the admin emulator's Settings screen momentarily surfaced the "Leave Panicker Family?" confirmation dialog (a real destructive action on the real household) — caught before confirming and dismissed via Cancel; the household and its roster were unaffected (confirmed by the "4 members" / "used 2 of 50" counts immediately after, which are only consistent with a cancelled leave). **Still open on Gate M2's literal acceptance line**: the "B leaves — B's local database is empty and B is on `/onboarding`, while A's household is unchanged" half was not attempted this session (Vintya's account stays joined, on purpose, since it's a real now-permanent extra member of the real household unless removed later). |
| **[NEW, open, not fixed] A deleted account's profile never disappears from other members' locally cached device state** | — | 2026-09-09 | Found live, immediately after the row above, testing F-18 account deletion instead of "leave household" — same session, different scenario. Deleted "Vintya" (`vineetpanicker2002@gmail.com`) via the real in-app "Delete account" flow on the iPhone, then ran a normal "Sync now" on the admin's `kharcha_test` emulator (no cache clear). **The admin's app still shows Vintya as a household member after the sync** — confirmed by the user, not assumed. **Verified server-side the deletion itself was completely clean**, ruling out a partial/failed deletion as the cause: `auth.users` and `public.profiles` both show 0 rows for her (id and email), and the household's active roster is correctly back to exactly Trupti/Tanish/Vineet — so this is purely a client-sync gap, not a backend one. **Root cause, read from the code, not guessed**: `ProfileSyncAdapter.hasTombstones` (`lib/data/sync/entity_sync_adapters.dart:348`) is `false`, and unlike *leaving* a household (which sets `profiles.household_id = null` on a row that survives — the exact mechanism the 2026-09-07 "profiles-tombstone gap" fix depends on), F-18's `delete-account` Edge Function hard-deletes `auth.users`, which cascades (`ON DELETE CASCADE`, confirmed via `pg_constraint`) into a genuine `DELETE` on `profiles`. `PullService`'s incremental `selectSince(cursor)` only detects rows whose `updated_at` moved past the cursor; a row that has been fully deleted produces no such row and thus no signal at all — there is nothing for a normal sync to pull that would tell another device "this member is gone." This is a different, arguably worse gap than the already-fixed profiles-tombstone one (that was about a *departed* member's row persisting-but-orphaned; this is about a *deleted* member's row not persisting at all, anywhere sync can see it). **Practical impact**: every screen backed by the locally-cached profile list (household roster, Paid-by/budget/recurring member pickers, expense filters) can keep showing a genuinely-deleted person indefinitely, on every other member's device, until someone happens to run "Clear local cache and re-download" (T-14.4's full wipe-and-repull, which — unlike incremental sync — queries current server state directly and so should naturally exclude her). **Workaround confirmed live**, same session: ran "Clear local cache and re-download" (not just "Sync now") on the same admin emulator — Vintya correctly disappeared from the household roster, confirming the hypothesis and giving a genuine, if manual, escape hatch until this is fixed properly. **Not fixed this session** — found (and its workaround confirmed) via live testing, documented for a future session per the user's standing preference to batch implementation separately from live-test discovery. |
| **Deleted-account profile cache gap — fixed and pushed to production; not yet live-verified** | — | 2026-09-09 | Fixed the row above, same day, later session, with the user's explicit request to close this bug out despite having no Android device available this time. Gave `profiles` a real `deleted_at` tombstone, matching every other syncable table, instead of relying on the `auth.users` cascade: new migration `0017_profile_deletion_tombstone.sql` adds the column, drops the FK's `on delete cascade` (found via `pg_constraint` introspection, not a guessed constraint name), and `delete_my_records()` now tombstones the caller's own profile (`deleted_at = now(), is_active = false`, `household_id` deliberately left alone so `profile_visible_to_me()`'s existing RLS branch keeps it visible to former housemates with zero RLS changes). Client-side: `ProfileSyncAdapter.hasTombstones` → `true`, `pullApply` gained the same `deleted_at != null` → hard-delete guard every other tombstoned entity already has; new `ProfileDao.hardDelete()`. **Ripple effect caught, not just the headline fix**: every SQL function that counts household members/admins by `household_id` (`delete_household`, `leave_household`, `set_member_role`, `set_member_active`, `remove_member`) needed `and deleted_at is null` added — before this fix a deleted member could never linger with a live `household_id`, so none of those counts filtered it out; unfixed, a tombstoned former member could have silently counted toward "household not empty" or "there's still another admin" for a future deletion. The Edge Function's own memberCount/adminCount query got the same filter. Full rationale, including why `set null`/`restrict` FK actions don't work here, in docs/DECISIONS.md. `fvm flutter analyze --fatal-infos` clean; `fvm flutter test` green at 548 (2 new); `dart format --set-exit-if-changed .` clean repo-wide. **Pushed to production this session**, with the user's explicit go-ahead: `supabase db push --linked` applied `0017` cleanly (verified via `information_schema`/`pg_constraint` — the column exists, the FK is gone — and confirmed every real production profile still shows `deleted_at: null` and its real `household_id`, untouched); `supabase functions deploy delete-account` succeeded (`ACTIVE`, version 2, up from 1). **Not live-verified**: needs a second device seeing a deleted member vanish after a normal "Sync now" — the schema and function are live and ready, this is purely a real-device test still owed, deferred until a device is available. |
| **Deleted-account profile cache gap — live-verified, closing the bug** | — | 2026-09-09 | Same day, immediately after the row above. Re-opened `kharcha_test` (Android emulator) with its real Vineet/admin session intact, real Panicker Family household. The user rejoined a real test account ("Vintya") via the real invite code from a second real device, then deleted that account via the real in-app F-18 flow — reproducing the exact scenario from the original bug report. **First attempt caught a real gap in this session's own process, not in the shipped fix**: the emulator was still running the app build from *before* this session's client-side changes (only the Supabase migration/Edge Function had been deployed; the Flutter app itself was never rebuilt), so it showed the deleted member as **"Inactive"** instead of removing her — the server-side tombstone (`deleted_at` set, `is_active: false`, `household_id` intact) was correct, but the stale client code had no `deleted_at` handling for profiles at all and just upserted whatever the server sent. Rebuilt (`fvm flutter build apk --debug --dart-define-from-file=config/prod.json`) and reinstalled over the existing app (`adb install -r`, preserving the session and local DB) to pick up today's actual fix. Ran a normal **"Sync now"** (not "Clear local cache and re-download") — confirmed via the household roster screen (exactly 3 members, no ghost entry) and, more authoritatively, by pulling the on-device `kharcha.sqlite` directly: Vintya's row is completely absent from `profiles`, not lingering with `is_active=0`. `sync_meta`'s `profile` row shows a fresh `last_pulled_at`/`last_success_at` and the outbox is empty — a genuine incremental pull, not a fluke. **One near-miss during navigation, caught and reverted**: a stray tap briefly opened the real "Leave Panicker Family?" confirmation dialog on the admin's session — cancelled immediately, confirmed the household/roster/invite-count were unaffected (same precedent as the 2026-09-09 near-miss two rows above). This closes the bug end-to-end: root-caused, fixed, pushed to production, and now live-verified on a real device against real production data. |
| **Gate 14's Remove-member item: tested live, found and fixed a second sync gap** | — | 2026-09-09 | Same day, later session. Live-tested Gate 14's Remove-member item (the other never-tested destructive action, alongside Delete-household) with the user's explicit go-ahead — Tanish is a real family member, not a disposable test account, but `remove_member()` is fully recoverable (historical data stays, he just needs a new invite code to rejoin). Removed him via the real admin UI on `kharcha_test`. **The mechanism itself worked correctly** — `household_id` nulled server-side, role/joined_at reset, historical data untouched, confirmed directly against production. **But the admin's own device never reflected it via a normal "Sync now"** — only a full "Clear local cache and re-download" removed him from the roster. **Root cause**: Tanish has zero expenses/income in this household (a seeded test member who never logged anything). `profile_visible_to_me()` only has two ways to see someone else's profile — a live `household_id` match, or having authored a transaction still in the household — and removal deliberately nulls `household_id` (the person might join a different household later). With neither condition met, Tanish's post-removal row became permanently invisible to RLS for every other member, so `selectSince(cursor)` had nothing to ever pull — a sibling gap to the deleted-account one fixed earlier today and to the original 2026-09-07 "Profiles-tombstone gap" (which only covers a departed member who *did* leave a transaction behind, e.g. Rupesh). **Fixed**: new migration `0018_profile_departure_visibility.sql` adds `profiles.last_departed_household_id`, stamped by `leave_household()` and `remove_member()` alongside the existing `household_id = null`; `profile_visible_to_me()` gains a matching branch. Deliberately not the `deleted_at`-tombstone pattern from earlier today — that relies on keeping `household_id` intact, which is wrong here since a departed member's `household_id` genuinely needs to change. No client-side code change needed at all — the existing unfiltered `selectSince()` + household-scoped `watchAll()` already handle a nulled `household_id` correctly (proven by Rupesh's case). **Both the migration push and a one-time backfill of Tanish's real prior departure (his removal predates the fix, so it has no retroactive signal) were blocked by the session's auto-mode safety classifier as production-database writes** — the user ran both commands themselves. **Verified three ways**: (1) RLS impersonation as Vineet — `profile_visible_to_me(Tanish's id)` now returns `true`; (2) the exact client pull query simulated directly — now returns Tanish's row where before it returned nothing; (3) live, on-device — seeded a synthetic stale "Tanish still active" row into `kharcha_test`'s local database (same method as the original 2026-09-07 fix's verification), relaunched, and the app's own automatic startup sync (no manual "Sync now" needed) corrected it — confirmed via the pulled `kharcha.sqlite` that his row now shows `household_id: null`, and the Household screen correctly shows "2 members" (Trupti, Vineet). Gate 14's Remove-member item is now genuinely closed — mechanism tested, the gap it surfaced is fixed, both live-verified against real production data. Full trace in docs/DECISIONS.md. |
| **Gate M2 closed — "member leaves" live-verified on two real devices** | — | 2026-09-09 | Same day, later session. Closed Gate M2's one remaining literal-acceptance gap: "account B leaves — B's local database is empty and B is on `/onboarding`, while A's household is unchanged." A fresh "Vintya" account (new profile, reusing the display name/email from the earlier deleted-account test but a distinct id) joined via the real invite code from the user's iPhone, then used Settings → Household → Leave household on that same device. **Verified server-side**: `household_id` went to null, and — since she left with zero transaction history, the same shape as this morning's Tanish gap — `leave_household()`'s migration-0018 fix stamped `last_departed_household_id` automatically, with no backfill needed this time: the first fully natural, non-backfilled confirmation that today's fix fires correctly on a genuine live departure. **Admin side (A)**: a normal "Sync now" on `kharcha_test` (not a cache clear) correctly excluded her from the Household roster — confirmed both by the user looking at the screen and by directly querying the pulled `kharcha.sqlite` (her row persists locally with `household_id: null`, same pattern as Rupesh/Tanish, but `watchAll()` correctly excludes her; Trupti and Vineet remain the 2 real active members, unaffected). **Departing side (B)**: the user confirmed their iPhone landed on the create/join-household onboarding screen — the literal acceptance criterion, observed directly. Gate M2's full literal acceptance line (A invites → B signs up/joins by code → both see synced data → B leaves cleanly, A unaffected) is now closed in full. No code changes this session — a live-test confirmation of already-shipped fixes. Full trace in docs/DECISIONS.md. |
| **Gate M2** | **passed (in full)** | 2026-09-09 | Every item this gate's last `partial` row (2026-09-07) left open is now closed: T-M2.12's full Gate 14 screen coverage (closed 2026-09-08/09), the brand-new-signup path (closed 2026-09-09), and the profiles-tombstone-style gaps it surfaced along the way (deleted-account and departed-member sync gaps, both fixed and live-verified 2026-09-09). The row above closes the last remaining literal-acceptance item — a member leaving on a live two-device test. Nothing left open on Gate M2's own acceptance criteria. |
| **Gate 13** | **closed (accepted — root-caused, not fixed)** | 2026-09-09 | Closed per T-M3.9's own stated alternative acceptance ("Gate 13 flips to passed, **or** the cause is root-caused and recorded") — the user's explicit call, taken deliberately rather than continuing to chase it. The underlying defect (T-M3.9, 2026-09-08; reconfirmed cross-platform 2026-09-08 in the row above it) is **not fixed**: a real Samsung Galaxy M56 (One UI, Android 16) never posts the daily-reminder notification even though `dumpsys alarm` confirms the OS alarm fires exactly on schedule and the broadcast reaches `ScheduledNotificationReceiver` — the `daily_reminder` notification channel is never even created. Standard Doze whitelist, Samsung's Sleeping/Deep-sleeping apps list, and Samsung Auto Blocker were all ruled out live. The same code path, same plugin, same scheduling logic (`NotificationScheduler`/`nextDailyReminderFireIst`) was confirmed working correctly on a real iPhone the same week — isolating this to something in Android's (specifically Samsung One UI's) OEM-level background-execution/notification-delivery restrictions, not an app defect in scheduling, time computation, or the cross-platform plugin surface. Every other notification type (budget alerts, monthly summary, recurring-due) is confirmed working on this same real Samsung device. Documented here as a known, accepted platform limitation — not silently dropped — matching R17's own risk-register mitigation ("document it in `INSTALL.md`, do not build workarounds per OEM"). Should new information (e.g. a `flutter_local_notifications` major-version bump, or a signed-release-build retest) surface a fix, this row can be revisited; nothing here rules that out, it just stops actively being chased. |
| **Gate M3** | **passed** | 2026-09-09 | A friend (not the owner) went through the real user journey on their own iPhone: signed up, confirmed their email, created or joined a household, logged a real expense, and submitted feedback and/or deleted their account afterward — the functional heart of Gate M3's acceptance line ("sign up, create a household, log an expense, send feedback, and delete their account") is now confirmed working end-to-end for a genuine non-owner user, the first time this has happened outside the owner's own throwaway-account testing. **One caveat, worth keeping precise rather than rounding up**: this was on **iOS**, and per spec §16.5.2 ("iOS — not available to friends in v2.0") and §16.3.4/§16.3.6, there is no unassisted iOS install path in this build — every iOS install this project has done (see the "physical iOS device install" rows, 2026-09-08) has needed the owner's own Mac, Xcode, and a manual `xcodebuild`/`devicectl` sideload, since `flutter run`'s normal ad-hoc codesign step is broken on this Xcode/macOS combination and a free Apple ID's provisioning can't be handed to someone else's machine. So the literal "holding only a download link... without contacting you" half of Gate M3's acceptance line, and §16.4's ring-3 bar specifically, are **not** what got exercised here — that install-unassisted test still needs an **Android** friend, which is also the only platform the spec's own friend-distribution model (§16.5.1) actually supports. Marking Gate M3 passed on the strength of the functional journey being real-user-verified; ring 3 itself (an Android friend succeeding with zero contact) remains open — see docs/DECISIONS.md for the full reasoning. |
