| Task   | Status | Date       | Notes                                                              |
|--------|--------|------------|---------------------------------------------------------------------|
| T-0.1  | done   | 2026-09-03 | CLT/Homebrew/git pre-existing. CocoaPods, Android Studio via brew. JDK 17 via manual Temurin tar.gz (see DECISIONS.md). Xcode 26.6 installed from App Store, switched via xcode-select, license accepted, iOS 26.5 simulator runtime installed. |
| T-0.2  | done   | 2026-09-03 | FVM 4.3.0 installed. Flutter 3.47.2 (stable) installed and pinned via `fvm use stable`. Version recorded in DECISIONS.md. |
| T-0.3  | done   | 2026-09-03 | Flutter project created in place at repo root (not `~/Developer`, see DECISIONS.md) with org `com.panicker`, name `kharcha`, platforms android+ios. |
| T-0.4  | done   | 2026-09-03 | `.gitignore` extended per §4.7. `docs/SPEC.md`, `docs/DECISIONS.md`, `docs/PROGRESS.md` created. |
| T-0.5  | done   | 2026-09-03 | All packages from §9.3 added except `custom_lint`/`riverpod_lint` (deferred, see DECISIONS.md). `integration_test` added as an SDK dependency. |
| T-0.6  | done   | 2026-09-03 | `flutter_lints` (from template) + default `analysis_options.yaml`. `flutter analyze --fatal-infos` passes with 0 issues. |
| T-0.7  | done   | 2026-09-03 | Android `minSdk = 26`, `compileSdk = 37` (bumped from default 36 for plugin requirements), core library desugaring enabled for `flutter_local_notifications`. iOS deployment target 15.0 (Flutter template default). |
| Gate 0 | partial | 2026-09-03 | **Android: PASSED** — empty app built, installed, and ran on emulator `kharcha_test` (Pixel 6, API 34), confirmed via live `flutter run` session. **iOS: BLOCKED**, not passed — see DECISIONS.md for the `com.apple.provenance`/codesign issue. Proceeding to Phase 1 on the Android path per user instruction; iOS to be revisited later. |
| T-1.1  | done   | 2026-09-03 | User created a Supabase account and the `kharcha` project (org "Panicker Family") in `ap-south-1` (Mumbai) via the dashboard, guided over Claude-in-Chrome. Project ref `jqorwgiowfxxgjvayznj`, URL `https://jqorwgiowfxxgjvayznj.supabase.co`. Project uses Supabase's newer key format (`sb_publishable_...`) instead of a legacy JWT anon key — used as the `SUPABASE_ANON_KEY` value. `config/dev.json` created (gitignored) with URL + publishable key; `config/example.json` created (committed placeholder). |
| T-1.2  | done   | 2026-09-03 | Supabase CLI 2.116.0 installed via `brew tap supabase/tap && brew install supabase/tap/supabase`. `supabase init` run at repo root. `supabase login`'s automatic browser flow does not work in a non-TTY/agent shell (`LegacyLoginMissingTokenError`) — worked around by having the user generate a personal access token from the dashboard and run `export SUPABASE_ACCESS_TOKEN=...` + `supabase link --project-ref jqorwgiowfxxgjvayznj` themselves in their own terminal (kept out of the chat transcript deliberately). Link succeeded. |
| T-1.3  | done   | 2026-09-03 | Migrations `0001_extensions.sql`–`0004_budgets_recurring.sql` written verbatim from spec §6.2–§6.5 and pushed to the live project via `supabase db push` (run by the user, token stayed in their shell). |
| T-1.4  | done   | 2026-09-03 | `0005_functions_triggers.sql` written from spec §6.6 and pushed live. |
| T-1.5  | done   | 2026-09-03 | `0006_rls.sql` written from spec §7 and pushed live (after the app_releases ordering fix — see DECISIONS.md). All 10 target tables show RLS enabled in the dashboard. |
| T-1.6  | done   | 2026-09-03 | `0007_views.sql` (reporting views + `get_dashboard` RPC) and `0010_app_releases.sql` pushed live. All 4 views (`v_month_household`, `v_month_user`, `v_month_category`, `v_month_income`) visible in Table Editor. |
| T-1.7  | done   | 2026-09-04 | `0008_storage.sql` (receipts bucket + policies) and `0009_seed.sql` pushed live — verified: household row + 20 categories + 6 payment methods present in Table Editor. 4 auth users created via the dashboard: Rupesh, Tanish, Trupti (`role='member'`) and Vineet (`role='admin'`), all auto-linked to the household via the `handle_new_user` trigger, `display_name`/`role` set via SQL editor. Household deliberately shipped with 4 members instead of the spec's 5 (per user — no 5th member needed right now). Sign-ups disabled: Authentication → Providers → Email → "Allow new users to sign up" turned off and saved ("Successfully updated settings"). |
| T-1.8  | done   | 2026-09-04 | All 8 RLS checks from spec §7.1 executed in the SQL editor via JWT-claim impersonation (`begin; set_config(...); set local role authenticated; <test>; rollback;`), against 2 throwaway expense rows inserted and later removed. All 8 passed as specified — see DECISIONS.md for the full per-check results. No policy changes needed. |
| **Gate 1** | **passed** | 2026-09-04 | Covered by the T-1.8 RLS checklist: RLS-1 confirmed a member JWT (Rupesh) can read all household expenses; RLS-2/3/4 confirmed a member cannot update, delete, or insert-as another member. |
| **Bug found & fixed** | — | 2026-09-03 | `0006_rls.sql` (as copied verbatim from spec §7) enabled RLS on `app_releases` before that table exists (it's created in `0010_app_releases.sql`) — `supabase db push` failed with `relation "public.app_releases" does not exist`. Fixed by moving the `app_releases` RLS-enable + policy from `0006_rls.sql` into `0010_app_releases.sql`. Recorded in DECISIONS.md as a spec correction. |
| T-2.1  | done   | 2026-09-04 | `core/config/app_config.dart` implemented verbatim from spec §5.6. |
| T-2.2  | done   | 2026-09-04 | `core/money/money.dart`: `Money` extension type over paise, Indian-grouping `format()` via `intl`'s `en_IN` locale, compact L/Cr/k formatting, `tryParse`. 15 unit tests in `test/unit/core/money_test.dart`, incl. 1 lakh, 1 crore, and 99.995 rounding. |
| T-2.3  | done   | 2026-09-04 | `core/time/app_time.dart`: fixed UTC+5:30 offset helpers (no `timezone` package needed here — that lands in Phase 13 for notification scheduling). 9 unit tests incl. the 23:55-on-the-30th boundary case. |
| T-2.4  | done   | 2026-09-04 | `Result`/`Ok`/`Err` (`core/result/result.dart`), `Failure` taxonomy (`core/errors/failure.dart`), `ErrorMapper` (`core/errors/error_mapper.dart`), `AppLogger` with a 500-entry ring buffer (`core/logging/app_logger.dart`). 6 unit tests for the mapper's classification. |
| T-2.5  | done   | 2026-09-04 | All 11 Drift tables mirroring §6 (`Households`, `Profiles`, `Categories`, `PaymentMethods`, `Expenses`, `Incomes`, `Attachments`, `Budgets`, `RecurringRules`) plus `OutboxEntries` + `SyncMeta`, each syncable table carrying `syncStatus`/`localUpdatedAt`/`isDirty`. `AppDatabase` in `core/db/app_database.dart`, `schemaVersion = 1`. `dart run build_runner build` generates cleanly. |
| T-2.6  | done   | 2026-09-04 | 9 DAOs written (`ExpenseDao`, `IncomeDao`, `CategoryDao`, `PaymentMethodDao`, `BudgetDao`, `RecurringDao`, `AttachmentDao`, `OutboxDao`, `SyncMetaDao`) under `core/db/daos/`, each with watch/find/upsert/softDelete/markSynced/hardDelete. In-memory Drift tests in `test/unit/db/expense_dao_test.dart` (insert/read, soft-delete filtering + ordering, dirty/pending flagging). |
| T-2.7  | done   | 2026-09-04 | Freezed domain models for all 9 entities + 6 enums under `domain/models/`, each with `fromJson`/`toJson` and snake_case `@JsonKey`s matching the DB columns exactly. Mapper extensions (`toDomain()` / `toCompanion()`) between Drift rows and domain models under `data/local/mappers/` — see DECISIONS.md for the naming-collision handling this required. 9 JSON round-trip tests. |
| T-2.8  | done   | 2026-09-04 | Riverpod (`flutter_riverpod` + `riverpod_generator`, code-generated) wired via `ProviderScope` in `main.dart`; `appDatabaseProvider` (keepAlive) in `core/db/database_provider.dart`. |
| T-2.9  | done   | 2026-09-04 | `go_router` set up in `routing/app_router.dart` with the full route map from spec §10.1 (`StatefulShellRoute.indexedStack` for the 4-tab bottom nav + top-level modal routes for expense/income/budget/recurring/etc.), backed by placeholder screens under `features/*/screens/`. Auth redirect logic (T-3.4) not yet wired — router boots straight to the dashboard for now. |
| **Gate 2** | **passed** | 2026-09-04 | `fvm flutter analyze --fatal-infos` clean. `fvm flutter test` green (44 tests). Verified live on the Android emulator (`kharcha_test`, Pixel 6-class, API 34) via `fvm flutter run --dart-define-from-file=config/dev.json`: app builds, installs, and boots straight to the placeholder Dashboard with working bottom-nav tabs and FAB — no crashes, no Dart exceptions. Screenshot captured via `adb screencap`. |
| T-3.1  | done   | 2026-09-04 | `Supabase.initialize()` (with `publishableKey:`, not the deprecated `anonKey:`) plus `timezone` init added to `main()`'s bootstrap, per spec §11.1 steps 1–4. Steps 6 (notifications) and 8 (kick off sync) deferred to Phase 13/Phase 4 as noted inline. |
| T-3.2  | done   | 2026-09-04 | `AuthRepository` (`data/repositories/auth_repository.dart`): sign in, sign out, reset password, `currentSession`, `onAuthStateChange`, all returning `Result<void, Failure>`. Unit tests in `test/unit/data/auth_repository_test.dart` against a mocktail-mocked `GoTrueClient`. |
| T-3.3  | done   | 2026-09-04 | Full login screen + `LoginController`: inline email/password validation, password-visibility toggle, "Forgot password?" → reset email + "Check your email" snackbar. `ErrorMapper`'s invalid-credentials message corrected to the spec-exact "Email or password is incorrect." (was "Incorrect email or password." from T-2.4, before this string was load-bearing) — see DECISIONS.md. |
| T-3.4  | done   | 2026-09-04 | Router `redirect` + `GoRouterRefreshStream` (bridges Supabase's `onAuthStateChange` to go_router's `refreshListenable`): signed-out → `/login` always; signed-in bounced off `/login`/`/splash` → dashboard. `initialLocation` changed from the dashboard to `/splash`. |
| T-3.5  | done   | 2026-09-04 | New `ProfileDao` (registered on `AppDatabase`) + `ProfileRepository` + `currentProfileProvider`: resolves the signed-in member's profile from the Drift cache first (works offline immediately), with a best-effort background refresh from Supabase's `profiles` table. |
| T-3.6  | done   | 2026-09-04 | `AppDatabase.wipeAll()` (deletes every row from every table) wired into a new `SignOutController`, triggered from a minimal Sign-out control added to the Settings placeholder (full Settings UI is Phase 14). Offline session tolerance itself needed no new code — it's `supabase_flutter`'s default persisted-session behaviour; verified live (see Gate 3). |
| **Gate 3** | **passed** | 2026-09-04 | Verified live on the `kharcha_test` emulator. Signed-out boots to `/login` (screenshot). Real sign-in (user-entered credentials, not scripted) → dashboard. Force-stopped the app, fully severed connectivity (`svc wifi disable` + `svc data disable`, confirmed 0 connected networks via `dumpsys connectivity`), cold-relaunched → straight to dashboard, still signed in, no crash. Sign-out → back to `/login`; pulled the on-device `kharcha.sqlite` via `adb exec-out run-as ... cat` and confirmed all 11 tables have 0 rows. Two real bugs were found and fixed during this live pass — see DECISIONS.md. |
